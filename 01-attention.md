# 01. Attention — KV를 어떻게 줄일 것인가

`00-foundations`의 `0.2`에서 decode 한 스텝을 열 단계로 나눠 봤다. 그중 컨텍스트 길이에
비례해 커지는 건 7번, **캐시 안의 K와 V 전부를 읽어 attention을 계산하는 단계** 하나뿐이었다.

이 파일은 그 한 단계를 공격하는 열 개의 절을 계보 순서로 따라간다.

전부 같은 질문에서 출발한다. **토큰마다 K와 V를 저장해야 하는데, 컨텍스트가 길어지면
감당이 안 된다. 어떻게 할 것인가.** 답은 크게 네 갈래로 갈렸다.

| 갈래 | 발상 | 모듈 |
|---|---|---|
| **헤드끼리 공유** | 헤드 수만큼 곱해지는 게 문제니 나눠 쓰자 | MQA, GQA |
| **저차원으로 압축** | 저장하는 값 자체를 작게 만들자 | MLA |
| **일부만 읽기** | 다 볼 필요가 있나? 중요한 것만 보자 | SWA, NSA, MoBA, DSA, CSA, HCA |
| **레이어끼리 공유** | 레이어마다 따로 가질 필요가 있나? | CLA, YOCO |

앞의 셋은 **가로 방향**(헤드·차원·토큰)이고 마지막 하나는 **세로 방향**(레이어)이다.
레이어 공유는 다른 축과 비교적 독립적으로 조합할 수 있지만, head 공유와 MLA처럼
같은 저장 구조를 바꾸는 방법은 보통 대안 관계다.

## 이 장의 발전 계보와 시스템 영향

| 단계 | 대표 기법 | 해결하려는 병목 | 핵심 아이디어 | 시스템 영향 |
|---|---|---|---|---|
| 기준점 | **MHA** | 모든 head의 KV가 컨텍스트와 함께 증가 | 각 query head가 독립된 K/V를 가짐 | 정확한 global read, decode가 **memory-traffic 중심** |
| head 공유 | **MQA · GQA** | KV head 수에 비례하는 용량·대역폭 | 여러 query head가 K/V를 공유 | KV memory 감소, batch 확대, **throughput 향상** |
| 차원 압축 | **MLA** | head 공유 뒤에도 큰 KV vector | latent 공간만 저장하고 projection을 흡수 | KV cache 대폭 감소, latent GEMM 증가, serving 효율 향상 |
| 읽기 범위 제한 | **SWA · NSA · MoBA · DSA** | 전체 KV scan과 `S²` prefill | window 또는 top-k block만 읽음 | attention FLOPs·traffic 감소, 대신 index·gather 비용 |
| 압축+선택 | **CSA · HCA** | 압축 후에도 남는 긴 문맥 scan | 압축 공간에서 후보를 만들고 선택 | KV와 FLOPs를 함께 줄이지만 불규칙 memory access 증가 |
| layer 공유 | **CLA · YOCO** | layer 수만큼 중복되는 KV | 여러 layer가 같은 KV를 사용 | 세로 방향 KV 감소, layer coupling·PP 제약 가능 |

> **이 장의 병목 이동:** KV **용량** → KV를 읽는 **대역폭** → 희소 선택의
> **불규칙 접근**. 저장량을 줄일수록 다음 문제는 “얼마나 작게 저장하나”보다
> **“필요한 KV를 얼마나 연속적으로 읽나”** 로 이동한다.

---

## 계보 지도

```
                            MHA  (2017)
                             │
        ┌────────────────────┼────────────────────┐
        │                    │                    │
 [헤드를 공유]        [차원을 압축]         [읽는 범위를 제한]
        │                    │                    │
      MQA (2019)             │                  SWA (2023)
        │                    │                    │
      GQA (2023) ───────►  MLA (2024)        StreamingLLM (2023)
                             │                    │
                    Gated Attention        [학습해서 고른다]
                       / Gated MLA            ┌───┴───┐
                        (2025)              NSA     MoBA  (2025)
                                              └───┬───┘
                                                  ┆
                                       MLA + 선택 = DSA  (2025)
                                                  │       ← 논문이 명시적으로 인용
                                      압축 + 선택 = CSA ─ HCA  (2026)
                                                    1:1 교차


 [레이어를 공유]  ── 위 어느 것과도 조합 가능한 직교 축
        CLA · YOCO · MLKV  (2024)
```
> **그림 1.0** — 점선(┆)은 구조적 유사성, 실선은 후속 논문이 명시한 연결이다.

**목차**

| | 모듈 | 한 줄 |
|---|---|---|
| [1.1](#11-mha--기준점) | **MHA** | 헤드마다 독립적인 K, V |
| [1.2](#12-mqa--극단적으로-줄이기) | **MQA** | KV 헤드를 하나로 |
| [1.3](#13-gqa--그-사이-어딘가) | **GQA** | 그룹 단위 공유 |
| [1.4](#14-mla--저장하는-값-자체를-압축) | **MLA** | 저차원 latent로 압축 후 흡수 |
| [1.5](#15-gated-attention--출력에-문을-달다) | **Gated Attention** | 출력에 게이트 |
| [1.6](#16-swa와-attention-sink--읽는-범위를-제한하기) | **SWA · sink** | 최근 W개만 |
| [1.7](#17-nsa와-moba--어디를-볼지-학습하기) | **NSA · MoBA** | 어디를 볼지 학습 |
| [1.8](#18-dsa--싸구려-색인으로-고르기) | **DSA** | 저렴한 indexer로 top-k |
| [1.9](#19-csa와-hca--압축과-선택을-겹치기) | **CSA · HCA** | 압축 후 선택 |
| [1.10](#110-cla와-yoco--레이어끼리-공유하기) | **CLA · YOCO** | 레이어 방향 공유 |
| [정리](#전체-비교) | | 전체 비교표 |

---

## 1.1 MHA — 기준점

*Multi-Head Attention*

### 왜 나왔나

MHA는 개선이 아니라 출발점이다. 2017년 Transformer 논문이 제안한 원형이고,
이후 모든 변형이 "MHA 대비 무엇을 바꿨나"로 설명된다.

당시의 관심사는 메모리가 아니었다. **하나의 attention으로는 한 가지 관계밖에 못 본다**는
게 문제였다. 어떤 단어가 문법적으로 어디에 걸리는지, 어떤 대명사가 무엇을 가리키는지는
서로 다른 종류의 관계인데, attention을 한 번만 하면 그걸 평균 내버린다.

### 아이디어와 구조

`d`차원을 `n_h`개로 쪼개고, 각 조각마다 **독립적인** `Wq`, `Wk`, `Wv`를 둔다.
헤드마다 다른 관점으로 문맥을 보게 하고, 결과를 이어붙여 `Wo`로 되돌린다.

```
        ┌─ Wq₁,Wk₁,Wv₁ ─ attention ─┐
        │                            │
 x ─────┼─ Wq₂,Wk₂,Wv₂ ─ attention ─┼─ concat ─ Wo ─► y
 (d)    │                            │
        └─ Wq_h,Wk_h,Wv_h ─ attn ───┘

 헤드 h개, 각각 d_h = d / n_h 차원
```
> **그림 1.1** — MHA. 헤드마다 K와 V가 따로 있다는 점이 이 파일 전체의 출발점이다.

각 헤드는 독립적으로 이 계산을 한다.

```
attention(q, K, V) = softmax(q · Kᵀ / √d_h) · V
```

### 추론에서 달라진 것

기준점이므로 비교 대상이 없다. 대신 여기서 KV cache가 어떻게 생겼는지 짚어둘 필요가 있다.

재미있는 건 **원 논문에는 KV cache라는 개념이 없다**는 점이다. 학습은 시퀀스 전체를
병렬로 처리하므로 캐시가 필요 없다. KV cache는 이후 autoregressive 생성을 실용화하는
과정에서 "이미 계산한 K, V를 다시 계산하지 말자"는 당연한 최적화로 등장했다.

즉 **MHA는 KV cache를 염두에 두고 설계되지 않았다.** 이 파일의 나머지 아홉 모듈은
그 사실의 뒷수습이라고 봐도 된다.

### 토큰 하나가 지나가는 길

`00-foundations`의 `0.2` 표 그대로다. 특기할 것은 3번 단계에서 `k`, `v`를
**`n_h`개 헤드 전부**에 대해 만들고, 전부 캐시에 넣는다는 점이다.

### 코드와 텐서

decode 한 스텝에서 일어나는 연산을 전부 펼치면 이렇다.

| # | 연산 | 식 | shape 변화 | 종류 |
|---|---|---|---|---|
| 1 | Q 투영 | `x @ Wq` | `(B,1,d) × (d, n_h·d_h)` → `(B,1,n_h,d_h)` | **GEMV** |
| 2 | K 투영 | `x @ Wk` | `(B,1,d) × (d, n_h·d_h)` → `(B,1,n_h,d_h)` | **GEMV** |
| 3 | V 투영 | `x @ Wv` | `(B,1,d) × (d, n_h·d_h)` → `(B,1,n_h,d_h)` | **GEMV** |
| 4 | RoPE | 회전 | `(B,1,n_h,d_h)` 유지 | elementwise |
| 5 | 캐시 append | 복사 | `(B,S−1,n_h,d_h)` → `(B,S,n_h,d_h)` | memory write |
| 6 | **점수** | `q @ Kᵀ` | `(B,n_h,1,d_h) × (B,n_h,d_h,S)` → `(B,n_h,1,S)` | **batched GEMV** ★ |
| 7 | 스케일 | `/√d_h` | 유지 | elementwise |
| 8 | softmax | | `(B,n_h,1,S)` 유지 | **reduction** |
| 9 | **가중합** | `p @ V` | `(B,n_h,1,S) × (B,n_h,S,d_h)` → `(B,n_h,1,d_h)` | **batched GEMV** ★ |
| 10 | 출력 투영 | `o @ Wo` | `(B,1,n_h·d_h) × (n_h·d_h, d)` → `(B,1,d)` | **GEMV** |

★ 표시한 6·9번만 `S`에 비례한다. 나머지 여덟 개는 컨텍스트가 1K든 1M이든 동일하다.

읽어둘 것이 세 가지다.

- **decode에서는 전부 GEMV다.** 곱해지는 쪽이 벡터 하나뿐이라 읽어온 가중치를 한 번씩만
  쓴다. `00-foundations` `0.6`에서 본 memory-bound가 바로 이 형태다.
- **prefill에서는 같은 표가 전부 GEMM이 된다.** `1`이 `S`가 되면서 6번은
  `(S,d_h) × (d_h,S) → (S,S)`가 되어 `S²`에 비례하는 진짜 행렬 곱이 된다.
- **6·9번이 읽는 것은 가중치가 아니라 KV 캐시다.** 1·2·3·10번은 배치가 커지면 가중치를
  나눠 쓸 수 있지만, 6·9번은 그럴 수 없다.

`d = n_h × d_h`이므로 헤드를 쪼개도 전체 연산량은 attention 한 번과 같다.
**연산량은 공짜인데 메모리는 아니다** — 캐시가 헤드 수만큼 늘어난다.

### 정리

| | |
|---|---|
| **핵심 아이디어** | `d`를 `n_h`개로 쪼개 헤드마다 독립적인 Q, K, V를 두고 병렬로 attention |
| **장점** | · 헤드마다 독립된 K, V 표현을 학습<br>· 구조가 단순하고 널리 지원됨<br>· 압축·공유 기법을 비교하는 기준점 |
| **한계** | · KV가 토큰·레이어당 `2 × n_h × d_h × b`. **헤드 수에 그대로 비례**<br>· 70B급에서 컨텍스트 32K·배치 32면 KV만 2.5 TB — 서빙 불가<br>· 헤드별로 KV를 따로 읽어야 해 decode에서 메모리 대역폭을 크게 먹음 |
| **대표 모델** | GPT-2, GPT-3, Llama 1, Llama 2 (7B·13B), OLMo 2, OLMo 3 — **2023년 이전의 사실상 전부** |
| **다음으로** | 헤드 수만큼 곱해지는 것이 문제다. 그렇다면 헤드끼리 나눠 쓰면 되지 않나 → **MQA** |

---

MHA의 KV는 헤드 수에 정비례한다. 헤드가 64개면 캐시도 64배다.

그런데 여기서 한 가지를 물을 수 있다. **쿼리는 헤드마다 달라야 하지만, K와 V도 정말
헤드마다 달라야 하나?** 헤드가 서로 다른 관점을 갖는다는 것은 "무엇을 찾는가(`q`)"가
다르다는 뜻이지, "무엇이 있는가(`k`, `v`)"까지 달라야 한다는 뜻은 아닐 수도 있다.

2019년 Shazeer의 답은 과감했다. **하나면 충분하다.**

---

## 1.2 MQA — 극단적으로 줄이기

*Multi-Query Attention*

### 왜 나왔나

논문 제목이 그대로 문제의식이다 — *Fast Transformer Decoding: One Write-Head is All You Need*.
학습이 아니라 **추론이 느린 것**이 표적이었고, 그중에서도 토큰을 하나씩 뱉는
incremental decoding이 대상이었다.

당시 진단은 지금과 같다. decode가 느린 이유는 연산이 많아서가 아니라 **메모리에서
읽어올 게 많아서**이고, 그 대부분이 KV cache다.

### 아이디어와 구조

쿼리 헤드는 `n_h`개 그대로 두고, **K와 V는 헤드 하나 분량만** 만든다.
모든 쿼리 헤드가 그 하나를 함께 본다.

```
        ┌─ Wq₁ ─┐
        │       │
 x ─────┼─ Wq₂ ─┼──► 모든 헤드가 같은 K, V를 봄
 (d)    │       │         ▲
        └─ Wq_h ┘         │
                    Wk, Wv 는 하나씩만
```
> **그림 1.2** — MQA. 쿼리는 여럿, K·V는 하나.

KV cache가 `n_h`배 줄어든다. Llama 3 70B 규모라면 토큰당 320 KB가 5 KB가 된다.

### 추론에서 달라진 것

| | MHA | MQA |
|---|---|---|
| KV에 저장 | 헤드마다 K, V | **K, V 하나씩만** |
| KV 크기 | 기준 | **1/n_h** |
| 연산량 | 기준 | 거의 동일 (`q`는 그대로 `n_h`개) |
| decode 속도 | 기준 | 크게 개선 — 읽을 게 확 줄어듦 |
| 대가 | — | 모든 헤드가 같은 K, V를 봄 |

주목할 점은 **연산량은 거의 그대로**라는 것이다. 쿼리 헤드가 `n_h`개인 건 변함없으니
곱셈 횟수는 같다. 줄어든 건 오직 메모리에서 읽어오는 양이다.
`00-foundations` `0.6`에서 본 대로 decode는 그 읽는 양이 속도를 정하므로,
연산은 그대로인데 속도만 빨라진다.

### 토큰 하나가 지나가는 길

`0.2`의 표에서 3번만 달라진다. `k`, `v`를 `n_h`개가 아니라 하나만 만들고,
캐시에도 하나만 쌓인다. 나머지 단계는 동일하다.

### 코드와 텐서

MHA 표(`1.1`)에서 달라지는 줄만 보면 이렇다.

| # | 연산 | 식 | shape 변화 | 종류 |
|---|---|---|---|---|
| 1 | Q 투영 | `x @ Wq` | → `(B,1,n_h,d_h)` | GEMV — **변화 없음** |
| 2·3 | **KV 투영** | `x @ Wk`, `x @ Wv` | `(B,1,d) × (d, d_h)` → `(B,1,1,d_h)` | GEMV — **가중치가 `n_h`배 작다** |
| 5 | 캐시 append | 복사 | → `(B,S,1,d_h)` | memory write — **1/n_h** |
| 6 | 점수 | `q @ Kᵀ` | `(B,n_h,1,d_h) × (B,**1**,d_h,S)` → `(B,n_h,1,S)` | batched GEMV + **브로드캐스트** |
| 9 | 가중합 | `p @ V` | `(B,n_h,1,S) × (B,**1**,S,d_h)` → `(B,n_h,1,d_h)` | batched GEMV + 브로드캐스트 |

6·9번의 **출력 shape는 MHA와 완전히 같다.** 쿼리 헤드가 `n_h`개인 건 변함없으니
곱셈 횟수도 같다. 달라진 건 오른쪽 피연산자의 헤드 차원이 `n_h`에서 `1`로 줄어
**같은 데이터를 `n_h`번 재사용**한다는 점뿐이다.

> 💡 여기가 MQA의 정체다. **연산량 그대로, 읽는 양만 `1/n_h`.**
> GEMV의 비용은 읽는 양이 정하므로(`0.6`), 이것만으로 decode가 빨라진다.

✅ `n_kv = 1`로 둔 GQA와 정확히 같다.
`snippets/test_equivalence.py::test_mqa_is_gqa_with_one_group`

### 정리

| | |
|---|---|
| **핵심 아이디어** | 쿼리 헤드는 그대로 두고 K, V는 하나만 만들어 전 헤드가 공유 |
| **장점** | · KV cache가 `n_h`배 감소 — 지금까지 나온 방법 중 가장 단순하면서 효과가 큼<br>· 연산량은 거의 그대로, 순수하게 메모리 이동량만 감소<br>· 구현이 간단하고 특수 커널이 필요 없음 |
| **한계** | · **품질 저하가 뚜렷하다.** 헤드를 여럿 둔 이유가 사라짐<br>· **텐서 병렬(TP)로 나눌 수 없다.** KV 헤드가 1개면 8장에 쪼갤 수 없어 전 GPU가 복제해 들고 있어야 하고, 줄인 메모리가 복제로 되돌아옴<br>· 학습이 불안정하다는 보고가 있었음 |
| **대표 모델** | PaLM, Falcon, StarCoder — **대형 모델에서 널리 쓰이지는 못했다.** 곧 GQA로 대체됨 |
| **다음으로** | 방향은 맞았는데 너무 많이 갔다. 전부 공유와 전혀 공유 안 함 사이를 고를 수 있으면 → **GQA** |

---

MQA는 확실히 효과가 있었지만, 대규모 모델에 적용하면서 두 가지 문제가 드러났다.

- **품질** — 헤드마다 다른 관점으로 문맥을 보라고 만들어둔 구조인데, 볼 수 있는 K와 V가
  하나뿐이면 그 다양성이 사라진다.
- **병렬화** — TP로 8장에 나눌 때 KV 헤드가 1개면 나눌 수가 없다. 8장이 같은 KV를
  복제해서 들고 있어야 하므로, 줄인 메모리가 복제로 되돌아온다.

GQA는 이 둘을 동시에 완화한다.

---

## 1.3 GQA — 그 사이 어딘가

*Grouped-Query Attention*

### 왜 나왔나

MHA는 KV가 너무 크고, MQA는 너무 많이 줄였다. 둘 중 하나를 고르는 대신
**그 사이를 고를 수 있게 하자**는 것이 출발점이다.

### 아이디어와 구조

쿼리 헤드를 몇 개씩 묶어 그룹을 만들고, 그룹 하나가 KV 헤드 하나를 공유한다.

```
   MHA (n_kv=8)              GQA (n_kv=2)              MQA (n_kv=1)

Q1 Q2 Q3 Q4 ...        Q1 Q2 Q3 Q4  Q5 Q6 Q7 Q8     Q1 Q2 ... Q8
 |  |  |  |             \  |  |  /   \  |  |  /       \  \  /  /
K1 K2 K3 K4 ...           \_____/       \_____/         \_____/
                            KV1           KV2             KV1

헤드마다 KV 하나        그룹당 KV 하나              전체가 KV 하나
```
> **그림 1.3** — `n_h = 8`일 때 세 방식의 KV 공유 구조

그룹 크기를 `g = n_h / n_kv`로 두면 세 방식이 하나의 축 위에 놓인다.

| `g` | 구조 | KV 캐시 | 비고 |
|---|---|---|---|
| 1 | MHA | 기준 | 그룹이 1개짜리 |
| 4 ~ 8 | **GQA** | 1/4 ~ 1/8 | 실무에서 쓰는 범위 |
| `n_h` | MQA | 1/`n_h` | 전부 한 그룹 |

GQA를 새로운 메커니즘으로 보기보다 **MHA와 MQA를 양 끝으로 갖는 연속적인 설정**으로
이해하는 편이 낫다. 두 극단 사이에 눈금을 긋는 이 방식은 뒤에서도 반복해서 나온다 —
sliding window와 full attention 사이(`1.6`), dense와 sparse 사이(`1.7`)가 모두 같은 구조다.

논문은 기존 MHA 체크포인트를 소량의 추가 학습(uptraining)으로 GQA로 전환하는 방법도
함께 제시했다. **이미 학습된 모델을 버리지 않아도 된다**는 점이 빠른 확산에 크게 기여했다.

### 추론에서 달라진 것

| | MHA | GQA |
|---|---|---|
| KV에 저장 | 헤드마다 K, V | **그룹마다** K, V |
| KV 크기 | 기준 | **1/g** |
| 새로 생긴 연산 | — | KV 헤드를 쿼리 헤드 수에 맞춰 펼치기 |
| 새로 생긴 제약 | — | TP 분할 시 `n_kv ≥ GPU 수` |
| prefill 영향 | — | 거의 없음 |
| decode 영향 | — | **읽을 KV가 1/g** |
| 대가 | — | 그룹 내 헤드가 같은 K, V를 봄 |

이득이 나오는 곳은 decode다. prefill은 원래 연산량이 많아 GPU가 바쁘게 돌아가지만,
decode는 토큰 하나를 만들려고 KV 전체를 읽어오는 것이 일의 대부분이다.
그 읽는 양이 `1/g`로 줄어든다.

### 토큰 하나가 지나가는 길

`0.2`의 표에서 3번(`k, v` 생성)이 `n_kv`개로 줄고, 7번(캐시 전체 읽기) 직전에
KV 헤드를 쿼리 헤드 수에 맞춰 펼치는 과정이 추가된다. 나머지는 동일하다.

### 코드와 텐서

| # | 연산 | 식 | shape 변화 | 종류 |
|---|---|---|---|---|
| 1 | Q 투영 | `x @ Wq` | `(B,1,d) × (d, n_h·d_h)` → `(B,1,n_h,d_h)` | GEMV |
| 2·3 | KV 투영 | `x @ Wk`, `x @ Wv` | `(B,1,d) × (d, n_kv·d_h)` → `(B,1,n_kv,d_h)` | GEMV — 가중치 `1/g` |
| 4 | RoPE | 회전 | 유지 | elementwise |
| 5 | 캐시 append | 복사 | → `(B,S,n_kv,d_h)` | memory write — **1/g** |
| 5.5 | **펼치기** | `repeat_interleave(g)` | `(B,S,n_kv,d_h)` → `(B,S,n_h,d_h)` | **개념상만. 아래 주의** |
| 6 | 점수 | `q @ Kᵀ` | `(B,n_h,1,d_h) × (B,n_h,d_h,S)` → `(B,n_h,1,S)` | batched GEMV ★ |
| 8 | softmax | | 유지 | reduction |
| 9 | 가중합 | `p @ V` | `(B,n_h,1,S) × (B,n_h,S,d_h)` → `(B,n_h,1,d_h)` | batched GEMV ★ |
| 10 | 출력 투영 | `o @ Wo` | `(B,1,n_h·d_h) × (n_h·d_h,d)` → `(B,1,d)` | GEMV |

5.5번이 이 모듈의 전부다. 나머지는 MHA와 shape까지 동일하고, 캐시에 실제로 담기는 양만
`1/g`다.

```python
# snippets/gqa.py 中
k = k.unsqueeze(3).expand(B, S, n_kv, g, d_h).reshape(B, S, n_h, d_h)
```

> ⚠️ **논문·HF 코드와 실제 커널이 갈리는 지점**
> HF `modeling_llama.py`는 `repeat_kv()`로 KV를 물리적으로 복제한다. 읽기 쉬우라고 그런 것이다.
> 반면 FlashAttention이나 vLLM 커널은 복제하지 않고 인덱싱만 다르게 한다.
> 복제하면 GQA로 줄인 메모리 트래픽이 도로 늘어나기 때문이다.
> **GQA의 실제 이득은 이 커널 구현에서 나온다.**

✅ `n_kv = n_h`로 두면 MHA와 출력이 정확히 일치한다.
`snippets/test_equivalence.py::test_gqa_reduces_to_mha`

### 정리

| | |
|---|---|
| **핵심 아이디어** | 쿼리 헤드를 `g`개씩 묶어 그룹을 만들고, 그룹 하나가 KV 헤드 하나를 공유 |
| **장점** | · KV 캐시 **1/g** — decode에서 읽는 양도 그대로 1/g<br>· `g`로 품질과 메모리 사이 어디든 선택 가능 (MHA↔MQA 연속축)<br>· `n_kv`를 GPU 수에 맞추면 TP로 깔끔하게 분할 (MQA의 복제 문제 해결)<br>· 기존 MHA 체크포인트에서 uptraining으로 전환 가능 |
| **한계** | · 그룹 내 헤드가 같은 K, V를 봄 → 표현력 손실<br>· KV가 **여전히 컨텍스트 길이에 비례**. `g`는 상수배 절감일 뿐<br>· `g`를 계속 키우면 MQA의 품질 문제로 회귀 |
| **대표 모델** | **Llama 2 70B**, **Llama 3 70B**(`n_h`=64 / `n_kv`=8), Gemma 3 |
| **다음으로** | 헤드를 나눠 갖는 방식으로는 상수배가 한계다. 저장하는 것 자체를 압축해야 한다 → **MLA** |

---

GQA는 훌륭한 타협이지만 근본적인 한계가 있다. `g`를 8로 하면 8배, 16으로 하면 16배 —
**상수배 절감**이다. 그리고 `g`를 키울수록 MQA의 품질 문제로 되돌아간다.

DeepSeek가 던진 질문은 달랐다. **헤드끼리 나눠 갖는 대신, 저장하는 값 자체를 작게
만들 수는 없나?** K와 V가 `d_h`차원짜리 벡터라면, 그걸 더 작은 차원으로 눌러 담았다가
쓸 때 펴면 되지 않나.

문제는 "쓸 때 편다"는 부분이다. 펴는 연산이 추가되면 decode가 더 느려질 텐데.
MLA의 진짜 기여는 **그 펴는 연산을 없애버린 것**에 있다.

---

## 1.4 MLA — 저장하는 값 자체를 압축

*Multi-head Latent Attention*

### 왜 나왔나

GQA로는 상수배가 한계다. 그리고 그 상수배마저 표현력을 깎아서 얻은 것이다.
DeepSeek-V2는 **KV를 저차원으로 압축해 저장**하는 쪽을 택했다.

### 아이디어와 구조

기본 발상은 단순하다. `x`로부터 K, V를 바로 만드는 대신,

1. 먼저 **작은 latent 벡터 `c_KV`** 로 눌러 담고 (down-projection)
2. **캐시에는 `c_KV`만 저장**하고
3. 필요할 때 `c_KV`에서 K, V를 복원한다 (up-projection)

`c_KV` **하나에서 K와 V가 둘 다** 나온다. K, V를 따로 저장하던 것이 latent 하나로 합쳐진다.

```
            ┌─ W^DKV ─► c_KV (512) ──► [캐시]
            │              ├─ W^UK ─► k_C  (헤드당 128, 내용부)
 x ─────────┤              └─ W^UV ─► V    (헤드당 128)
 (7168)     └─ W^KR ─ RoPE ─► k_R (64) ──► [캐시]  ← 모든 헤드 공유
```
> **그림 1.4** — MLA. 캐시에 들어가는 건 `c_KV`와 `k_R` 둘뿐이다.
> DeepSeek-V3 기준 512 + 64 = **576개 값**. `k_C`와 V는 저장하지 않는다.

#### 키가 두 조각인 이유 — `k_R`의 정체

RoPE는 위치에 따라 벡터를 회전시키는데(`04-position`), 회전량이 **토큰 위치마다
다르다.** 아래의 흡수 트릭은 `W^UK`를 쿼리 쪽에 **미리** 곱해두는 것인데, 그 사이에
위치별로 다른 회전이 끼어들면 미리 곱해둘 수가 없다. DeepSeek의 해법은 **키를 두
조각으로 쪼개는 것**이었다.

| 조각 | 기호 | 차원 (V3) | 위치 정보 | 헤드별? | 어떻게 만드나 |
|---|---|---|---|---|---|
| **내용부** | `k_C` | 128 / 헤드 | 없음 | 헤드마다 다름 | `c_KV`에서 `W^UK`로 복원 → **흡수 대상** |
| **위치부** | `k_R` | 64 | **RoPE 적용** | **전 헤드 공유** | `x → W^KR →` 회전 |

즉 `k_R`은 **"RoPE를 걸어놓은 채 따로 떼어둔 작은 키 조각"** 이다(R = RoPE).
헤드가 쓰는 키는 이 둘을 이어붙인 192차원이고, 점수도 두 내적의 합이 된다.

```
k_head = [ k_C (128, 헤드별) ; k_R (64, 공유) ]
score  =   q_C · k_Cᵀ    +    q_R · k_Rᵀ
           └ 내용 매칭 ┘      └ 위치 매칭 ┘      ← 표의 5·6번이 쪼개진 이유
```

> 📌 `k_C`를 캐시하지 않는 건 `c_KV`에서 나오기 때문이고(사실 나올 필요조차 없다),
> `k_R`은 압축 대신 **전 헤드 공유**로 값을 줄인다 — 헤드가 128개여도 64개면 끝이다.
> **"RoPE를 포기할 수 없어서 낸 64차원의 세금"** 인 셈. 이 구조가 decoupled RoPE다.

#### Q 쪽은 어떻게 되나

**Q도 대칭적으로 두 조각이지만, 압축의 목적이 다르다.** Q는 현재 토큰 것 하나만
있으면 되므로 애초에 캐시하지 않는다. KV 압축이 캐시를 줄이려는 것이라면, Q 압축은
파라미터와 학습 시 활성값을 줄이려는 것이다 (V3의 `q_lora_rank` = 1536).

```
 x ─ W^DQ ─► c_Q (1536) ─┬─ W^UQ ─► q_C (헤드당 128)  ─────────── 내용부
                         └─ W^QR ─► q_R (헤드당 64) ─ RoPE ────── 위치부
```

키와 다른 점 하나. **`q_R`은 헤드마다 다르다.** 공유가 필요했던 건 저장하는 쪽뿐이다.

#### 핵심 트릭 — 흡수(absorption)

여기까지는 "저장은 줄지만 복원 연산이 붙는다"는 뻔한 거래다.
MLA의 기여는 그 **복원을 아예 없앤 것**에 있다. `k_C = c_KV · W^UK`이므로

```
q_C · k_Cᵀ  =  q_C · (c_KV · W^UK)ᵀ  =  (q_C · W^UKᵀ) · c_KVᵀ
                └─── 원래: 캐시를 편다 ───┘   └── 흡수: 쿼리를 옮긴다 ──┘
```

**왜 이득인가 — 복원이 1회성 비용이 아니기 때문이다.** 핵심은 `W^UK`를 **몇 개에
곱하느냐**다. 캐시 쪽에 곱하면 `S`개 전부, 쿼리 쪽에 곱하면 1개다.

| (V3, 레이어 1개·토큰 1개 생성·`S`=4096) | 복원(naive) | 흡수(absorbed) |
|---|---|---|
| `W^UK`를 곱하는 대상 | 캐시의 **모든 토큰** — `S`개 | **현재 쿼리 하나** |
| 곱셈 횟수 | `S × 512 × 32768` ≈ **69G** | `128 × 128 × 512` ≈ **0.008G** |
| 중간 텐서 K,V (bf16) | `S × 32768` ≈ **268MB** | **없음** |

"복원한 K, V를 캐시하면 되지 않나?" 싶지만 그러면 캐시가 MHA 크기로 돌아간다.
압축한 의미가 사라지므로, 복원은 **매 디코드 스텝마다 컨텍스트 전체에** 되풀이할
수밖에 없다. decode가 memory-bound임을 떠올리면 세 번째 줄이 더 아프다 — naive는
매 스텝 268MB를 만들어 HBM에 쓰고 다시 읽는다. **압축해 저장해도 쓸 때마다 펴면
남는 게 없다.**

**"latent 공간에서 바로 계산한다"의 의미.** attention 점수는 내적 = 유사도이고,
내적은 두 벡터가 **같은 좌표계**에 있을 때만 성립한다. 흡수는 그 **비교의 무대를
옮기는 것**이다.

| | 비교가 일어나는 공간 | 무대로 올라와야 하는 쪽 |
|---|---|---|
| 원래 | 헤드별 키 공간 (128차원) | **캐시** `c_KV → k_C`, **`S`개 전부** |
| 흡수 | **latent 공간 (512차원)** | **쿼리** `q_C → q̃`, **1개** |

`q̃ = q_C · W^UKᵀ`는 "이 헤드의 질문을 latent 좌표계의 언어로 번역한 것"이다.
캐시는 이미 그 좌표계에 있으니 번역 없이 곧바로 내적하면 되고, 번역기 `W^UK`가
헤드마다 다르므로 **헤드별 개성도 그대로 살아 있다.** 사라진 건 캐시를 펴는 작업뿐이다.

> 💡 환전 비유 — 캐시의 가격 `S`건은 "latent 통화", 쿼리는 "헤드 통화"로 적혀 있다.
> `S`건을 환전하든 쿼리 1건을 환전하든 비교 결과는 같은데, 비용이 `S`배 다르다.

그 결과 흡수된 쿼리 `[q̃(512) ; q_R(64)]`는 캐시 엔트리 `[c_KV(512) ; k_R(64)]`와
**차원이 정확히 같아진다(576).** 즉 흡수 후의 MLA는 `head_dim`=576, **KV 헤드 1개짜리
MQA**와 계산 형태가 똑같다. 표의 "모든 헤드가 같은 latent를 읽음"이 이 뜻이다.
다만 각 헤드가 자기 `W^UK`로 번역해 들어오므로 **MQA의 메모리 이점 + MHA의 표현력**이 된다.

V 쪽도 논리가 같다. `(p · c_KV) · W^UV · W^O` 순으로 묶으면 가중합을 latent에서
먼저 끝내게 되어, `W^UV`도 `S`개가 아니라 **결과 벡터 하나**에만 걸린다.

**공짜는 아니다.** `q̃`는 헤드당 512차원으로 원래 `q_C`(128)보다 4배 크다.
쿼리·출력 쪽 projection의 곱셈량이 그만큼 늘어난다. 읽는 양은 줄고 곱하는 양은 느는
거래이며, decode는 memory-bound이므로 대개 이득이지만 batch가 커지면 달라진다.

> ⚠️ `W^Q`와 `W^UK`를 하나의 행렬로 미리 합쳐둔다는 서술을 종종 보는데, 실제 커널은
> 대개 그러지 않는다 — 합치면 가중치가 4배로 불어난다(V3 출력 쪽만 117M → 470M/레이어).
> 런타임에 헤드별 행렬곱으로 적용하며, 수학적으로 같다. 요점은 어느 쪽이든
> **`S`개가 아니라 1개에 적용한다**는 것이다.

#### 얼마나 줄어드나

DeepSeek-V3 기준으로 토큰·레이어당 **576개 값**이다.
같은 모델이 MHA였다면 `2 × 128 × 128 = 32,768`개다.

📌 [T1] DeepSeek-V2 논문은 이를 **"2.25개 그룹짜리 GQA에 해당한다"** 고 표현한다.
GQA는 `g`를 아무리 키워도 `n_kv = 1`(MQA)이 하한인데, MLA는 그 아래로 내려간 셈이다.

### 추론에서 달라진 것

| | GQA | MLA |
|---|---|---|
| KV에 저장 | 그룹별 K, V | **latent `c_KV` + 공유 `k_R`** |
| 저장량 (V3 기준) | — | 576개 값 / 토큰 / 레이어 |
| 압축 해제 비용 | — | **0 — 가중치에 흡수됨** |
| 읽기 패턴 | 그룹별로 나눠 읽음 | **모든 헤드가 같은 latent를 읽음** |
| 연산량 | 기준 | **오히려 증가** (아래) |
| 필요한 것 | 표준 커널 | **전용 커널** (FlashMLA 등) |

두 줄이 중요하다.

**읽기 패턴**: 흡수된 형태에서는 모든 헤드가 동일한 latent를 읽는다. 메모리 접근
패턴만 보면 MQA와 비슷한데, 헤드별 표현력은 유지된다. **MQA의 메모리 이점과 MHA의
표현력을 동시에 노린 것**이라고 볼 수 있다.

**연산량 증가**: 흡수된 `q · W^UKᵀ`는 원래 `q`보다 차원이 크다. 읽는 양은 크게 줄지만
곱셈 횟수는 늘어난다. decode는 memory-bound이므로 대개 이득이지만, **공짜는 아니다.**
`00-foundations` `0.4`에서 말한 압력 이전의 한 예다.

### 토큰 하나가 지나가는 길

naive와 absorbed 두 경로가 다르다.

| | naive (논문 수식대로) | absorbed (실제 추론) |
|---|---|---|
| 1 | `c`를 캐시에서 읽기 | `c`를 캐시에서 읽기 |
| 2 | `c → k_C, V` 복원 — **`S`개 전부** | *(생략)* |
| 3 | `q_C · k_Cᵀ` | `(q_C·W^UKᵀ) · cᵀ` — latent 공간에서 직접 |
| 4 | `p · V` | `p · c` |
| 5 | `· W^O` | `· (W^UV·W^O)` — 미리 곱해둔 것 |

**같은 결과가 나오지만 메모리 접근량이 완전히 다르다.** naive는 복원된 K, V를 들고
있어야 하므로 MHA와 다를 게 없다. 흡수를 해야 MLA가 MLA가 된다.

### 코드와 텐서

흡수된 형태(실제 추론 경로) 기준이다.

| # | 연산 | 식 | shape 변화 | 종류 |
|---|---|---|---|---|
| 1 | KV 압축 | `x @ W^DKV` | `(B,1,d) × (d, d_c)` → `(B,1,d_c)` | GEMV |
| 2 | RoPE 키 | `x @ W^KR` → 회전 | `(B,1,d) × (d, d_r)` → `(B,1,d_r)` | GEMV + elementwise |
| 3 | **캐시 append** | 복사 | → `(B,S,d_c)`, `(B,S,d_r)` | memory write — **헤드 차원이 없다** |
| 4 | 쿼리 (흡수형) | `x @ (W^Q·W^UKᵀ)` | `(B,1,d) × (d, n_h·d_c)` → `(B,1,n_h,d_c)` | GEMV — **가중치가 커졌다** |
| 5 | 점수 (본체) | `q_c @ cᵀ` | `(B,n_h,1,d_c) × (B,**1**,d_c,S)` → `(B,n_h,1,S)` | batched GEMV — **전 헤드가 같은 `c`** |
| 6 | 점수 (RoPE) | `q_r @ k_Rᵀ` | `(B,n_h,1,d_r) × (B,1,d_r,S)` → `(B,n_h,1,S)` | batched GEMV |
| 7 | 합산 + softmax | `5 + 6` | `(B,n_h,1,S)` 유지 | elementwise + reduction |
| 8 | 가중합 | `p @ c` | `(B,n_h,1,S) × (B,**1**,S,d_c)` → `(B,n_h,1,d_c)` | batched GEMV |
| 9 | 출력 (흡수형) | `o @ (W^UV·W^O)` | `(B,1,n_h·d_c) × (n_h·d_c, d)` → `(B,1,d)` | GEMV — **가중치가 커졌다** |

세 가지가 눈에 띈다.

**첫째, 5·8번의 오른쪽 피연산자에 헤드 차원이 없다.** `(B, 1, d_c, S)`다.
모든 헤드가 **같은 latent를 읽는다** — MQA와 똑같은 접근 패턴이다. 그런데 왼쪽의
`q_c`는 헤드마다 다르니 표현력은 살아 있다. `1.4` 서두에서 말한
"MQA의 메모리 이점 + MHA의 표현력"이 여기서 나온다.

**둘째, 4·9번의 가중치가 커졌다.** 원래 `Wq`는 `(d, n_h·d_h)`인데 흡수형은
`(d, n_h·d_c)`다. `d_c`(512)가 `d_h`(128)보다 크므로 **이 두 GEMV의 연산량과
가중치 읽기가 늘어난다.** 앞서 말한 "연산량은 오히려 증가"의 정체가 이것이다.
(표기는 두 행렬을 합쳐둔 형태지만, 실제 커널은 합치지 않고 `W^UK`·`W^UV`를 런타임에
헤드별로 따로 적용한다. 위 ⚠️ 참고.)

**셋째, 점수 계산이 5·6번으로 쪼개진다.** RoPE를 적용하는 차원과 흡수 경로를 타는
차원이 분리돼 있어서다. 두 결과를 더한 뒤 softmax를 건다.

📌 정리하면 MLA의 거래는 이렇다.
**3번(캐시 쓰기)과 5·8번(캐시 읽기)에서 크게 아끼고, 4·9번(가중치 읽기·연산)에서 지불한다.**
캐시 쪽은 `S`와 `B`에 비례하고 가중치 쪽은 그렇지 않으므로, 컨텍스트가 길수록 이득이 커진다.

> ⚠️ **논문과 구현이 가장 크게 갈리는 모듈이다.**
> 논문 수식은 naive 형태로 쓰여 있고, HuggingFace 참조 구현도 대개 그렇다.
> 실제 추론 엔진(vLLM, SGLang의 FlashMLA 계열)은 흡수 형태로 돌린다.
> **논문만 읽으면 "왜 이게 빠른지" 이해할 수 없다.**

✅ 검증할 것이 둘이다.
- absorbed 형태와 naive 형태의 출력이 일치하는가
- latent만 캐시해도 결과가 변하지 않는가

`snippets/test_equivalence.py::test_mla_absorption`

### 정리

| | |
|---|---|
| **핵심 아이디어** | KV를 저차원 latent로 저장하고, projection을 Q와 O 쪽에 흡수해 전체 K/V 복원을 피한다 |
| **장점** | · DeepSeek-V2 설정에서 KV가 **약 2.25-group GQA 상당**으로 작음<br>· 전체 K/V를 materialize하지 않고 latent에서 attention 계산 가능<br>· 모든 헤드가 같은 latent cache를 읽어 접근 패턴이 단순 |
| **한계** | · **구현이 복잡하다.** 흡수를 안 하면 이점이 사라지는데, 흡수 형태는 전용 커널이 필요<br>· **RoPE와 충돌**해 별도 차원(`d_r`)을 떼어내야 함<br>· 읽는 양은 줄지만 **연산량은 늘어난다** — batch가 커지면 이득이 줄 수 있음<br>· 기존 MHA 체크포인트에서 전환하기 어려움 (GQA와 대조적) |
| **대표 모델** | **DeepSeek-V2 / V3 / R1**(`d_c`=512, `d_r`=64) · Kimi K2 / K2.5 · GLM-5 · Ling 2.5 · Sarvam 105B — **긴 컨텍스트를 노리는 대형 모델 쪽에서 채택 확대 중** |
| **다음으로** | 저장량은 크게 줄였지만 **여전히 컨텍스트 길이에 비례**한다. 1M 컨텍스트에서는 576개 값도 감당이 안 된다. 여기서 길이 두 갈래로 갈린다 — 품질을 보강하는 쪽(**Gated Attention**)과, 아예 **다 읽지 않는** 쪽(`1.6`부터) |

---

여기서 잠깐 다른 이야기를 하나 하고 가자. 지금까지는 "무엇을 저장할 것인가"만 봤는데,
attention에는 오래된 이상 증상이 하나 있었다. **모델이 볼 것이 없을 때도 어딘가는
봐야만 한다**는 문제다.

---

## 1.5 Gated Attention — 출력에 문을 달다

### 왜 나왔나

softmax는 확률분포를 만든다. 합이 반드시 1이다. 그런데 어떤 쿼리는 문맥에서 특별히
가져올 게 없을 수도 있다. 그럴 때도 softmax는 **1만큼을 어딘가에 배분해야 한다.**

모델이 찾아낸 편법은 **맨 앞 토큰에 몰아넣기**였다. 첫 몇 개 토큰은 내용상 별 의미가
없으니 거기다 버리는 것이다. 이 현상을 attention sink라고 부른다.

부작용이 여럿이다.

- 그 위치의 활성값이 비정상적으로 커진다 (massive activation) → **양자화가 어려워진다**
- 컨텍스트가 길어질수록 분포가 왜곡되어 **긴 입력에서 일반화가 나빠진다**

### 아이디어와 구조

attention 결과를 `Wo`로 되돌리기 직전에, **헤드별로 게이트를 하나 곱한다.**
게이트 값은 현재 쿼리로부터 계산되고 sigmoid를 거치므로 0~1 사이다.

```
 q, K, V ─► softmax(qKᵀ)·V ─► ⊗ ─► Wo ─► y
                               ▲
                               │
        x ──► 작은 선형층 ──► sigmoid ──► g  (헤드별, 0~1)
```
> **그림 1.5** — 출력 게이트. 추가되는 것은 작은 선형층 하나와 elementwise 곱 하나뿐이다.

효과를 세 가지로 정리할 수 있다.

| 무엇 | 왜 |
|---|---|
| **비선형성** | 원래 attention 출력은 V들의 볼록결합이라 선형적이다. 게이트가 곱해지면서 비선형이 붙는다 |
| **희소성** | 필요 없는 헤드를 아예 닫아버릴 수 있다 |
| **sink 제거** | softmax가 억지로 배분한 몫을 게이트가 걸러낸다 |

세 번째가 핵심이다. 이 구조는 결국 **"아무것도 안 봐도 된다"는 선택지를 attention에
준 것**이다. 그동안 없던 선택지였고, 그게 없어서 sink가 생겼다.

📌 [T1] 이 연구는 NeurIPS 2025 Best Paper로 선정되었고, 게이트를 현재 쿼리에
조건화했을 때 sink 비율이 크게 억제된다고 보고한다.

MLA와 결합한 형태를 **Gated MLA**라고 부른다. 별개의 계보가 아니라 `1.4`에 게이트를
얹은 것이다.

### 추론에서 달라진 것

| | 게이트 없음 | Gated Attention |
|---|---|---|
| KV 크기 | 기준 | **변화 없음** |
| 추가 연산 | — | 작은 선형층 + elementwise 곱 |
| 추가 메모리 | — | 게이트용 가중치 (`d × n_h` 정도) |
| 얻는 것 | — | 긴 컨텍스트 안정성, 양자화 친화성 |

**KV 문제를 푸는 모듈이 아니다.** 비용이 거의 안 드는데 품질과 안정성을 얻으니
채택 문턱이 낮았고, 그래서 빠르게 퍼졌다.

### 토큰 하나가 지나가는 길

`0.2`의 표에서 7번과 8번 사이에 게이트 계산과 곱셈이 끼어든다.
둘 다 elementwise라 decode 비용에 거의 영향이 없다.

### 코드와 텐서

`1.1` 표의 9번과 10번 사이에 두 줄이 끼어든다.

| # | 연산 | 식 | shape 변화 | 종류 |
|---|---|---|---|---|
| 9 | 가중합 | `p @ V` | → `(B,1,n_h,d_h)` | batched GEMV |
| **9.1** | **게이트 계산** | `x @ Wg` → sigmoid | `(B,1,d) × (d, n_h)` → `(B,1,n_h,1)` | **GEMV(아주 작음) + elementwise** |
| **9.2** | **게이트 적용** | `o * g` | `(B,1,n_h,d_h)` 유지, 브로드캐스트 | **elementwise** |
| 10 | 출력 투영 | `o @ Wo` | → `(B,1,d)` | GEMV |

추가되는 가중치는 `(d, n_h)` 하나뿐이다. Llama 3 70B 규모면 `8192 × 64`,
레이어당 50만 개 남짓 — 전체 파라미터의 0.001% 수준이다.
9.2번은 순수 elementwise라 비용이 사실상 없다.

> 💡 **이 모듈이 빠르게 퍼진 이유가 이 표에 있다.** 구조 변경이 GEMV 하나와
> elementwise 하나뿐인데, 얻는 건 attention sink 제거와 긴 컨텍스트 안정성이다.
> KV 구조를 전혀 건드리지 않으므로 GQA든 MLA든 그대로 얹을 수 있다.

✅ 게이트를 1로 고정하면 원래 attention과 출력이 일치한다.
`snippets/test_equivalence.py::test_gate_identity`

### 정리

| | |
|---|---|
| **핵심 아이디어** | attention 출력에 쿼리 조건부 sigmoid 게이트를 헤드별로 곱해, "아무것도 보지 않는" 선택지를 준다 |
| **장점** | · **attention sink를 제거** — 활성값 폭주가 사라져 양자화에 유리<br>· 비용이 거의 0에 가깝다 (작은 선형층 + elementwise)<br>· 긴 컨텍스트 일반화와 학습 안정성이 개선됨<br>· KV 구조를 건드리지 않아 MLA·GQA 어디에나 얹을 수 있음 |
| **한계** | · **KV cache 문제 자체는 전혀 풀지 않는다**<br>· 재학습이 필요하다 — 기존 체크포인트에 그냥 붙일 수 없음<br>· 게이트 위치와 정규화 방식(zero-centered RMSNorm 등)에 민감 |
| **대표 모델** | **Qwen3-Next** |
| **다음으로** | 이제 본론으로 돌아가자. 저장량을 줄이는 방법은 한계에 다다랐다. 그렇다면 **저장은 하되 다 읽지 않으면** 어떨까 → **SWA** |

---

여기서 발상이 한 번 바뀐다.

`1.1`부터 `1.4`까지는 전부 **무엇을 저장할 것인가**의 문제였다. 헤드를 공유하든
차원을 압축하든, 결국 토큰마다 뭔가를 남기고 **그걸 전부 읽었다.**

그런데 정말 전부 읽어야 하나? 10만 토큰짜리 문서에서 지금 만들 토큰과 관련 있는 건
그중 몇 개뿐일 텐데.

가장 단순한 답부터 보자. **최근 것만 보면 된다.**

---

## 1.6 SWA와 attention sink — 읽는 범위를 제한하기

*Sliding Window Attention*

### 왜 나왔나

언어에는 강한 지역성이 있다. 다음 단어를 예측할 때 대부분의 정보는 바로 앞 문장에 있다.
그렇다면 **최근 `W`개 토큰만 보면 되지 않을까.**

### 아이디어와 구조

각 쿼리가 자기 앞 `W`개만 본다. 그보다 오래된 것은 캐시에서 버린다.

```
   full attention              sliding window (W=4)

 t=7 ▓▓▓▓▓▓▓▓                t=7 ░░░░▓▓▓▓
 t=6 ▓▓▓▓▓▓▓░                t=6 ░░░▓▓▓▓░
 t=5 ▓▓▓▓▓▓░░                t=5 ░░▓▓▓▓░░
 t=4 ▓▓▓▓▓░░░                t=4 ░▓▓▓▓░░░
     0123456                     0123456
     ▓ = 본다                    최근 W개만
```
> **그림 1.6** — SWA의 attention 패턴

이게 중요한 이유가 하나 있다. **KV cache가 컨텍스트 길이에 비례하지 않는 첫 번째
방법**이다. `min(S, W)`에서 멈춘다. 컨텍스트가 1M이어도 `W`가 4096이면 캐시는
4096개 분량이다.

대신 명백한 손실이 있다. **한 SWA 층에서는 윈도우 밖 토큰에 직접 접근할 수 없다.**
먼 정보는 여러 층을 거쳐 전달되거나 별도의 global 층이 있어야 한다.

그래서 실제로는 전 레이어에 쓰지 않는다. **몇 개 층은 full attention으로 남긴다.**
Gemma 3는 local과 global을 5:1로 교차 배치한다. 지역 정보는 값싼 층들이 처리하고,
전역 정보는 가끔 있는 비싼 층이 나른다.

> 💡 이 "값싼 층 여러 개 + 비싼 층 하나" 패턴은 앞으로 계속 나온다.
> `02-linear-attention`의 3:1 하이브리드도, `1.9`의 CSA/HCA 교차 배치도 같은 구조다.
> **어느 층을 비싸게 둘 것인가**가 최근 아키텍처 설계의 반복되는 질문이다.

### attention sink — 왜 앞부분을 버리면 안 되나

SWA를 스트리밍에 적용하다 발견된 현상이 있다. **윈도우를 굴리면서 맨 앞 토큰들을
버리면 모델 품질이 갑자기 무너진다.**

원인은 `1.5`에서 본 그 문제다. softmax는 확률 1을 어딘가 배분해야 하고, 모델은
그 몫을 초기 토큰에 버리도록 학습되어 있다. 그 배출구를 없애면 확률이 다른 곳으로
밀려가면서 분포가 망가진다.

해법은 단순했다. **초기 토큰 몇 개는 내용과 무관하게 항상 남겨둔다.**
윈도우 + 앞 몇 개, 이 조합으로 아주 긴 스트림에서도 안정적으로 동작한다.
gpt-oss처럼 아예 학습 가능한 sink 파라미터를 두는 모델도 있다.

> `1.5`의 Gated Attention과 비교하면 재미있다. 같은 문제(softmax의 강제 배분)에 대해
> **StreamingLLM은 배출구를 유지하는 쪽**을, **Gated Attention은 배출구가 필요 없게
> 만드는 쪽**을 택했다.

### 추론에서 달라진 것

| | full attention | SWA |
|---|---|---|
| KV 크기 | `S`에 비례 | **`min(S, W)`에서 상한** |
| 읽는 범위 | 전체 | 최근 `W`개 |
| 긴 컨텍스트 비용 | 선형 증가 | **일정** |
| 잃는 것 | — | 윈도우 밖 정보 완전 소실 |
| 보완책 | — | 일부 층은 full로 유지 + sink 토큰 |

### 토큰 하나가 지나가는 길

`0.2`의 표에서 6번(캐시에 붙이기)에 **오래된 항목 버리기**가 추가되고,
7번이 전체가 아닌 `W`개만 읽는다. 캐시를 원형 버퍼로 관리하는 것이 보통이다.

### 코드와 텐서

GQA 표에서 `S`가 전부 `min(S, W)`로 바뀌는 것이 전부다. 새 연산은 없다.

| # | 연산 | shape 변화 | 종류 |
|---|---|---|---|
| 5 | 캐시 append + **오래된 것 폐기** | `(B, min(S,W), n_kv, d_h)` — **상한이 있다** | memory write (원형 버퍼) |
| 6 | 점수 | `(B,n_h,1,d_h) × (B,n_h,d_h,**min(S,W)**)` → `(B,n_h,1,min(S,W))` | batched GEMV |
| 9 | 가중합 | `(B,n_h,1,min(S,W)) × (B,n_h,min(S,W),d_h)` → `(B,n_h,1,d_h)` | batched GEMV |

> 💡 **연산의 종류가 하나도 안 바뀌고 크기만 바뀐다.** 그래서 커널 지원이 좋고
> 구현이 단순하다. 캐시를 원형 버퍼로 돌리면 되고, 읽는 구간도 여전히 **연속**이다.
>
> 이 점이 `1.7`의 희소 계열과 결정적으로 다르다. 거기서는 읽는 위치가 흩어지면서
> 연산 종류에 **gather**가 새로 등장한다.

✅ `W ≥ S`로 두면 full attention과 출력이 일치한다.
`snippets/test_equivalence.py::test_swa_full_window`

### 정리

| | |
|---|---|
| **핵심 아이디어** | 각 쿼리가 최근 `W`개만 보게 하고, 나머지는 캐시에서 버린다 |
| **장점** | · **KV가 컨텍스트 길이에 비례하지 않는 첫 방법** — `W`에서 상한<br>· 구현이 단순하고 커널 지원이 좋다 (연속된 구간이라 접근 패턴이 깔끔)<br>· 일부 층만 SWA로 두는 식으로 손실을 조절할 수 있음 |
| **한계** | · 한 층에서 윈도우 밖 토큰에 직접 접근 불가<br>· 정보를 여러 층에 걸쳐 간접 전달해야 하므로 긴 의존 관계에 약함<br>· streaming에서는 attention sink 처리가 필요할 수 있음 |
| **대표 모델** | **Mistral 7B**(`W`=4096) · **Gemma 2 / 3**(local:global = 5:1) |
| **다음으로** | "최근"이 항상 중요한 건 아니다. **어디를 볼지 모델이 정하게 하면** → **NSA · MoBA** |

---

SWA의 한계는 명확하다. **고정 패턴**이라는 것이다. 최근 것이 중요하다는 가정은 대체로
맞지만, 항상 맞지는 않다. 긴 문서에서 답이 앞쪽에 있으면 그냥 못 찾는다.

그러면 **어디를 볼지 모델이 직접 고르게 하면** 어떨까. 그런데 여기엔 함정이 있다.
토큰마다 제각각 다른 위치를 고르면 메모리를 여기저기서 긁어와야 하고, GPU는 그런
접근을 아주 싫어한다. 이론상 연산은 줄었는데 실제로는 더 느려질 수 있다.

2025년 초에 나온 두 연구가 같은 답을 내놓았다. **블록 단위로 고르면 된다.**

---

## 1.7 NSA와 MoBA — 어디를 볼지 학습하기

*Native Sparse Attention · Mixture of Block Attention*

### 왜 나왔나

고정 패턴(SWA)의 한계와, 자유로운 선택의 비효율 사이에서 타협점을 찾는 문제다.
두 연구가 다른 경로로 접근했지만 **"블록 단위 선택"이라는 결론은 같았다.**

### NSA — 세 갈래를 병렬로

NSA(Native Sparse Attention)는 세 종류의 attention을 **동시에** 돌리고 학습된 게이트로
섞는다.

```
            ┌─► 압축 갈래 ──┐   전체를 거칠게 요약해서 봄
            │               │
  q ────────┼─► 선택 갈래 ──┼──► 게이트로 가중합 ──► o
            │               │   중요한 블록만 정밀하게
            │               │
            └─► 지역 갈래 ──┘   최근 512개
```
> **그림 1.7a** — NSA의 세 갈래. 게이트는 입력으로부터 MLP + sigmoid로 계산된다.

📌 [T1] 각 갈래의 설정값은 이렇다.

| 갈래 | 하는 일 | 설정 |
|---|---|---|
| **압축** | 연속된 토큰 블록을 MLP로 눌러 하나의 표현으로 | 블록 32, stride 16 (겹침) |
| **선택** | 중요한 블록만 골라 원본 그대로 봄 | 블록 64, 상위 16개 |
| **지역** | 최근 구간을 그대로 | 512개 |

여기서 영리한 부분이 둘 있다.

**첫째, 블록 점수를 공짜로 얻는다.** 어느 블록이 중요한지 따로 계산하지 않고,
**압축 갈래에서 이미 나온 attention 점수를 재활용**한다. 압축 표현에 높은 점수를
준 블록이 곧 중요한 블록이다.

**둘째, 지역 갈래를 따로 떼어낸 이유가 있다.** 지역 정보는 워낙 예측력이 좋아서,
한데 섞어두면 모델이 그것만 보고 나머지 두 갈래는 학습을 게을리한다. 분리해두면
압축·선택 갈래가 각자 할 일을 배운다.

#### hardware-aligned가 무슨 뜻인가

NSA가 강조하는 표현이다. 핵심은 **GQA 그룹 안의 쿼리 헤드들이 전부 같은 블록을
고르게 강제**한 것이다.

헤드마다 다른 블록을 고르면 KV를 헤드 수만큼 따로 긁어와야 한다. 그룹이 같은 걸
고르면 **한 번 SRAM에 올려서 그룹 전체가 쓴다.** 정확도를 조금 포기하고 메모리
접근을 살린 것이고, 결과적으로 이쪽이 빨랐다.

📌 [T1] 64k 컨텍스트에서 full attention 대비 forward 9.0배, backward 6.0배,
**decode 11.6배** 빨랐다고 보고한다. decode 배수가 가장 큰 것은 그 단계가
memory-bound이기 때문이다 — 읽는 양을 줄이는 게 그대로 속도가 된다.

또 하나, NSA는 **처음부터 희소하게 학습**한다(natively trainable). 조밀하게 학습한
모델을 나중에 희소화하는 방식과 달리, 모델이 희소성에 맞춰 표현을 배운다.

### MoBA — attention에 MoE를 적용하면

MoBA(Mixture of Block Attention)는 Moonshot의 접근이다. 이름 그대로 **MoE의 발상을
attention에 옮겼다.**

컨텍스트를 블록으로 나누고, 각 쿼리가 **라우터를 통해 상위 `k`개 블록을 고른다.**
MoE에서 토큰이 expert를 고르듯, 여기서는 쿼리가 블록을 고른다.

```
  컨텍스트 ─► [블록1][블록2][블록3][블록4]...[블록n]
                  ▲              ▲
                  │              │      라우터가 고른 top-k
  q ──► 라우터 ───┴──────────────┘
```
> **그림 1.7b** — MoBA. 라우터는 파라미터 없이 쿼리와 블록 대표값의 내적으로 점수를 낸다.

설계 철학을 논문은 **"less structure"** 라고 부른다. sink니 window니 하는 사전 구조를
사람이 정해주지 말고, **어디를 볼지는 모델이 배우게 하자**는 것이다. SWA가 정반대
방향이었다는 점에서 대조적이다.

실용적인 장점도 있다. 라우터에 파라미터가 없고 블록 전체를 고르면 full attention과
같아지므로, **희소와 조밀을 같은 파라미터로 오갈 수 있다.**

### 추론에서 달라진 것

| | full attention | NSA / MoBA |
|---|---|---|
| KV 저장량 | 기준 | **거의 그대로** (버리지 않는다) |
| 읽는 양 | 전체 | 고른 블록만 |
| 접근 패턴 | 연속 | **흩어진 블록** (gather) |
| 추가 비용 | — | 블록 점수 계산 + 정렬 |
| 학습 | — | **처음부터 희소하게 학습해야 함** |

**저장량은 안 줄어든다**는 점을 놓치면 안 된다. 줄어드는 건 읽는 양이다.
메모리 용량 압력이 아니라 대역폭 압력을 푸는 방법이다.

> ⚠️ **여기서 성격이 바뀐다.**
> `1.1`~`1.6`은 전부 KV를 **연속으로** 읽었다. 희소 방식은 흩어진 곳에서 긁어온다.
> HBM은 연속 접근에서 최고 성능이 나오므로, 실효 대역폭이 이론치보다 떨어진다.
>
> **희소화의 진짜 제약은 정확도가 아니라 메모리 접근 패턴이다.**
> 두 연구가 모두 토큰이 아닌 블록 단위를 택한 이유가 여기 있다.

### 토큰 하나가 지나가는 길

NSA 기준이다. `0.2`의 7번이 이렇게 펼쳐진다.

| # | 하는 일 |
|---|---|
| 7a | 압축 표현들과 attention → 압축 갈래 출력 + **블록 점수** |
| 7b | 블록 점수로 상위 16개 선택 |
| 7c | 선택된 블록의 원본 KV를 gather해서 attention |
| 7d | 최근 512개로 지역 attention |
| 7e | 게이트 3개를 계산해 세 출력을 가중합 |

7b의 정렬과 7c의 gather가 새로 생긴 비용이다.

### 코드와 텐서

NSA 기준이다. `1.1` 표의 6~9번이 세 갈래로 갈라진다.

| # | 연산 | 식 | shape 변화 | 종류 |
|---|---|---|---|---|
| **압축 갈래** | | | | |
| c1 | 블록 압축 | `MLP(K_block)` | `(B,S,n_kv,d_h)` → `(B,S/16,n_kv,d_h)` | **GEMM + elementwise** (블록마다) |
| c2 | 압축 점수 | `q @ K_cmpᵀ` | `(B,n_h,1,d_h) × (·,d_h,S/16)` → `(B,n_h,1,S/16)` | batched GEMV |
| c3 | softmax → 가중합 | | → `(B,n_h,1,d_h)` | reduction + batched GEMV |
| **선택 갈래** | | | | |
| s1 | 블록 점수 | **c2 결과 재활용** | `(B,n_h,1,S/16)` → `(B,n_kv,S/64)` | **reduction (그룹 내 헤드 합산)** |
| s2 | top-16 | 정렬/선택 | → `(B,n_kv,16)` 인덱스 | **sort / top-k** ← 새 연산 |
| s3 | KV 모으기 | `K[idx]` | → `(B,16×64,n_kv,d_h)` | **gather** ★ ← 새 연산 |
| s4 | 점수 → 가중합 | | → `(B,n_h,1,d_h)` | batched GEMV + reduction |
| **지역 갈래** | | | | |
| w1 | 최근 512개 | | → `(B,n_h,1,d_h)` | batched GEMV (연속 읽기) |
| **합치기** | | | | |
| g1 | 게이트 3개 | `MLP(x)` → sigmoid | `(B,1,d)` → `(B,1,3)` | GEMV + elementwise |
| g2 | 가중합 | `Σ gᶜ · oᶜ` | → `(B,1,n_h,d_h)` | elementwise |

여기서 **연산의 종류 자체가 새로 두 개 늘었다.**

| 새 연산 | 왜 문제인가 |
|---|---|
| **s2 — top-k 정렬** | 비교·정렬은 GPU가 행렬 곱만큼 잘하지 못한다. 매 토큰·매 레이어마다 돈다 |
| **s3 — gather** ★ | 흩어진 위치에서 긁어온다. HBM은 연속 접근에서 최고 성능이 나오므로 **실효 대역폭이 이론치보다 낮다** |

s1이 영리한 부분이다. 블록 점수를 따로 계산하지 않고 **c2에서 이미 나온 것을 재활용**한다.
그리고 `(B,n_h,·)`를 `(B,n_kv,·)`로 줄이는 reduction이 들어가는데, 이게
**GQA 그룹 안 헤드들이 같은 블록을 고르도록 강제**하는 부분이다.
덕분에 s3의 gather를 헤드마다가 아니라 **그룹당 한 번만** 하면 된다.

> 💡 **NSA가 "hardware-aligned"라고 주장하는 근거가 s1과 s3의 관계다.**
> 헤드마다 제각각 고르게 두면 gather가 `n_h`번 일어난다. 그룹으로 묶으면 `n_kv`번이다.
> 정확도를 조금 내주고 메모리 접근을 산 것이다.

한편 c1의 압축은 **GEMM**이다. 이 표에서 유일하게 GEMV가 아닌 항목인데,
블록 여러 개를 한 번에 처리하기 때문이다.

✅ 상위 `n`을 전체 블록 수로 두면 dense attention과 일치한다.
`snippets/test_equivalence.py::test_nsa_dense_limit`

### 정리 — NSA

| | |
|---|---|
| **핵심 아이디어** | 압축·선택·지역 세 갈래를 병렬로 돌리고 학습된 게이트로 섞는다. 선택 점수는 압축 갈래에서 재활용 |
| **장점** | · 64k에서 **decode 11.6배** — 실측으로 검증된 큰 폭의 개선<br>· 압축(전역)과 선택(정밀)을 모두 갖춰 정보 손실이 적음<br>· **GQA 그룹이 같은 블록을 고르게 강제**해 메모리 접근을 커널 친화적으로 만듦<br>· 처음부터 희소하게 학습해 모델이 희소성에 적응 |
| **한계** | · **처음부터 그렇게 학습해야 한다** — 기존 모델에 붙일 수 없음<br>· 갈래가 셋이라 구조가 복잡하고 하이퍼파라미터가 많음<br>· KV **저장량은 줄지 않는다** (읽는 양만 감소)<br>· 블록 단위 선택이라 토큰 단위 정밀도는 포기 |
| **대표 모델** | DeepSeek 연구 라인 — 이후 DSA·CSA로 이어짐 |
| **다음으로** | 세 갈래는 무겁다. 이미 학습된 MLA 모델에 얹을 수 있는 가벼운 방식이 필요하다 → **DSA** |

### 정리 — MoBA

| | |
|---|---|
| **핵심 아이디어** | MoE의 top-k 라우팅을 attention에 적용. 컨텍스트를 블록으로 나누고 쿼리가 볼 블록을 고른다 |
| **장점** | · **파라미터 없는 라우터** — 추가 학습 부담이 작다<br>· 전체 블록을 고르면 full attention과 같아져 **희소·조밀 전환이 자유롭다**<br>· sink·window 같은 사전 구조를 넣지 않아 도메인에 덜 의존 |
| **한계** | · 블록 단위 선택이므로 블록 크기가 정밀도를 좌우<br>· 라우팅 품질에 성능이 직결<br>· NSA와 마찬가지로 저장량은 줄지 않음 |
| **대표 모델** | Moonshot(Kimi) 연구 라인 |
| **다음으로** | NSA와 함께 "학습된 선택"이라는 갈래를 열었다. 다음은 이 발상을 MLA와 결합하는 단계다 |

---

NSA와 MoBA는 강력하지만 조건이 하나 붙는다. **처음부터 그렇게 학습해야 한다.**
이미 MLA로 잘 학습해둔 모델이 있다면? 세 갈래 구조를 새로 얹고 다시 학습시키는 건
비싸다.

DeepSeek-V3.2의 답은 훨씬 가벼웠다. **어디를 볼지 골라주는 아주 싼 모듈 하나만
덧붙이자.**

---

## 1.8 DSA — 싸구려 색인으로 고르기

*DeepSeek Sparse Attention*

### 왜 나왔나

기존 MLA 모델 위에 희소성을 얹으려면, 선택 과정 자체가 저렴해야 한다.
NSA처럼 압축·선택·지역 세 갈래를 돌리면 배보다 배꼽이 커진다.

### 아이디어와 구조

두 부분으로 나뉜다.

```
              ┌──────────────────┐
  q ─────────►│ lightning indexer│──► 모든 이전 토큰의 점수 ──► top-k (2048개)
              │  적은 헤드 / 저rank│                              │
              │  FP8              │                              ▼
              └──────────────────┘                     ┌──────────────────┐
  캐시 (MLA latent) ─────────────────────────────────► │ sparse MLA       │──► o
                                                       │ 고른 것만 attention│
                                                       └──────────────────┘
```
> **그림 1.8** — DSA. indexer는 "어디를 볼지"만 정하고, 실제 attention은 MLA가 한다.

**lightning indexer**는 이름대로 아주 가볍게 만들어져 있다.

| 무엇 | 어떻게 |
|---|---|
| 헤드 수 | 적게 |
| projection | 저rank |
| 연산 정밀도 | **FP8** |
| 점수 계산 | ReLU를 끼운 내적 |

이렇게 만들어서 본 attention(MLA)보다 토큰당 비용을 **한 자릿수 배 이상 낮춘다.**
📌 V3.2 리포트는 희소 학습 단계에서 쿼리당 **2048개**를 고른다고 밝힌다.

계보상 위치가 중요하다. **DSA는 압축 계보(MLA)와 선택 계보(NSA/MoBA)가 처음으로
결합한 지점이다.** 저장은 MLA로 줄이고, 읽는 양은 선택으로 줄인다. 두 압력을
동시에 공격한다.

#### 남는 비용

indexer도 **모든 이전 토큰을 훑어야 한다.** 즉 `O(S)`는 사라지지 않는다.
정확히 말하면 DSA가 한 일은

> **비싼 `O(S)`를 싼 `O(S)`로 바꾸고, 본 attention만 `O(k)`로 줄인 것**

이다. 컨텍스트가 극단적으로 길어지면 indexer 비용 자체가 다시 문제가 된다.
이 지점을 CSA가 건드린다.

### 추론에서 달라진 것

| | MLA | DSA (= MLA + 선택) |
|---|---|---|
| KV 저장 | latent 그대로 | **동일** |
| 본 attention이 읽는 양 | 전체 `S` | **`k` = 2048** |
| 추가 연산 | — | indexer (`O(S)`지만 매우 저렴) + 정렬 |
| 접근 패턴 | 연속 | 흩어짐 |
| 기존 모델 적용 | — | **가능** (추가 학습 단계 필요) |

### 토큰 하나가 지나가는 길

| # | 하는 일 | 비고 |
|---|---|---|
| 7a | indexer로 모든 이전 토큰 점수 계산 | FP8, 저rank — 싸다 |
| 7b | 상위 `k`개 인덱스 뽑기 | 정렬 비용 |
| 7c | 그 위치의 latent만 gather | 흩어진 읽기 |
| 7d | MLA attention (`k`개 대상) | 본 계산 |

### 코드와 텐서

| # | 연산 | 식 | shape 변화 | 종류 |
|---|---|---|---|---|
| **indexer** | | | | |
| i1 | 색인 쿼리 | `x @ W_idx` | `(B,1,d) × (d, n_i·d_i)` → `(B,1,n_i,d_i)` | GEMV — **저rank, FP8** |
| i2 | 색인 점수 | `ReLU(q_i @ K_iᵀ)` | `(B,n_i,1,d_i) × (·,d_i,S)` → `(B,1,S)` | **batched GEMV — `O(S)` 남음** |
| i3 | top-k | 정렬/선택 | `(B,1,S)` → `(B,1,k)` 인덱스 | **sort / top-k** |
| **sparse MLA** | | | | |
| m1 | latent 모으기 | `c[idx]`, `k_R[idx]` | `(B,S,d_c)` → `(B,k,d_c)` | **gather** ★ |
| m2 | 점수 | `q_c @ c_selᵀ` | `(B,n_h,1,d_c) × (B,1,d_c,**k**)` → `(B,n_h,1,k)` | batched GEMV |
| m3 | softmax | | `(B,n_h,1,k)` 유지 | reduction |
| m4 | 가중합 | `p @ c_sel` | `(B,n_h,1,k) × (B,1,k,d_c)` → `(B,n_h,1,d_c)` | batched GEMV |

MLA 표(`1.4`)와 비교하면 **`S`가 `k`로 바뀐 것**이 본질이다. `S`가 100만이어도
m2·m4는 2048개만 본다.

대신 i2가 남는다. 여기는 여전히 `S` 전체를 훑는다.

| | 헤드 수 | 차원 | 정밀도 | 대상 |
|---|---|---|---|---|
| indexer (i2) | 적음 | 저rank | **FP8** | `S` 전체 |
| 본 attention (m2) | `n_h`=128 | `d_c`=512 | BF16 | `k`=2048 |

> 💡 **DSA가 한 일을 연산 관점에서 말하면 이렇다.**
> `O(S)` 짜리 비싼 batched GEMV를 `O(S)` 짜리 **싼** batched GEMV로 바꾸고,
> 비싼 쪽은 `O(k)`로 줄였다. `O(S)` 자체는 사라지지 않았고, 단가만 한 자릿수 배 낮췄다.
>
> 이 남은 `O(S)`를 건드리는 것이 `1.9`의 CSA다 — 훑을 대상 자체를 `S/m`개로 줄인다.

✅ `k ≥ S`로 두면 원래 MLA와 출력이 일치한다.
`snippets/test_equivalence.py::test_dsa_dense_limit`

### 정리

| | |
|---|---|
| **핵심 아이디어** | 아주 싼 indexer로 볼 토큰 `k`개를 고르고, 본 attention(MLA)은 그것만 계산한다 |
| **장점** | · **압축(MLA)과 선택을 처음으로 결합** — 두 압력을 동시에 공격<br>· indexer가 FP8·저rank·소수 헤드라 오버헤드가 작음<br>· **기존 MLA 모델에 얹을 수 있다** (NSA와 대조적)<br>· 본 attention이 `O(S)` → `O(k)` |
| **한계** | · **indexer 자체는 여전히 `O(S)`** — 극단적으로 긴 컨텍스트에서 다시 병목<br>· top-k 정렬 비용과 gather로 인한 접근 지역성 저하<br>· 희소 학습 단계가 별도로 필요<br>· KV **저장량은 MLA 그대로** |
| **대표 모델** | **DeepSeek-V3.2** · **GLM-5** ✅ (`model_type`=`glm_moe_dsa`, `index_topk`=2048, indexer 헤드 32) |
| **다음으로** | indexer가 훑어야 할 대상 자체를 줄이면 된다. 토큰을 미리 압축해두고 그 위에서 고르면? → **CSA** |

DSA에는 아직 `O(S)`가 남아 있었다. indexer가 모든 토큰을 훑어야 하니까.
1M 컨텍스트로 가면 그 자체가 부담이 된다.

DeepSeek-V4의 답은 두 단계를 겹치는 것이었다. **먼저 토큰들을 압축해 개수를 줄이고,
그 줄어든 것들 위에서 고른다.**

---

## 1.9 CSA와 HCA — 압축과 선택을 겹치기

*Compressed Sparse Attention · Heavily Compressed Attention*

> 이 절의 수치는 DeepSeek-V4 논문(arXiv:2606.19348), HuggingFace `config.json`,
> vLLM/SGLang 배포 문서에서 확인한 것이다.
>
> | 약어 | 뜻 |
> |---|---|
> | **CSA** | **C**ompressed **S**parse **A**ttention |
> | **HCA** | **H**eavily **C**ompressed **A**ttention |
>
> HCA는 **Heavily Compressed Attention**이다. 같은 모델의 residual 기법인
> **mHC**(Manifold-Constrained Hyper-Connections, `06-norm-residual` 6.6)와는 별개다.

### 왜 나왔나

DSA는 `O(S)` indexer를 남겼다. 그리고 압축 계보(MLA)와 선택 계보를 결합했지만,
둘이 **직렬로 겹쳐 있지는 않았다.** MLA는 차원을 압축했을 뿐 토큰 개수는 그대로다.

**토큰 개수 자체를 줄인 다음 그 위에서 고르면** 두 절감이 곱해진다.

### 아이디어와 구조

**CSA (Compressed Sparse Attention)** 는 두 단계를 직렬로 잇는다.

```
 원본 토큰들   ●●●●●●●●●●●●●●●●   (S개)
                │
        ① m개씩 묶어 하나로 압축
                ▼
 압축 엔트리   ■■■■              (S/m개)
                │
        ② 그중 상위 k개만 선택
                ▼
 실제로 보는 것 ■  ■              (k개)
```
> **그림 1.9a** — CSA. 압축으로 후보를 줄이고, 선택으로 다시 줄인다.

📌 [T1] 압축은 **학습된다.** 고정된 평균이 아니라 학습 가능한 파라미터
(`W^aKV`, `W^bKV`, `W^aZ`, `W^bZ`)와 학습되는 위치 bias를 쓴다.
그리고 **겹치는 두 계열**(`C^a`, `C^b`)을 만들어 압축 출력 하나당 `2m`개 토큰이 반영되게 한다.
경계에서 정보가 잘리는 것을 막기 위한 설계로 보인다.

📌 [T1] **선택에는 DSA의 lightning indexer를 그대로 쓴다.**
`1.8`에서 본 그 모듈이다. CSA는 DSA를 대체한 게 아니라 **DSA를 압축 엔트리 위에 얹은 것**이다.

**HCA**는 같은 발상을 다른 지점에서 쓴다. 압축률을 훨씬 높이는 대신,
**압축된 결과에 대해서는 선택 없이 전부 본다(dense).**

| | **CSA** | **HCA** |
|---|---|---|
| 압축률 | **`m` = 4** | **`m'` = 128** (32배) |
| 그 위에서 | lightning indexer로 상위 `k`개 선택 | **전부** (dense) |
| 성격 | 정밀하지만 좁게 | 거칠지만 넓게 |
| `k` (V4-Pro) | **1024** | — |
| `k` (V4-Flash) | **512** | — |

📌 [T1/config] V4-Pro는 `index_topk`=1024, `index_n_heads`=64, `index_head_dim`=128.
V4-Flash 논문 값은 `m`=4, `m'`=128, 선택 512, indexer 헤드 64 / 차원 128이다.

### 레이어 배치 — config에 그대로 들어 있다

V4-Pro `config.json`의 `compress_ratios` 필드가 **레이어별 압축률 61개를 나열**한다.
이게 곧 CSA/HCA 배치도다.

```
 layer  0   1   2   3   4   5   6  ...  57  58  59  60
        128 128  4  128  4  128  4  ... 128  4  128  0
        └─┬──┘  └──────── 교대 ────────────────┘   └┬┘
       도입 2개    CSA(4) ↔ HCA(128) 1:1 교대      마지막
```
> **그림 1.9b** — V4-Pro(61층)의 실제 레이어 구성 📌 [config.json]

읽어낼 것이 셋이다.

- **비율은 1:1이다.** CSA 29개, HCA 30개 정도로 거의 정확히 반반이다.
  `1.6`의 Gemma(5:1)나 `02`의 하이브리드(3:1)와 달리 **한쪽을 값싼 보조로 두지 않는다.**
- **앞 2개 층이 별도다.** 둘 다 128이고, V4-Flash 논문은 첫 두 층을
  **순수 sliding window attention**으로 둔다고 밝힌다. config에도 `sliding_window`=128이 있다.
- **마지막 층은 `0`이다.** 압축을 하지 않는다는 뜻으로 보인다.

> 💡 `1.6`의 Gemma 5:1, `02` 2.5의 3:1과 **발상은 같지만 성격이 다르다.**
> 그쪽은 "값싼 층 여럿 + 비싼 층 하나"였다. V4는 **비슷한 비용의 두 종류를 반반씩** 둔다.
> Gemma가 "가까이 vs 멀리"로 나눴다면, V4는 **"정밀하게 조금 vs 거칠게 많이"**로 나눈 것이고,
> 어느 쪽도 보조 역할이 아니다.

### 계보상 무엇이 새로운가

NSA와 비교하면 차이가 선명하다.

| | NSA | CSA |
|---|---|---|
| 압축과 선택 | **병렬** — 각각 따로 돌려 게이트로 섞음 | **직렬** — 압축 결과 위에서 선택 |
| 선택 대상 | 원본 블록 | **압축 엔트리** |
| 효과 | 두 관점을 함께 봄 | 두 절감이 **곱해짐** |

NSA는 압축 갈래에서 나온 점수로 원본 블록을 골랐다. CSA는 압축한 것 자체를 고른다.
**한 단계 더 나아간 것**이다.

> 📌 **계보 연결에 대해**
> V4 논문은 **DSA를 명시적으로 인용한다** — "CSA는 압축 엔트리 위에 DSA를 적용한다"고
> 직접 쓴다. 그래서 **DSA → CSA는 실선**이다.
> NSA는 확인한 범위에서 언급되지 않았다. 구조적으로는 명백히 닮았지만
> **NSA → CSA는 계보 지도에서 점선으로 남긴다.**

### 추론에서 달라진 것

| | DSA (V3.2) | **CSA** (V4) | **HCA** (V4) |
|---|---|---|---|
| KV 저장 | latent `S`개 | **`S/4`개** | **`S/128`개** |
| 훑는 대상 | `S`개 | **`S/4`개** | — (dense) |
| 실제로 보는 것 | top-2048 | **top-1024 엔트리** | 압축 엔트리 **전부** |
| 추가 연산 | indexer | 학습된 압축 + indexer | 학습된 압축 |
| top-k 정렬 · gather | 있음 | 있음 | **없음** |
| 레이어 구성 | 균일 | **CSA/HCA 1:1 교차** — 층마다 비용이 다름 | |

📌 [T1] **1M 컨텍스트에서 DeepSeek-V3.2 대비**

| | 단일 토큰 추론 FLOPs | KV cache |
|---|---|---|
| **V4-Pro** (1.6T / 49B) | **27%** | **10%** |
| **V4-Flash** (284B / 13B) | **10%** | **7%** |

> 💡 **KV가 10%인데 FLOPs가 27%인 것에 주목할 만하다.**
> 압축은 저장을 줄이지만 **압축 연산 자체가 비용**이고, 학습된 압축이라
> 단순 평균보다 무겁다. `1.4`의 MLA에서 본 것과 같은 구조 —
> **메모리를 아끼고 연산으로 지불한다.**

### 코드와 텐서

**CSA** — DSA(`1.8`)의 표에서 훑는 대상이 `S`에서 `S/4`로 바뀐다.

| # | 연산 | shape 변화 | 종류 |
|---|---|---|---|
| p1 | **토큰 압축** (학습됨) | `(B,S,·)` → `(B,S/4,·)`, 겹치는 두 계열 | **GEMM + 가중합** |
| p2 | indexer 점수 | → `(B,1,**S/4**)` | batched GEMV — **대상이 1/4** |
| p3 | top-k = 1024 | → `(B,1,1024)` 인덱스 | **sort / top-k** |
| p4 | 엔트리 모으기 | → `(B,1024,·)` | **gather** ★ |
| p5 | attention | → `(B,n_h,1,d)` | batched GEMV |

**HCA** — 압축률을 32배로 올리는 대신 p3·p4가 없다.

| # | 연산 | shape 변화 | 종류 |
|---|---|---|---|
| h1 | **고압축** (학습됨) | `(B,S,·)` → `(B,**S/128**,·)` | GEMM + 가중합 |
| h2 | attention (dense) | → `(B,n_h,1,S/128)` | batched GEMV — **연속 읽기** |

1M 컨텍스트로 넣어보면 차이가 선명하다.

| | 압축 후 엔트리 | 실제로 보는 것 | 접근 |
|---|---|---|---|
| **CSA** | 250,000개 | top **1,024**개 | **gather** |
| **HCA** | **7,812개** | 7,812개 전부 | **연속** |

> 💡 **두 층이 하는 일이 완전히 다르다.**
> CSA는 25만 개 중 1천 개를 골라 정밀하게 본다. HCA는 8천 개를 통째로 훑는다.
> 보는 토큰 수로 치면 CSA가 4천 개(1024×4), HCA가 100만 개 전부다.
>
> **"정밀하게 조금" 층과 "거칠게 전부" 층을 번갈아 두는 것** —
> 이게 1:1 교대의 의미다. 어느 하나로는 부족하다.

> 💡 **HCA의 시스템적 이점이 이 표에 있다.** top-k 정렬도, gather도 없다.
> `1.7`에서 새로 생겼던 두 가지 골칫거리가 사라지고, 읽기가 다시 **연속**이 된다.
> 대신 압축률을 크게 올려 정보를 더 버린다.
>
> CSA/HCA 교차 배치는 결국 **"정렬·gather를 감수하는 층"과 "감수하지 않는 층"을
> 번갈아 두는 것**이다. 층마다 커널도 비용 프로파일도 다르다.

### 시스템 관점에서 새로 생긴 것

교차 배치가 만드는 문제가 있다. **층마다 메모리·연산 프로파일이 다르다.**
CSA 층과 HCA 층은 읽는 양도, 접근 패턴도 다르다.

지금까지의 모델은 모든 층이 같은 모양이라 스케줄링이 단순했다. 이제는 그렇지 않다.
이 문제는 `02-linear-attention`의 하이브리드 설계, `10-serving`의 스케줄링에서
다시 만나게 된다.

### 정리 — CSA

| | |
|---|---|
| **핵심 아이디어** | 4개 토큰의 KV를 학습된 압축으로 하나로 묶어 후보를 1/4로 줄이고, 그 위에 DSA의 lightning indexer를 얹어 상위 1024개를 고른다 |
| **장점** | · **압축과 선택의 절감이 곱해진다** — 저장량과 읽는 양을 동시에 감소<br>· indexer가 훑을 대상이 `S`에서 `S/4`로 줄어 **DSA가 남긴 `O(S)` 부담을 완화**<br>· 압축이 **학습된다** — 고정 평균이 아니라 무엇을 남길지 배운다<br>· 겹치는 두 계열(`C^a`, `C^b`)로 압축 경계의 정보 손실을 줄임 |
| **한계** | · **압축이 되돌릴 수 없는 손실** — 묶인 4개 토큰을 다시 구분할 수 없다<br>· top-k 정렬과 **gather가 그대로 남는다** (`1.7`의 문제)<br>· 압축 연산 자체가 비용 — KV는 10%인데 FLOPs는 27%<br>· 전용 커널 필요. 처음부터 그렇게 학습해야 함 |
| **대표 모델** | **DeepSeek-V4-Pro**(`m`=4, top-1024) · **V4-Flash**(`m`=4, top-512) |
| **다음으로** | 정밀하게 조금 보는 것만으로는 부족하다. 거칠게 전부 보는 층을 섞는다 → **HCA** |

### 정리 — HCA

| | |
|---|---|
| **핵심 아이디어** | 압축률을 128로 크게 올리는 대신, 압축 결과는 선택 없이 전부 본다 |
| **장점** | · **top-k 정렬도 gather도 없다** — `1.7`에서 생긴 두 골칫거리가 사라지고 읽기가 다시 연속<br>· 컨텍스트 **전체를 빠짐없이** 본다 (1M이어도 압축 후 8천 개면 dense가 가능)<br>· CSA와 1:1로 교차해 "정밀 + 광역"을 층 단위로 분담<br>· KV가 `S/128` — 압축 계보 중 가장 공격적 |
| **한계** | · 압축률 128이라 **세부 정보 손실이 크다** — 단독으로는 못 쓴다<br>· 128개 토큰이 한 엔트리로 뭉개져 정확한 검색이 불가능<br>· 층마다 프로파일이 달라 **스케줄링과 KV 할당이 복잡해진다** |
| **대표 모델** | **DeepSeek-V4-Pro / V4-Flash** (`m'`=128, CSA와 1:1 교차) |
| **다음으로** | 여기까지가 가로 방향(헤드·차원·토큰)이다. 마지막으로 세로 방향이 남았다 → **CLA · YOCO** |

---

지금까지 아홉 개 모듈은 전부 **가로 방향**이었다. 헤드를 공유하거나, 차원을 줄이거나,
토큰을 골랐다. 전부 한 레이어 안에서 벌어지는 일이다.

그런데 KV cache의 크기에는 `L`이 곱해져 있었다. **레이어 수만큼 캐시가 복사되는 셈**이다.
여기는 왜 아무도 안 건드렸을까?

---

## 1.10 CLA와 YOCO — 레이어끼리 공유하기

*Cross-Layer Attention · You Only Cache Once*

### 왜 나왔나

관찰에서 출발한다. **레이어마다 독립 KV를 저장하는 데 중복이 있다.** CLA는 인접
레이어가 같은 KV를 쓰게 하고, YOCO는 모델 후반부가 하나의 전역 KV를 공유하게 한다.

비슷하다면 굳이 따로 만들어 따로 저장할 이유가 없다.

### 아이디어와 구조

두 가지 방식이 있다.

```
  CLA (인접 그룹)                    YOCO (절반 공유)

 Layer 1 ─ KV₁ ──┐                 Layer 1 ─ KV₁
 Layer 2 ─────────┤ 공유            Layer 2 ─ KV₂    self-decoder
 Layer 3 ─ KV₂ ──┐                    ...           (자기 KV 생성)
 Layer 4 ─────────┤ 공유            Layer L/2 ─ KV*  ★ 여기서 한 번
 Layer 5 ─ KV₃ ──┐                 ─────────────────
 Layer 6 ─────────┘                 Layer L/2+1 ──┐
                                       ...         ├ KV* 재사용
                                    Layer L ───────┘  cross-decoder
```
> **그림 1.10** — 왼쪽은 인접 레이어를 짝지어 묶고, 오른쪽은 중간에서 한 번 만든 것을
> 위쪽 절반 전체가 쓴다.

**CLA**는 레이어를 몇 개씩 그룹으로 묶고, 그룹 안에서 가장 아래 레이어가 만든 K, V를
위 레이어들이 그대로 쓴다. 그룹 크기가 2면 캐시가 절반이다.

**YOCO**는 더 과감하다. 모델을 절반으로 나눠, 아래 절반(self-decoder)은 각자 KV를
만들지만 **위 절반(cross-decoder)은 중간에서 만들어진 KV 하나만 본다.**

### 이 축이 특별한 이유 — 직교성

여기가 중요하다. `1.1`~`1.9`의 방법들은 서로 대체 관계인 경우가 많다.
GQA를 쓰면서 동시에 MLA를 쓰지는 않는다.

그런데 **레이어 방향 공유는 그것들과 곱해진다.**

```
 GQA (헤드 1/8)  ×  CLA (레이어 1/2)  =  1/16
 MLA            ×  YOCO             =  더 작게
```

가로로 이미 줄인 것 위에 세로로 또 줄일 수 있다. 조합 가능성은 `99-landscape`에서
정리한다.

### 추론에서 달라진 것

| | 일반 | CLA / YOCO |
|---|---|---|
| KV 저장 | 레이어마다 | **그룹당 하나 / 절반만** |
| 추가 연산 | — | 거의 없음 |
| 새로 생긴 제약 | — | **레이어 간 의존성** |
| 다른 기법과 | — | **조합 가능** (직교) |

마지막에서 두 번째 줄이 대가다. 원래 레이어들은 KV에 대해 독립적이었는데,
이제 위 레이어가 아래 레이어의 결과를 기다린다. **파이프라인 병렬(PP)로 레이어를
GPU에 나눠 배치할 때 제약**이 되고, prefill 단계의 병렬성도 일부 줄어든다.
`10-serving`에서 다시 본다.

### 코드와 텐서

CLA(그룹 크기 2) 기준이다. 짝수 레이어에서 어떤 일이 벌어지는지가 전부다.

| # | 연산 | 일반 | CLA 짝수 레이어 |
|---|---|---|---|
| 1 | Q 투영 | GEMV | GEMV — **그대로** |
| 2·3 | KV 투영 | GEMV `(d, n_kv·d_h)` ×2 | **생략** ← 연산이 사라진다 |
| 5 | 캐시 append | memory write | **생략** |
| 6·9 | 점수·가중합 | 자기 레이어의 `K, V` | **아래 레이어의 `K, V`를 그대로 참조** |
| 10 | 출력 투영 | GEMV | GEMV — 그대로 |

> 💡 **이 방식은 KV projection과 write를 생략한다.**
> `1.5`는 게이트를, `1.7`은 정렬과 gather를, `1.8`은 indexer를 추가했다. 전부 뭔가를
> 지불하고 메모리를 샀다. CLA·YOCO는 2·3번 GEMV와 5번 write를 **없애면서** 캐시도 줄인다.
>
> 그래서 순수 비용만 보면 가장 유리한데, 대신 **품질**과 **레이어 독립성**이라는
> 다른 종류의 대가를 치른다. 대형 모델에서 아직 주류가 아닌 이유다.

### 정리 — CLA · YOCO

| | |
|---|---|
| **핵심 아이디어** | 인접 레이어의 K, V가 비슷하다는 관찰에 기반해, 레이어 그룹이 하나의 KV를 공유 |
| **장점** | · **다른 모든 기법과 곱해진다** — 가로 방향 절감 위에 세로로 추가 절감<br>· 추가 연산이 거의 없다 (그냥 안 만들고 재사용)<br>· 절감 비율을 그룹 크기로 자유롭게 조절<br>· 온디바이스처럼 메모리가 절대적으로 부족한 환경에 효과적 |
| **한계** | · **레이어 간 의존성이 생긴다** — 파이프라인 병렬화와 prefill 병렬성에 제약<br>· 공유 범위를 넓히면 품질 저하 (레이어별 특화가 사라짐)<br>· 어느 레이어를 원본으로 삼을지 설계 공간이 넓고 정답이 불분명 |
| **대표 모델** | CLA·YOCO 연구 모델. 공개 대형 모델에서는 아직 제한적으로 채택 |
| **다음으로** | 여기까지가 "KV를 줄이는" 열 가지 방법이다. 다음 파일은 질문 자체를 바꾼다 — **KV를 아예 없애면?** |

---

## 전체 비교

| 모듈 | 발상 | KV 저장량 | 읽는 양 | 재학습 | 대표 모델 |
|---|---|---|---|---|---|
| **MHA** | 기준점 | `2·n_h·d_h` | 전체 | — | GPT-2/3, Llama 1 |
| **MQA** | KV 헤드 1개 | `2·d_h` | 전체 | 필요 | PaLM, Falcon |
| **GQA** | 그룹당 KV 1개 | `2·n_kv·d_h` | 전체 | uptraining | **Llama 3, Qwen3, Gemma 3** |
| **MLA** | 저차원 latent + 흡수 | `d_c + d_r` (576) | 전체 | 필요 | **DeepSeek-V2/V3, Kimi K2, GLM-5** |
| **Gated Attn** | 출력 게이트 | 변화 없음 | 변화 없음 | 필요 | Qwen3-Next, Qwen3.5 |
| **SWA** | 최근 `W`개만 | `min(S,W)`로 상한 | `W`개 | 필요 | **Mistral 7B, Gemma 3** |
| **NSA** | 압축·선택·지역 병렬 | 거의 그대로 | **블록 단위 선택** | 처음부터 | DeepSeek 연구 라인 |
| **MoBA** | MoE식 블록 라우팅 | 거의 그대로 | **블록 단위 선택** | 가벼움 | Moonshot 연구 라인 |
| **DSA** | 싼 indexer + MLA | MLA와 동일 | `k`=2048개 | 추가 단계 | **DeepSeek-V3.2, GLM-5** |
| **CSA** | 압축 후 선택 | **`S/4`** | top-1024 엔트리 | 처음부터 | **DeepSeek-V4** |
| **HCA** | 고압축 후 dense | **`S/128`** | 압축 결과 전부 | 처음부터 | **DeepSeek-V4** |
| **CLA/YOCO** | 레이어 공유 | `× 1/그룹` | — | 필요 | 연구 모델 |

읽어야 할 흐름은 이렇다.

1. **`1.1`→`1.4`는 "무엇을 저장할까"의 역사다.** 헤드 공유(상수배) → 차원 압축(더 큰 상수배).
   그런데 전부 `S`에 비례한다는 사실은 변하지 않았다.
2. **`1.6`부터는 "무엇을 읽을까"로 질문이 바뀐다.** 고정 패턴(SWA) → 학습된 선택(NSA/MoBA)
   → 저렴한 선택(DSA) → 압축과 선택의 결합(CSA/HCA).
3. **`1.10`은 방향이 다르다.** 세로 방향이라 나머지 전부와 곱해진다.
4. 그리고 `1.5`는 KV와 무관하다. **softmax가 강제로 확률을 배분해야 한다는
   구조적 문제**를 다루는 곁가지다.

여기까지 와도 남는 것이 있다. **어떤 방법도 KV를 완전히 없애지는 못했다.**
읽는 양은 크게 줄었지만 저장은 여전히 `S`에 비례하거나(NSA, DSA),
상한이 있더라도 정보를 버리는 대가를 치렀다(SWA, CSA).

`02-linear-attention.md`는 질문 자체를 바꾼다. **애초에 토큰마다 뭔가를 남기지 않으면
어떻게 되는가.**

---

## Sources

**T1 — 논문**
- Vaswani et al. (2017), *Attention Is All You Need*, arXiv:1706.03762 — MHA
- Shazeer (2019), *Fast Transformer Decoding: One Write-Head is All You Need*, arXiv:1911.02150 — MQA
- Ainslie et al. (2023), *GQA: Training Generalized Multi-Query Transformer Models from Multi-Head Checkpoints*, arXiv:2305.13245
- DeepSeek-AI (2024), *DeepSeek-V2*, arXiv:2405.04434 — MLA, decoupled RoPE, "GQA 2.25 groups"
- DeepSeek-AI (2024), *DeepSeek-V3*, arXiv:2412.19437 — MLA 실전 설정 (`d_c`=512, `d_r`=64)
- Qiu et al. (2025), *Gated Attention for Large Language Models*, NeurIPS 2025 Best Paper
- Jiang et al. (2023), *Mistral 7B*, arXiv:2310.06825 — SWA
- Xiao et al. (2023), *Efficient Streaming Language Models with Attention Sinks*, arXiv:2309.17453
- Yuan et al. (2025), *Native Sparse Attention*, arXiv:2502.11089 — 블록 32/stride 16, 선택 블록 64/top-16, 윈도우 512, 64k 속도 배수
- Lu et al. (2025), *MoBA: Mixture of Block Attention for Long-Context LLMs*, arXiv:2502.13189
- DeepSeek-AI (2025), *DeepSeek-V3.2* — DSA, lightning indexer, top-k 2048
- DeepSeek-AI (2026), *DeepSeek-V4*, arXiv:2606.19348 — CSA/HCA 명칭, `m`=4 / `m'`=128,
  학습된 겹침 압축, DSA indexer 재사용, 1M 효율 수치
- **DeepSeek-V4-Pro / V4-Flash `config.json`** (HuggingFace) — 레이어별 `compress_ratios`,
  `index_topk`=1024, `index_n_heads`=64, `index_head_dim`=128, `sliding_window`=128
- Brandon et al. (2024), *Reducing Transformer Key-Value Cache Size with Cross-Layer Attention*, arXiv:2405.12981
- Sun et al. (2024), *You Only Cache Once*, arXiv:2405.05254
- 각 모델 HuggingFace `config.json` — 하이퍼파라미터

**T2 — 구현**
- HuggingFace `transformers` `modeling_llama.py` — `repeat_kv()` (`1.3` 주의 박스)
- FlashAttention / vLLM / SGLang attention 커널 — GQA 인덱싱, FlashMLA 흡수 구현
- SGLang DSA 구현 노트 — indexer와 sparse MLA 결합

**범위와 주의**
- NSA와 CSA는 구조적으로 닮았지만, DeepSeek-V4가 명시하는 직접 계보는 **DSA → CSA**다.
- sparse attention의 실제 이득은 top-k 크기뿐 아니라 indexer, 정렬, gather 커널의
  지역성과 배치 크기에 좌우된다.
