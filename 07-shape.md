# 07. Shape — 모델의 형상이 지연을 정한다

지금까지 여섯 파일은 전부 **블록 안쪽**이었다. attention을 어떻게 할지, FFN을 어떻게 할지,
정규화를 어디에 둘지.

이 파일은 밖으로 나온다. **그 블록을 몇 개 쌓고, 하나를 얼마나 넓게 만들 것인가.**

시시해 보이는 질문인데 시스템 관점에서는 그렇지 않다. 이유는 한 줄로 요약된다.

> **너비는 tensor parallelism으로 한 레이어의 연산을 나눌 수 있다. 깊이는 pipeline
> stage로 배치할 수 있지만, 한 토큰은 레이어를 순서대로 통과해야 한다.**

`00-foundations` `0.4`의 분류로는 **직렬 지연 압력**이다.
그리고 이 압력은 실제로 최근 모델의 형상을 바꾸고 있다.

## 이 장의 발전 계보와 시스템 영향

| 설계 축 | 대표 선택 | 해결하려는 병목 | 핵심 아이디어 | 시스템 영향 |
|---|---|---|---|---|
| depth ↔ width | 깊고 좁게 / 얕고 넓게 | 품질과 token latency | 같은 parameter를 layer 수와 hidden width에 배분 | depth는 직렬 지연, width는 TP·큰 GEMM으로 병렬화 가능 |
| block 내부 직렬성 | **parallel block** | attention→FFN 순차 실행 | attention과 FFN을 같은 입력에서 병렬 계산 | critical path·collective 수 감소, 품질 trade-off 가능 |
| vocabulary | 큰 vocab | 같은 문장을 더 적은 token으로 표현 | tokenizer와 embedding/LM head 확대 | sequence FLOPs 감소, embedding memory·logit compute 증가 |
| device memory | **PLE** | 모든 embedding성 parameter를 accelerator에 상주시킴 | 필요한 parameter를 외부 메모리에서 조회 | HBM 절감, host-device transfer·prefetch scheduling 증가 |
| 탄력적 너비 | **MatFormer** | 배포 예산마다 별도 모델 필요 | 중첩된 FFN subnetwork 중 일부를 선택 | 품질·지연 조절, 여러 실행 shape 지원 필요 |
| embedding 공유 | weight tying | input/output embedding 중복 | 두 matrix를 하나로 묶음 | parameter memory 감소, 대형 모델에서는 표현력 제약 가능 |

> **이 장의 병목 이동:** parameter 수가 같아도 **depth는 latency**, **width는 communication과
> GEMM 효율**, **vocab은 token 수와 embedding 비용**을 바꾼다. 모델 크기만으로는 실제
> 지연을 예측할 수 없고, 같은 parameter budget의 **형상**이 시스템 실행 경로를 결정한다.

---

## 계보 지도

```
     [블록을 몇 개, 얼마나 넓게]
                │
        ┌───────┴────────┐
        │                │
   depth vs width    parallel block
   깊이는 직렬이다    (GPT-J, PaLM)
        │            레이어 안 직렬성 제거
        │
        │
     [임베딩이라는 큰 덩어리]
                │
        ┌───────┼────────┐
        │       │        │
   vocab 크기  PLE     weight tying
              (Gemma 3n) 입출력 공유
```
> **그림 7.0** — 축7의 구성. 계보라기보다 서로 독립적인 설계 결정들에 가깝다.

**목차**

| | 절 | 한 줄 |
|---|---|---|
| [7.1](#71-depth-vs-width--깊이는-쪼갤-수-없다) | **depth vs width** | 깊이는 쪼갤 수 없다 ★ |
| [7.2](#72-parallel-block--레이어-안의-직렬성-없애기) | **parallel block** | 레이어 안의 직렬성 없애기 |
| [7.3](#73-embedding과-vocab--잊기-쉬운-큰-덩어리) | **embedding · vocab** | 잊기 쉬운 큰 덩어리 |
| [7.4](#74-per-layer-embedding--파라미터를-가속기-밖으로) | **PLE · MatFormer** | 파라미터를 가속기 밖으로 |
| [7.5](#75-weight-tying--묶을-것인가) | **weight tying** | 묶을 것인가 |

---

## 7.1 depth vs width — 깊이는 쪼갤 수 없다

### 문제 설정

파라미터 예산이 정해져 있다. 레이어를 많이 쌓고 각각을 좁게 만들 것인가,
적게 쌓고 넓게 만들 것인가.

```
 깊고 좁게              얕고 넓게
 ┌──┐  L=92            ┌────────┐  L=78
 ├──┤                  ├────────┤
 ├──┤                  ├────────┤
 ├──┤                  ├────────┤
 ├──┤                  └────────┘
 ├──┤
 └──┘
```

### 품질 관점

어느 정도까지는 **깊은 쪽이 유리하다.** 레이어를 쌓는다는 건 변환을 합성한다는 뜻이고,
합성이 많을수록 복잡한 함수를 표현할 수 있다.

다만 **수확 체감이 뚜렷하다.** 80층에서 90층으로 늘리는 것이 20층에서 30층으로
늘리는 것만큼의 이득을 주지는 않는다.

### 시스템 관점 — 여기가 핵심

깊이에는 시스템 쪽 대가가 여럿 붙는다.

| 무엇 | 왜 |
|---|---|
| **직렬 지연** | 레이어는 순서대로 통과해야 한다. decode 한 스텝의 지연 = `L` × (레이어 하나) |
| **커널 런치 오버헤드** | 레이어마다 커널이 여러 개. `L`배로 누적된다 |
| **동기화 지점** | TP를 쓰면 레이어마다 all-reduce. `L`배 |
| **GEMM이 작아진다** | 좁은 레이어는 행렬이 작아 GPU를 못 채운다 |

그리고 병렬화 전략과의 관계가 결정적이다.

| | 무엇을 쪼개나 | decode에서 |
|---|---|---|
| **TP** (텐서 병렬) | **너비** | 잘 동작. 지연을 실제로 줄인다 |
| **PP** (파이프라인 병렬) | **깊이** | 파이프라인 버블 때문에 **작은 배치에서 비효율** |

> 💡 **비대칭이 여기 있다.**
> 너비는 GPU를 더 붙이면 실제로 빨라진다 (TP). 깊이는 GPU를 붙여도(PP)
> 지연이 줄지 않는다 — 파이프라인은 **처리량**을 늘리지 **지연**을 줄이지 않는다.
>
> 즉 **깊이는 하드웨어로 살 수 없는 비용**이다.

### 실제 설정 예

GLM-5의 공개 설정은 78개 레이어와 넓은 hidden dimension을 사용한다. 이 값만으로
이전 세대보다 깊이를 줄인 이유까지 단정할 수는 없지만, 깊이와 너비가 독립적인
시스템 선택임을 보여주는 사례다.

| 필드 | GLM-5 |
|---|---|
| `num_hidden_layers` | **78** |
| `hidden_size` | 6144 |
| `num_attention_heads` | 64 |
| `model_type` | `glm_moe_dsa` — **MLA + DSA** |
| `kv_lora_rank` / `q_lora_rank` | 512 / 2048 |
| `index_topk` | 2048 (DSA) |
| 실제 컨텍스트 | 202,752 |

중요한 것은 특정 모델의 의도를 추측하는 것이 아니라, 같은 파라미터 예산에서도
깊이는 직렬 지연을, 너비는 tensor-parallel 통신과 GEMM 크기를 바꾼다는 점이다.

### 추론에서 달라진 것

| | 깊고 좁게 | 얕고 넓게 |
|---|---|---|
| 품질 (같은 파라미터) | **유리** (체감 있음) | 약간 불리 |
| decode 지연 | **나쁨** | 좋음 |
| TP 확장성 | 제한적 (쪼갤 너비가 작음) | **좋음** |
| PP 확장성 | 좋음 | 제한적 |
| GEMM 효율 | 낮음 | **높음** |
| 커널 런치 누적 | 많음 | 적음 |
| KV cache | `L`에 비례 → **많음** | 적음 |

마지막 줄도 놓치면 안 된다. KV cache는 `B × S × L`에 비례하므로
**층을 줄이면 KV도 줄어든다.** `01-attention`과 연결되는 지점이다.

### 정리

| | |
|---|---|
| **핵심 아이디어** | 같은 파라미터 예산에서 깊이와 너비의 배분을 정하는 문제. 깊이는 직렬 지연으로 직결된다 |
| **장점 (얕고 넓게)** | · **decode 지연이 줄어든다** — 깊이는 병렬화로 해결 불가<br>· TP로 GPU를 붙이면 실제로 빨라진다<br>· GEMM이 커져 GPU 효율이 좋다<br>· KV cache도 함께 줄어든다 (`L`에 비례) |
| **한계 (얕고 넓게)** | · 같은 파라미터 기준 품질이 다소 불리<br>· 너비를 늘리면 TP 통신량이 커진다<br>· PP로 확장할 여지가 줄어든다 |
| **대표 모델** | **GLM-5**(92→78층) · 저지연을 노린 Flash 계열 모델들 |
| **다음으로** | 레이어 **사이**의 직렬성은 어쩔 수 없다. 레이어 **안**의 직렬성은? → **parallel block** |

---

## 7.2 parallel block — 레이어 안의 직렬성 없애기

### 아이디어

기본 블록은 attention과 FFN이 **순서대로** 실행된다.

```
 순차 (일반)
   x ─► norm ─► attention ─► (+) ─► norm ─► FFN ─► (+) ─► 출력
                             └── FFN은 attention을 기다린다 ──┘

 병렬 (parallel block)
        ┌─► attention ─┐
   x ─► norm           ├─► (+) ─► 출력
        └─► FFN ───────┘
        두 경로가 같은 입력을 쓴다
```
> **그림 7.2** — 같은 정규화 결과로 attention과 FFN을 각각 계산하고, 둘 다 residual에 더한다.

수식으로는 이렇게 바뀐다.

```
 순차:  y = x + FFN( norm₂( x + Attn(norm₁(x)) ) )
 병렬:  y = x + Attn(norm(x)) + FFN(norm(x))
```

### 무엇이 좋아지나

| 무엇 | 왜 |
|---|---|
| **직렬 의존성 제거** | FFN이 attention 결과를 기다리지 않는다 |
| **GEMM 병합 가능** | `Wq,Wk,Wv`와 `W1,W3`가 같은 입력을 쓰므로 **하나의 큰 GEMM으로 합칠 수 있다** |
| **TP 통신 감소** | all-reduce를 레이어당 2회에서 **1회**로 줄일 수 있다 |
| **norm 1회 절약** | 정규화를 두 번이 아니라 한 번만 |

두 번째와 세 번째가 실질적이다. `05-moe` `5.4`에서 봤듯 **GEMM은 클수록 GPU 효율이 좋고**,
통신 횟수는 `L`배로 누적되므로 절반으로 줄이는 것이 작지 않다.

### 대가

품질이 다소 떨어진다. FFN이 attention 결과를 못 보기 때문에 표현력이 준다.
다만 **모델이 클수록 손실이 작아진다**고 보고되어, 대형 모델에서는 쓸 만한 거래가 된다.

### 코드와 텐서

| # | 연산 | 순차 | 병렬 | 종류 |
|---|---|---|---|---|
| 1 | 정규화 | 2회 | **1회** | reduction |
| 2 | QKV + FFN 입력 투영 | 따로 2번 | **합쳐서 1번** | GEMV/GEMM — **더 큰 행렬** |
| 3 | attention | | | (`01` 참조) |
| 4 | 출력 투영 | 따로 | 합칠 수 있음 | GEMV |
| 5 | TP all-reduce | 2회 | **1회** | 통신 |

### 정리

| | |
|---|---|
| **핵심 아이디어** | attention과 FFN을 같은 정규화 입력으로 병렬 계산해 둘 다 residual에 더한다 |
| **장점** | · **레이어 안 직렬 의존성 제거**<br>· 입력 투영 GEMM을 합쳐 GPU 효율 향상<br>· **TP all-reduce를 레이어당 절반으로**<br>· 정규화 1회 절약 |
| **한계** | · FFN이 attention 결과를 보지 못해 품질이 다소 하락<br>· 작은 모델에서 손실이 더 크다<br>· 널리 쓰이지는 않는다 — 기존 학습 코드와 호환이 깨짐 |
| **대표 모델** | GPT-J · PaLM |
| **다음으로** | 블록 이야기는 끝났다. 블록 밖에 큰 덩어리가 하나 더 있다 → **embedding** |

---

## 7.3 embedding과 vocab — 잊기 쉬운 큰 덩어리

### 크기 감각

임베딩 테이블은 `V × d`다. Llama 3 70B로 계산하면

```
 128,256 × 8,192 ≈ 1.05B 파라미터
```

LM head도 같은 크기다. 둘이 합쳐 2.1B, 70B 모델에서는 3% 정도다. 무시할 만하다.

그런데 **작은 모델에서는 이야기가 완전히 달라진다.**

| 모델 크기 | 임베딩 + LM head | 비중 |
|---|---|---|
| 70B | ~2.1B | **3%** |
| 8B | ~2.1B | **26%** |
| 3B | ~1.0B (d가 작음) | **30% 이상** |

소형 모델에서는 **파라미터의 3분의 1이 단어장에 들어간다.**

### vocab이 커지는 추세

| 시기 | vocab | 이유 |
|---|---|---|
| GPT-2 | 50K | 영어 중심 |
| Llama 2 | 32K | |
| Llama 3 | **128K** | 다국어, 토큰 효율 |
| 최근 모델 | 128K ~ 256K | 다국어 + 코드 |

vocab이 커지면 같은 문장을 더 적은 토큰으로 표현할 수 있어 **추론 비용이 줄어든다.**
컨텍스트도 실질적으로 길어진다. 대신 임베딩 테이블이 커진다.

### decode에서의 비용

`00-foundations` `0.2` 표의 10번, LM head가 여기 해당한다.

| # | 연산 | shape 변화 | 종류 |
|---|---|---|---|
| 입구 | 임베딩 조회 | 인덱스 → `(B,1,d)` | **gather** (아주 작음) |
| 출구 | LM head | `(B,1,d) × (d, V)` → `(B,1,V)` | **GEMV — `V`가 크면 무겁다** |

입구는 한 줄만 꺼내오므로 사실상 공짜다. **출구가 문제다.**
`d`=8192, `V`=128K면 이 GEMV 하나가 1B 파라미터를 읽는다.
**레이어 하나보다 크다.**

> 💡 소형 모델에서 "레이어 수를 줄여도 별로 안 빨라지는" 현상의 원인 중 하나가 이것이다.
> 레이어를 아무리 줄여도 LM head는 그대로 남는다.

### 정리

| | |
|---|---|
| **핵심 아이디어** | `V × d` 임베딩 테이블과 LM head는 vocab이 커지면서 무시할 수 없는 덩어리가 되었다 |
| **장점 (큰 vocab)** | · 같은 문장을 **더 적은 토큰으로** 표현 → 추론 비용 감소<br>· 다국어·코드 처리 품질 향상<br>· 실질 컨텍스트 길이 증가 |
| **한계** | · **소형 모델에서 파라미터의 30% 이상**을 차지<br>· LM head GEMV가 레이어 하나보다 무거울 수 있다<br>· 학습에서 임베딩 gradient 처리 부담 |
| **대표 모델** | Llama 3 이후 128K가 표준. 다국어 특화 모델은 256K 이상 |
| **다음으로** | 이 덩어리를 가속기 메모리 밖에 둘 수 있다면 → **PLE** |

---

## 7.4 per-layer embedding — 파라미터를 가속기 밖으로

### 왜 나왔나

온디바이스 환경에서는 **가속기 메모리가 절대적으로 부족하다.**
휴대폰의 NPU가 쓸 수 있는 메모리는 몇 GB 수준이다.

그런데 모든 파라미터가 매 순간 필요한 건 아니다. 특히 임베딩 성격의 파라미터는
**조회 후 필요한 부분만 쓰인다.**

### 아이디어

Gemma 계열의 **Per-Layer Embeddings(PLE)** 는 레이어마다 별도의 임베딩을 두고,
그것을 가속기가 아닌 **일반 메모리에 보관**한다. 필요할 때 꺼내 쓴다.

```
 가속기 메모리 (빠르고 작다)      일반 메모리 (느리고 크다)
 ┌──────────────────┐            ┌──────────────────┐
 │  Transformer     │◄───────────│  Per-Layer        │
 │  레이어 가중치    │  필요할 때  │  Embeddings       │
 └──────────────────┘            └──────────────────┘
```
> **그림 7.4** — 파라미터를 두 계층으로 나눈다.

그래서 이름이 **E2B, E4B** 다. `E`는 **effective**의 약자다.

📌 [T1] 실제 숫자로 보면 이렇다.

| 모델 | 총 파라미터 | **effective** | 실행 RAM |
|---|---|---|---|
| **Gemma 3n E2B** | 5.1B | **2.3B** | **2 GB** |
| **Gemma 3n E4B** | 8B | **4.5B** | **3 GB** |

5B 모델을 2 GB로 돌린다. 절반 이상의 파라미터를 가속기 밖에 두는 셈이다.

📌 [T1] 동작 방식도 확인됐다. **PLE 데이터는 모델 실행 메모리 밖에서 미리 생성해
빠른 저장장치에 캐시해두고, 레이어를 실행할 때마다 가져와 붙인다.**
그 부분은 CPU에서 계산할 수 있다.

**MatFormer**(Matryoshka Transformer)는 같은 목표의 다른 접근이다.
📌 [T1] 표준 Transformer 레이어 안에 **크기가 다른 FFN 블록 여러 개를 중첩**해두고
**함께 학습**한다. 그래서 상황에 따라 작은 부분 모델만 떼어 쓸 수 있다.

> 💡 **둘의 차이가 중요하다.**
> PLE는 **파라미터를 어디에 둘지**의 문제이고(메모리 계층),
> MatFormer는 **얼마나 쓸지**의 문제다(탄력적 추론).
> 온디바이스에서는 둘 다 필요해서 함께 쓴다.

### 시스템 관점

이건 아키텍처라기보다 **메모리 계층 설계**에 가깝다.

| | 무엇 |
|---|---|
| 얻는 것 | 가속기 메모리 요구량이 크게 줄어든다 |
| 잃는 것 | 느린 메모리에서 읽어오는 지연이 추가된다 |
| 성립 조건 | 그 파라미터가 **자주 안 쓰이거나, 조회 성격**이어야 한다 |

`10-serving`에서 볼 KV cache 오프로딩과 같은 아이디어다.
**빠르고 작은 메모리와 느리고 큰 메모리를 계층으로 쓴다.**

### 정리

| | |
|---|---|
| **핵심 아이디어** | 임베딩 성격의 파라미터를 가속기 밖 메모리에 두고 필요할 때 가져온다 |
| **장점** | · **가속기 메모리 요구량 대폭 감소** — 온디바이스 실행을 가능하게 함<br>· 조회 성격의 파라미터라 지연 영향이 제한적<br>· MatFormer와 결합하면 하나의 모델로 여러 크기 제공 |
| **한계** | · **느린 메모리 접근이 추가된다**<br>· 데이터센터 서빙에서는 이점이 크지 않다 (배치가 크면 오히려 손해)<br>· 어떤 파라미터를 밖에 둘지가 설계 부담 |
| **대표 모델** | **Gemma 3n** |
| **다음으로** | 임베딩을 줄이는 더 단순한 방법 하나 → **weight tying** |

---

## 7.5 weight tying — 묶을 것인가

### 아이디어

입력 임베딩과 출력 LM head는 둘 다 `V × d` 행렬이다.
**같은 가중치를 쓰면** `V × d` 하나를 통째로 아낀다.

의미상으로도 그럴듯하다. 입력에서 "이 단어의 벡터"와 출력에서 "이 벡터와 가까운 단어"는
같은 공간을 쓰는 게 자연스럽다.

### 언제 묶고 언제 안 묶나

| | 묶기 (tied) | 안 묶기 (untied) |
|---|---|---|
| 파라미터 | `V × d` 절약 | 그만큼 더 씀 |
| 소형 모델 | **유리** — 절약분이 크다 | |
| 대형 모델 | | **유리** — 성능이 더 좋다 |

소형 모델에서 절감 비율이 더 크지만, tying 여부는 모델 크기만으로 정해지지 않는다.
입력·출력 표현을 공유했을 때의 품질과 메모리 예산을 함께 비교해야 한다.

### 정리

| | |
|---|---|
| **핵심 아이디어** | 입력 임베딩과 출력 LM head가 같은 가중치를 공유한다 |
| **장점** | · `V × d` 파라미터를 통째로 절약 — 소형 모델에서 특히 큼<br>· 입출력 표현 공간이 일관됨<br>· 메모리와 학습 부담 동시 감소 |
| **한계** | · 입력과 출력에서 요구되는 표현이 다를 수 있음<br>· 품질 효과는 모델과 학습 조건에 따라 달라짐 |
| **대표 모델** | 두 설계 모두 널리 쓰이므로 `config.json`의 tying 설정 확인 필요 |
| **다음으로** | 형상 이야기는 끝. 다음은 한 스텝에 토큰을 여러 개 뽑는 법 → **08-decoding** |

---

## 이 파일의 정리

| 결정 | 품질 쪽 | 시스템 쪽 |
|---|---|---|
| **깊이 ↑** | 유리 (체감) | **직렬 지연 증가, 하드웨어로 못 산다** |
| **너비 ↑** | 약간 불리 | TP로 확장 가능, GEMM 효율 좋음 |
| **parallel block** | 약간 불리 | GEMM 병합, TP 통신 절반 |
| **vocab ↑** | 유리 (토큰 효율) | 소형 모델에서 파라미터 비중 급증 |
| **PLE** | 중립 | 가속기 메모리 대폭 절약 (온디바이스) |
| **weight tying** | 소형 유리, 대형 불리 | `V × d` 절약 |

이 파일에서 가져갈 것은 하나다.

> **깊이는 단일 토큰의 직렬 의존성을 만든다.**
>
> PP는 여러 microbatch의 throughput을 높일 수 있지만, 한 토큰이 통과해야 할
> 레이어 수 자체를 줄이지는 않는다. 따라서 저지연 모델에서는 layer 수와
> layer당 실행 시간을 함께 봐야 한다.

---

## Sources

**T1 — 논문·리포트**
- Wang & Komatsuzaki (2021), GPT-J — parallel block
- Chowdhery et al. (2022), *PaLM* — parallel block 대규모 적용
- GLM-5 공개 `config.json` — 78층과 hidden shape
- Google, Gemma 3n 공식 개발자 문서 — Per-Layer Embeddings, MatFormer
- Press & Wolf (2017), *Using the Output Embedding to Improve Language Models* — weight tying
- 각 모델 HuggingFace `config.json` — `num_hidden_layers`, `hidden_size`, `vocab_size`

**T2 — 구현**
- Megatron-LM / vLLM의 TP·PP 구현 — 너비/깊이 분할과 통신 패턴
- 파이프라인 버블에 관한 서빙 프레임워크 문서

**범위와 주의**
- 깊이·너비 비교는 파라미터 수만 맞춘다고 공정해지지 않는다. 학습 compute,
  optimizer, 병렬화와 하드웨어 이용률까지 함께 봐야 한다.
- 이 장의 PLE·MatFormer 사례와 수치는 **Gemma 3n**에 한정한다.
