# 04. MoE — 파라미터는 늘리고 연산은 그대로

앞의 세 파일은 전부 attention 이야기였다. `00-foundations` 그림 0.1로 돌아가면
블록에는 절반이 더 있다. **FFN이다.**

그리고 decode에서 가장 비싼 단계가 바로 거기였다 (`0.2`의 9번). 파라미터의 대부분이
FFN에 있고, 토큰 하나를 만들 때마다 그걸 전부 읽어야 한다.

이 파일이 다루는 압력은 앞과 다르다. 메모리가 아니라 **연산량**이다.

> **모델을 키우면 성능이 좋아진다. 그런데 파라미터를 두 배로 늘리면 토큰당 연산도
> 두 배가 된다. 이 둘을 떼어놓을 수 없을까?**

MoE의 답은 명확하다. **파라미터는 전부 두되, 토큰마다 일부만 쓴다.**

그런데 이 선택은 공짜가 아니다. 이 파일의 후반부는 MoE가 연산량 압력을
**메모리 용량과 통신 압력으로 바꾼** 이야기다.

---

## 계보 지도

```
              FFN (2017)
                │
             SwiGLU (2020)
          게이트가 붙은 FFN
                │
        [파라미터와 연산을 분리]
                │
        GShard / Switch (2020-21)
        expert 여럿 + top-k 라우팅
                │
        ┌───────┴────────┐
        │                │
  [잘게 쪼개기]     [균형 잡기]
        │                │
  DeepSeekMoE (2024)   aux loss
  fine-grained          │
  + shared expert    aux-loss-free (2024)
        │                │
        └───────┬────────┘
                │
         granularity 논쟁 (2026)
      품질 ↔ 추론 처리량 트레이드오프
                │
          LatentMoE ⚠️


      [시스템 쪽 귀결]
      Expert Parallelism → all-to-all 통신 압력
```
> **그림 4.0** — 축4의 계보

**목차**

| | 절 | 한 줄 |
|---|---|---|
| [4.1](#41-ffn과-swiglu--무엇을-바꾸려는-건가) | **FFN · SwiGLU** | 무엇을 바꾸려는 건가 |
| [4.2](#42-moe-기본--라우터와-expert) | **MoE 기본** | 파라미터와 연산을 떼어놓기 |
| [4.3](#43-deepseekmoe--잘게-쪼개고-하나는-공유) | **DeepSeekMoE** | 잘게 쪼개고 하나는 공유 |
| [4.4](#44-granularity--얼마나-잘게-쪼갤-것인가) | **granularity** | 품질 ↔ 처리량 ★ |
| [4.5](#45-load-balancing--쏠림을-어떻게-막나) | **load balancing** | 쏠림을 어떻게 막나 |
| [4.6](#46-expert-parallelism--새로-생긴-통신-압력) | **EP · all-to-all** | 새로 생긴 통신 압력 ★ |

---

## 4.1 FFN과 SwiGLU — 무엇을 바꾸려는 건가

### 구조

FFN은 단순하다. 차원을 늘렸다가 줄인다.

```
 x (d) ──► W1 ──► 활성함수 ──► W2 ──► y (d)
              (d_ff)                  d_ff는 보통 4d 안팎
```

**SwiGLU**는 여기에 게이트를 붙인다. 행렬이 셋이 된다.

```
 FFN(x) = W2 · ( silu(W1 x) ⊙ W3 x )
                 └─ 게이트 ─┘  └값┘
```

같은 파라미터 예산으로 더 좋은 성능이 나와서, Llama 이후 사실상 표준이 되었다.
`d_ff`를 `8/3 · d` 정도로 잡아 파라미터 수를 맞추는 것이 관례다.

### 여기가 왜 문제인가

Llama 3 70B 기준으로 보면 명확하다.

| 부분 | 레이어당 파라미터 | 비중 |
|---|---|---|
| attention (`Wq,Wk,Wv,Wo`) | 약 0.2B | 1/4 |
| **FFN** (`W1,W2,W3`) | 약 0.7B | **3/4** |

그리고 `00-foundations` `0.6`에서 봤듯, decode에서 토큰 하나를 만들려면
**이 가중치를 전부 읽어야 한다.**

> **dense 모델에서 파라미터 수와 토큰당 연산량은 묶여 있다.**
> 모델을 키우면 비용도 정확히 그만큼 늘어난다. 이 결합을 끊는 것이 MoE의 목표다.

### 정리

| | |
|---|---|
| **핵심 아이디어** | 차원을 늘렸다 줄이는 두 층 신경망. SwiGLU는 게이트를 추가한 변형 |
| **장점** | · 구조가 단순하고 GEMM 세 번이라 GPU 효율이 좋다<br>· SwiGLU는 같은 파라미터로 더 나은 성능 |
| **한계** | · **파라미터의 3/4을 차지하고, 전부 매 토큰 읽어야 한다**<br>· 파라미터를 늘리면 연산량도 정비례해서 늘어난다 |
| **대표 모델** | SwiGLU는 Llama 2 이후 사실상 전부 |
| **다음으로** | 파라미터와 연산량의 결합을 끊는다 → **MoE** |

---

## 4.2 MoE 기본 — 라우터와 expert

### 아이디어와 구조

FFN 하나 대신 **여러 개(expert)를 두고, 토큰마다 몇 개만 쓴다.**

```
                    ┌── expert 1 ──┐
                    │   expert 2   │
   x ──► 라우터 ────┤   expert 3   ├──► 가중합 ──► y
        (top-k 선택) │     ...      │
                    └── expert E ──┘
                        ▲
              이 중 k개만 실제로 계산
```
> **그림 4.2** — 라우터가 `E`개 중 `k`개를 고르고, 그 출력을 가중합한다.

라우터는 작은 선형층 하나다. `x`로부터 `E`개 점수를 내고 상위 `k`개를 고른다.

핵심은 이 한 줄이다.

> **파라미터는 `E`배로 늘어나지만, 토큰당 연산은 `k`배에 그친다.**
> `k ≪ E`이므로 파라미터와 연산량이 분리된다.

DeepSeek-V3를 보면 감이 온다.

| | 값 |
|---|---|
| 총 파라미터 | 671B |
| 토큰당 활성 | **37B** |
| 비율 | 약 18배 |

671B 모델의 지식을 갖되 토큰당 계산은 37B 모델만큼만 한다.

### 그런데 공짜가 아니다

세 가지가 따라온다.

| 새 문제 | 왜 |
|---|---|
| **메모리 용량** | 계산은 `k`개만 하지만 **파라미터 671B를 전부 들고 있어야 한다** |
| **쏠림** | 라우터가 특정 expert만 고르면 불균형 → `4.5` |
| **통신** | expert가 여러 GPU에 흩어져 있으면 토큰을 보내고 받아야 한다 → `4.6` |

> 💡 **MoE는 연산량 압력을 메모리 용량과 통신 압력으로 바꾼 것이다.**
> `00-foundations` `0.4`에서 말한 압력 이전의 대표 사례다.

### 추론에서 달라진 것

| | dense FFN | MoE FFN |
|---|---|---|
| 저장하는 가중치 | `P` | **`P × E/k` 수준으로 증가** |
| decode에서 읽는 가중치 | 전부 | **선택된 `k`개만** |
| 연산량 | 기준 | `k/E` 수준 |
| 새로 생긴 연산 | — | 라우터, top-k, **all-to-all 통신** |
| 배치 효율 | 좋음 | **토큰이 흩어져 나빠진다** |

네 번째 줄과 다섯 번째 줄이 실전에서 아프다. 특히 마지막이 그렇다 —
배치 안 토큰들이 서로 다른 expert로 흩어지면, expert 하나가 받는 토큰이 몇 개 안 된다.
GEMM이 되어야 할 것이 GEMV가 되면서 GPU 효율이 떨어진다.

### 코드와 텐서

| # | 연산 | 식 | shape 변화 | 종류 |
|---|---|---|---|---|
| 1 | 라우터 | `x @ W_r` | `(B,1,d) × (d, E)` → `(B,1,E)` | GEMV (아주 작음) |
| 2 | **top-k** | 정렬/선택 | `(B,1,E)` → 인덱스 `(B,1,k)` + 가중치 | **top-k 정렬** |
| 3 | **dispatch** | 토큰을 expert의 GPU로 | `(B,1,d)` → 재배치 | **all-to-all 통신** ★ |
| 4 | expert FFN | `W1, W3, W2` | `(n_i, d) × (d, d_ff_e)` … | GEMM 또는 GEMV — **expert마다** |
| 5 | **combine** | 결과 회수 | 재배치 | **all-to-all 통신** ★ |
| 6 | 가중합 | `Σ gᵢ · yᵢ` | → `(B,1,d)` | elementwise |

`01-attention`에서 `gather`가 새로 등장했듯, 여기서는 **all-to-all 통신**이
새로운 종류로 등장한다. HBM보다 훨씬 좁은 자원을 쓰고, 모든 GPU가 동기화되어야 한다.

4번의 `n_i`는 expert `i`에 배정된 토큰 수다. 이 값이 균일한지 아닌지가
성능을 크게 좌우한다 — `4.5`의 주제다.

### 정리

| | |
|---|---|
| **핵심 아이디어** | FFN을 여러 개 두고 라우터가 토큰마다 `k`개만 골라 쓴다 |
| **장점** | · **파라미터와 연산량의 분리** — 671B 지식에 37B 계산<br>· decode에서 읽는 가중치가 `k/E`로 줄어 dense보다 오히려 빠를 수 있음<br>· expert마다 다른 것을 학습해 전문화가 일어남 |
| **한계** | · **파라미터 전부를 메모리에 들고 있어야 한다** — 용량 압력이 커짐<br>· 라우팅 쏠림 문제<br>· **all-to-all 통신이 새로 생긴다**<br>· 배치 안 토큰이 흩어져 expert별 GEMM이 작아짐 |
| **대표 모델** | GShard, Switch Transformer(원조) → Mixtral → **DeepSeek-V3, Qwen3-235B, GLM, Kimi K2, MiniMax** — 2024년 이후 대형 모델의 기본형 |
| **다음으로** | expert를 몇 개, 얼마나 크게 둘 것인가 → **DeepSeekMoE** |

---

초기 MoE는 expert를 크고 적게 뒀다. Switch Transformer는 `k=1`, 즉 토큰마다 하나만 썼다.

DeepSeek의 관찰은 이랬다. **expert가 크면 하나가 여러 역할을 겸해야 한다.**
전문화가 덜 일어난다는 뜻이다.

---

## 4.3 DeepSeekMoE — 잘게 쪼개고 하나는 공유

### 두 가지 변경

**① fine-grained expert** — 같은 활성 파라미터 예산을 유지하면서 expert를 잘게 쪼갠다.

```
 기존:   [  큰 expert  ][  큰 expert  ]  중 1개 선택
 변경:   [작][작][작][작][작][작][작][작] ... 중 8개 선택
```

expert 하나의 크기를 1/4로 줄이고 개수를 4배로, 선택 개수도 4배로 늘리면
**활성 파라미터는 그대로인데 조합의 수가 폭발적으로 늘어난다.**

`E`개 중 `k`개를 고르는 경우의 수를 생각하면 명확하다. 64개 중 2개보다
256개 중 8개가 훨씬 많은 조합을 만든다. 표현력이 그만큼 커진다.

**② shared expert** — 항상 활성인 expert를 하나(또는 몇 개) 둔다.

```
   x ──┬──► shared expert ────────┐
       │   (항상 계산)             ├──► 합산 ──► y
       └──► 라우팅 expert × k ────┘
```

의도는 분업이다. 모든 토큰에 공통으로 필요한 처리(문법, 기본 어휘 등)를
shared expert가 맡으면, 라우팅되는 expert들은 **특화에 집중**할 수 있다.
공통 지식이 모든 expert에 중복 저장되는 낭비도 줄어든다.

### 실제 설정

| 모델 | `E` | `k` | shared | 균형 방식 |
|---|---|---|---|---|
| Switch Transformer | 64~2048 | 1 | 없음 | aux loss |
| Mixtral 8×7B | 8 | 2 | 없음 | aux loss |
| **DeepSeek-V3** | **256** | **8** | 1 | **aux-loss-free** |
| Qwen3-Next | 다수 (4배 증가) | — | 있음 | — |
| **GLM-5** ✅ | **256** (GLM-4.7의 160에서 증가) | **8** | **1** | `noaux_tc` |
| **DeepSeek-V4-Pro** ✅ | **384** | **6** | **1** | `noaux_tc` (앞 3층 hash) |
| **DeepSeek-V4-Flash** ✅ | **256** | **6** | **1** | 동일 |
| **Kimi K3** ✅ | **896** | **16** | **2** | `noaux_tc` + LatentMoE |

✅ 표시는 HuggingFace `config.json`에서 직접 확인한 값이다.

> 💡 **V4에서 `k`가 8에서 6으로 줄었다.** V3(256/8)에서 V4-Pro(384/6)로 가면서
> expert는 늘리고 활성은 줄인 것이다. 통신 목적지 수를 줄이려는 선택으로 보이는데,
> 논문이 이유를 밝힌 대목은 확인하지 못했다.

**추세가 분명하다. expert 개수가 계속 늘고 있다.**

### 정리

| | |
|---|---|
| **핵심 아이디어** | expert를 잘게 쪼개 조합의 수를 늘리고, 공통 지식은 항상 활성인 shared expert에 몰아준다 |
| **장점** | · 같은 활성 파라미터로 **훨씬 많은 조합** → 표현력 증가<br>· expert의 전문화가 뚜렷해짐<br>· shared expert가 공통 지식 중복 저장을 줄임<br>· 이후 대형 MoE의 사실상 표준 설계 |
| **한계** | · expert가 작아져 **개별 GEMM 크기가 줄어든다** → GPU 효율 저하<br>· `k`가 커져 **통신 횟수와 복잡도 증가**<br>· 라우팅 쏠림이 더 민감해짐 |
| **대표 모델** | **DeepSeek-V2/V3/V4** · Qwen3 시리즈 · GLM-4.5/4.7/5 · Kimi K2/K3 · Step 3.5 Flash · Arcee Trinity(변형) |
| **다음으로** | 그런데 정말 잘게 쪼갤수록 좋은가. **시스템 쪽에서 반론이 있다** → **granularity** |

---

## 4.4 granularity — 얼마나 잘게 쪼갤 것인가

이 절은 **아키텍처 설계와 시스템 제약이 정면으로 충돌하는 지점**이다.
이 위키 전체에서 가장 교과서적인 트레이드오프 사례라고 봐도 좋다.

### 두 관점이 정반대를 가리킨다

**품질 관점**에서는 `4.3`에서 본 대로 잘게 쪼갤수록 좋다.

**시스템 관점**에서는 정반대다.

| 무엇 | 잘게 쪼개면 |
|---|---|
| expert별 GEMM 크기 | **작아진다** — GPU가 채워지지 않음 |
| all-to-all 통신 | `k`가 크니 **더 많은 목적지로 흩어짐** |
| expert별 실효 배치 | 토큰이 더 많은 곳으로 분산 → **각 expert가 받는 토큰이 줄어듦** |
| 라우팅 쏠림의 영향 | expert가 작을수록 **불균형이 더 자주 발생** |
| 커널 오버헤드 | expert 수만큼 커널 호출 |

세 번째가 특히 아프다. decode에서 배치 32에 `E=256`, `k=8`이면
토큰 256개(32×8)가 256개 expert로 흩어진다. **expert 하나당 평균 한 개다.**
GEMM이 되어야 할 연산이 GEMV가 되면서 `00-foundations` `0.6`의 memory-bound에 갇힌다.

### 실제 사례

📌 **Arcee Trinity Large는 DeepSeek식보다 expert를 일부러 굵게 만들었다.**
품질에서 조금 손해를 보더라도 **추론 throughput을 확보하겠다는 선택**이다.

이 결정이 흥미로운 이유는, 논문의 스케일링 법칙이 가리키는 방향과
서빙 현실이 가리키는 방향이 다를 수 있음을 보여주기 때문이다.

| | fine-grained | coarse |
|---|---|---|
| 품질 (같은 활성 파라미터 기준) | **좋음** | 덜 좋음 |
| GPU 효율 | 낮음 | **높음** |
| 통신 복잡도 | 높음 | **낮음** |
| decode 처리량 | 낮음 | **높음** |
| 대표 | DeepSeek, Qwen3, GLM-5, Kimi | Arcee Trinity |

### 배치 크기가 결정을 바꾼다

여기에 중요한 조건이 붙는다. **배치가 충분히 크면 fine-grained의 단점이 줄어든다.**

배치 512에 `E=256`, `k=8`이면 토큰 4096개가 256개 expert로 흩어져
expert당 평균 16개다. GEMM으로 처리할 만한 크기가 된다.

> 💡 **그래서 granularity 선택은 "어떤 배치로 서빙할 것인가"와 묶여 있다.**
> 대규모 배치 서빙(API 제공)을 전제하면 fine-grained가 유리하고,
> 소규모 배치나 저지연이 중요하면 coarse가 유리하다.
>
> 아키텍처가 서빙 시나리오를 전제하고 설계된다는 뜻이고,
> `99-landscape`에서 계열별 선택을 볼 때 염두에 둘 관점이다.

### 정리

| | |
|---|---|
| **핵심 아이디어** | expert를 얼마나 잘게 쪼갤지는 품질과 추론 처리량 사이의 트레이드오프다 |
| **장점 (fine-grained)** | · 조합의 수가 많아 표현력이 크다<br>· 같은 활성 파라미터로 더 나은 품질 |
| **한계 (fine-grained)** | · expert별 GEMM이 작아져 GPU 효율 저하<br>· 통신 목적지가 흩어짐<br>· **작은 배치에서 특히 불리** — GEMM이 GEMV로 전락<br>· 쏠림에 민감 |
| **대표 모델** | fine-grained: DeepSeek 계열, Qwen3, GLM-5, Kimi<br>coarse: **Arcee Trinity Large**(추론 처리량 우선) |
| **다음으로** | 잘게 쪼개면 쏠림이 더 문제가 된다. 어떻게 막나 → **load balancing** |

---

## 4.5 load balancing — 쏠림을 어떻게 막나

### 문제

라우터를 그냥 학습시키면 **특정 expert에만 토큰이 몰린다.**
초기에 우연히 조금 나은 expert가 더 많이 선택되고, 더 많이 학습되어 더 나아지고,
그래서 더 선택되는 되먹임이 생긴다.

결과는 셋 다 나쁘다.

- 인기 expert는 **과부하** — 용량 제한에 걸려 토큰이 버려진다(drop)
- 나머지는 **학습이 안 된다** — 죽은 expert가 파라미터만 차지
- EP 환경에서는 **특정 GPU만 바쁘고 나머지는 논다**

세 번째가 시스템 관점에서 결정적이다. all-to-all은 **모두가 끝나야 다음으로 간다.**
하나가 느리면 전부 기다린다.

### 해법의 계보

**auxiliary loss** — 불균형에 페널티를 주는 손실 항을 추가한다.
간단하고 효과가 있지만 근본적인 문제가 있다. **주 목적(언어 모델링)과 충돌한다.**
균형을 맞추려고 라우터가 최선이 아닌 expert를 고르게 되고, 품질이 깎인다.
**균형을 품질로 사는 셈이다.**

**expert choice routing** — 방향을 뒤집는다. 토큰이 expert를 고르는 대신
expert가 토큰을 고른다. 균형이 구조적으로 보장되지만,
causal 생성에서는 미래 토큰을 봐야 해서 그대로 쓰기 어렵다.

**aux-loss-free** (DeepSeek-V3) — 손실을 건드리지 않는다.
라우터 점수에 expert별 **bias**를 더해두고, 학습 중 그 bias를 규칙으로 조정한다.

```
 점수 = 라우터 출력 + bias_i

 expert i가 과부하 →  bias_i 를 조금 낮춘다
 expert i가 한산   →  bias_i 를 조금 올린다
```

> 💡 **핵심은 이 조정이 gradient를 타지 않는다는 것이다.**
> bias는 학습되는 파라미터가 아니라 규칙으로 갱신되는 값이라,
> **주 손실을 전혀 오염시키지 않는다.** aux loss가 치르던 품질 대가가 사라진다.

단순한 아이디어인데 효과가 좋아서 빠르게 표준이 되었다.

**Quantile Balancing** (Kimi K3) — aux-loss-free의 남은 문제를 겨냥한다.

bias를 "과부하면 낮추고 한산하면 올린다"는 **휴리스틱으로 갱신**하다 보니,
얼마나 올리고 내릴지가 **민감한 하이퍼파라미터**로 남아 있었다.

✅ K3는 bias를 조정하는 대신 **라우터 점수의 분위수에서 직접 유도**한다.
목표 부하에 해당하는 분위수를 배치별 히스토그램으로 추정해 그 값을 bias로 쓴다.

```
 aux loss          : 손실에 페널티 → 품질을 대가로 지불
 aux-loss-free     : bias를 규칙으로 조정 → 조정 속도가 하이퍼파라미터로 남음
 Quantile Balancing: bias를 분위수에서 유도 → 하이퍼파라미터가 사라짐
```

> 💡 계보가 **"균형을 맞추는 대가를 하나씩 없애는" 방향**으로 진행됐다.
> 품질 → 하이퍼파라미터 → (없음). 896개 expert를 굴리는 규모에서
> 튜닝할 손잡이 하나를 없애는 게 왜 중요한지는 짐작이 간다.

### 추론 시점의 쏠림

학습만의 문제가 아니다. 서빙에서도 들어오는 요청의 성격에 따라 특정 expert에
토큰이 몰릴 수 있다. 코드 요청이 많은 시간대와 일반 대화가 많은 시간대의
라우팅 분포가 다를 수 있다.

대응은 시스템 쪽이다 — 인기 expert를 여러 GPU에 복제하거나,
expert 배치를 부하에 맞춰 재조정한다. `4.6`과 `09-serving`에서 다룬다.

### 정리

| | |
|---|---|
| **핵심 아이디어** | 라우터 점수에 expert별 bias를 더하고, 그 bias를 부하에 따라 규칙으로 조정해 균형을 맞춘다 |
| **장점** | · **주 손실을 오염시키지 않는다** — aux loss가 치르던 품질 대가가 없음<br>· 구현이 단순하고 하이퍼파라미터가 적다<br>· EP 환경의 GPU 부하 불균형을 직접 완화 |
| **한계** | · 학습 중 조정이라 **추론 시점의 분포 변화에는 대응하지 못한다**<br>· bias 조정 속도가 하이퍼파라미터로 남음<br>· 완벽한 균형을 보장하지는 않음 |
| **대표 모델** | **DeepSeek-V3 이후** — 이후 다수 모델이 채택 |
| **다음으로** | 균형을 맞춰야 하는 진짜 이유는 시스템에 있었다 → **Expert Parallelism** |

---

## 4.6 Expert Parallelism — 새로 생긴 통신 압력

### 왜 필요한가

DeepSeek-V3의 파라미터는 671B다. BF16이면 1.3 TB이고, FP8이어도 671 GB다.
**한 장에 절대 안 들어간다.** expert들을 여러 GPU에 나눠 배치해야 한다.

이것이 Expert Parallelism(EP)이다.

### 무슨 일이 벌어지나

```
  GPU 0          GPU 1          GPU 2          GPU 3
 [expert       [expert        [expert        [expert
  0~63]         64~127]        128~191]       192~255]
    │              │              │              │
    └──────────────┴──── all-to-all ────┴───────┘
         ① dispatch: 토큰을 담당 GPU로 보냄
         ② 각자 계산
         ③ combine: 결과를 원래 GPU로 회수
```
> **그림 4.6** — MoE 레이어 하나마다 all-to-all이 **두 번** 일어난다.

레이어마다 두 번이다. DeepSeek-V3는 레이어가 61개이므로 토큰 하나를 만드는 데
**120번이 넘는 all-to-all**을 거친다.

### 왜 이게 병목인가

| 이유 | 설명 |
|---|---|
| **대역폭이 좁다** | NVLink는 HBM보다 훨씬 좁다. 노드를 넘어가면 더 좁아진다 |
| **최악의 통신 패턴** | all-to-all은 모든 GPU가 모든 GPU와 주고받는다 |
| **동기화 지점** | 하나가 늦으면 전부 기다린다 → `4.5`의 불균형이 여기서 아프다 |
| **`k`배로 늘어난다** | 토큰 하나가 `k`개 expert로 가므로 통신량이 `k`배 |

> 💡 **`00-foundations` `0.3`에서 통신을 네 번째 자원으로 넣은 이유가 여기다.**
> MoE 모델에서는 HBM 대역폭이 아니라 **GPU 간 대역폭이 진짜 병목**이 되는 구간이 있다.

### 얼마나 차지하나 — 실측 범위

📎 여러 연구에서 보고된 값들이다. **측정 환경(모델, EP 정도, 배치, 네트워크)이 제각각이라
단일 수치로 인용하면 안 된다.** 범위와 경향만 받아들이는 게 맞다 (`CONTESTED.md` C6의 교훈).

| 조건 | all-to-all이 차지하는 비중 |
|---|---|
| **EP=2** | 약 30% |
| **EP=6** | 약 77% |
| 배치를 키울수록 | 최대 **79%** 까지 보고됨 |
| 대규모 클러스터 | 47~68% |
| **노드 안 (NVLink 범위)** | **~20%** |
| **노드를 넘어가면** | **40~60%** |

두 가지가 분명하다.

- **EP를 키울수록 통신 비중이 급격히 커진다.** 30%에서 77%는 두 배 이상이다.
- **노드 경계가 결정적이다.** 같은 작업인데 노드 안이면 20%, 넘어가면 40~60%다.

> 💡 **`4.4`의 granularity 논쟁과 `9.6`의 병렬화 배치가 왜 중요한지가 이 표에 있다.**
> "통신이 병목이 될 수 있다"가 아니라 **"흔히 절반을 넘는다"** 는 것이다.
> 그래서 통신이 잦은 TP는 노드 안에, EP는 노드 간에 두는 배치가 정석이 됐고,
> `4.6` 뒤에서 볼 LatentMoE 같은 접근이 나왔다.

### 대응

| 방법 | 무엇 |
|---|---|
| 전용 통신 라이브러리 | DeepEP 같은 MoE 전용 all-to-all 구현 |
| **계산·통신 중첩** | dispatch 중에 앞 레이어 계산을 진행 |
| expert 배치 최적화 | 인기 expert를 복제하거나 노드 안에 모아 배치 |
| 라우팅 제약 | 노드 간 이동을 제한 (토큰이 갈 수 있는 노드 수 상한) |
| **하드웨어** | 아래 참조 |

📎 **[T3]** NVLink 6가 per-GPU 3.6 TB/s로 이전 세대의 두 배가 되었고,
자료들은 이를 **MoE expert 라우팅의 all-to-all 패턴을 겨냥한 것**으로 설명한다.
공식 스펙 대조 전이다 (`CONTESTED.md` C4 관련).

**아키텍처가 하드웨어 로드맵을 끌어당긴 사례**로 볼 수 있다는 점에서 흥미롭다.

### LatentMoE — 통신 압력을 정면으로 겨냥한 답

발상은 `01-attention` 1.4의 MLA와 정확히 같다. **작은 공간에서 일하고 나온다.**

```
            ┌── down-proj ──┐                    ┌── up-proj ──┐
  x (d) ────┤               ├─► [latent 공간 ℓ] ─┤             ├──► y (d)
            └───────────────┘   라우팅 + expert   └─────────────┘
                                 전부 여기서
```
> **그림 4.6b** — 토큰을 `ℓ`차원으로 눌러 내린 뒤, **라우팅과 expert 연산을 전부
> 그 안에서** 끝내고 다시 올린다.

| 모델 | `d` → `ℓ` → `d` | 비율 |
|---|---|---|
| **Nemotron 3 Super** | 4096 → **1024** → 4096 | 4배 |
| **Kimi K3** ("Stable LatentMoE") | 7168 → **3584** → 7168 | 2배 |

#### 왜 중요한가 — 줄어드는 게 둘이다

📌 [T1] expert 가중치를 읽는 양과 **all-to-all 통신 payload가 동시에 `d/ℓ`배 줄어든다.**

두 번째가 핵심이다. `4.6`에서 본 대로 MoE의 진짜 병목은 통신인데,
**LatentMoE는 주고받는 벡터 자체를 작게 만든다.** 레이어마다 all-to-all이 두 번
일어나는 건 그대로지만, 한 번에 오가는 양이 절반이나 1/4이 된다.

> 💡 **이 위키에서 통신 압력을 직접 겨냥한 첫 아키텍처 기법이다.**
> `4.4`의 granularity 조절이나 `4.6`의 배치 최적화는 통신을 "관리"하는 쪽이었다.
> LatentMoE는 통신량 자체를 줄인다.

#### 절감분을 어디에 쓰나

여기서 설계 철학이 갈린다.

📌 [T1] **Nemotron 3는 아낀 것을 되돌려 쓴다.** 비용이 `d/ℓ`배 줄었으니
**expert 수와 활성 expert 수를 `d/ℓ`배 늘린다.** 같은 비용으로 더 큰 MoE를 굴리는 것이다.

`4.4`의 granularity 논쟁을 떠올리면 재미있다. fine-grained가 좋은데 통신이 발목을 잡았다면,
**LatentMoE는 그 제약을 풀어 더 잘게 쪼갤 여지를 만든 셈**이다.

#### Kimi K3의 변형

✅ `config.json`에서 확인되는 것들이다.

| 무엇 | 값 / 필드 |
|---|---|
| latent 차원 | `routed_expert_hidden_size` = **3584** (`hidden_size` 7168의 절반) |
| up-projection 전 정규화 | `latent_moe_use_norm` = **true** (RMSNorm) |
| 활성화 함수 | `hidden_act` = **"situ"** (SiTU-GLU) |
| expert | **896개 중 16개**, shared **2개** |
| 라우터 | sigmoid, `topk_method` = `noaux_tc` |
| 균형 | **Quantile Balancing** (`4.5` 참조) |

✅ 두 가지가 더 확인됐다.

**shared expert는 latent로 내리지 않는다.** 라우팅되는 expert만 latent 공간에서 돌고,
shared expert 2개는 **full-width로 유지**한다. 통신을 타지 않으니 줄일 이유가 없다.

**"Stable"이 붙은 이유는 활성값 폭주 때문이다.** expert가 896개로 극단적으로 희소해지면
활성값이 튀기 쉬운데, K3는 **SiTU-GLU**로 이를 억제한다.

```
 SiTU-GLU(x) = [β₁·tanh(W_g x / β₁) ⊙ σ(W_g x)] ⊙ [β₂·tanh(W_u x / β₂)]
                                                   β₁ = 4, β₂ = 25
```

`tanh`가 부드러운 상한(softcap) 역할을 해서 출력을 묶어두되, 원점 근처에서는
SwiGLU와 거의 같게 동작한다. **`08-numerics` 8.5의 극단값 문제를 구조로 막는
또 하나의 사례**다 — Gated Attention이 attention sink를 없앤 것과 같은 방향이다.

### 정리

| | |
|---|---|
| **핵심 아이디어** | expert들을 여러 GPU에 나눠 배치하고, 토큰을 담당 GPU로 보냈다 받아온다 |
| **장점** | · 한 장에 안 들어가는 거대 MoE 모델을 서빙 가능하게 한다<br>· expert 수를 GPU 수에 맞춰 확장할 수 있다 |
| **한계** | · **레이어마다 all-to-all 두 번** — 61층이면 120회 이상<br>· 좁은 GPU 간 대역폭을 쓰는 최악의 통신 패턴<br>· 동기화 지점이라 **부하 불균형이 그대로 지연으로 이어진다**<br>· 노드를 넘어가면 대역폭이 급격히 나빠진다 |
| **대표 구현** | DeepEP(DeepSeek) · vLLM / SGLang의 EP 지원 |
| **다음으로** | 병렬화 전략 전반과 서빙 구조는 → **09-serving** |

---

## 이 파일의 결론

MoE는 이 위키에서 **압력 이전을 가장 선명하게 보여주는 사례**다.

```
 【해결한 것】  연산량 압력
    파라미터를 E배로 늘리면서 토큰당 계산은 k/E로 유지

 【생긴 것】
    ① 메모리 용량 압력  — 파라미터 전부를 들고 있어야 한다
    ② 통신 압력         — 레이어마다 all-to-all 두 번
    ③ 부하 균형 문제    — 쏠리면 전부가 기다린다
    ④ 배치 효율 저하    — 토큰이 흩어져 GEMM이 GEMV로
```

그리고 `4.4`가 보여주듯, **아키텍처 선택(granularity)이 서빙 조건(배치 크기)과
얽혀 있다.** 논문의 스케일링 법칙만 보고 정할 수 없는 문제다.

`01-attention`이 "무엇을 저장하고 읽을 것인가"였다면, 이 파일은
**"무엇을 계산하고, 그 대가로 무엇을 주고받을 것인가"** 였다.

---

## Sources

**T1 — 논문**
- Shazeer et al. (2017), *Outrageously Large Neural Networks* — MoE의 원형
- Lepikhin et al. (2020), *GShard*
- Fedus et al. (2021), *Switch Transformer* — `k`=1 라우팅
- Shazeer (2020), *GLU Variants Improve Transformer* — SwiGLU
- DeepSeek-AI (2024), *DeepSeekMoE*, arXiv:2401.06066 — fine-grained + shared expert
- DeepSeek-AI (2024), *DeepSeek-V3*, arXiv:2412.19437 — `E`=256/`k`=8, aux-loss-free, 671B/37B
- Zhou et al. (2022), *Mixture-of-Experts with Expert Choice Routing*
- 각 모델 HuggingFace `config.json` — `num_experts`, `num_experts_per_tok`

**T2 — 구현**
- **DeepEP** (DeepSeek) — MoE 전용 all-to-all 통신 라이브러리
- **DeepGEMM** — FP8 grouped GEMM
- vLLM / SGLang의 EP 구현 — expert 배치, 계산·통신 중첩
- NVIDIA NVLink / NVSwitch 문서 — 통신 대역폭

**T3 — 참고**
- Sebastian Raschka, 분기별 아키텍처 리뷰 — expert 수 추세, **Arcee Trinity의 coarse 선택**
- NVLink 6와 MoE all-to-all의 관계에 대한 2차 자료

**미검증 항목**
- Kimi K3의 "Quantile Balancing" 세부 — 2차 자료 기반
- V4에서 `k`가 8 → 6으로 줄어든 이유 — 논문에서 설명을 확인하지 못함
- 실제 서빙에서 all-to-all이 차지하는 시간 비중 — 실측 자료 없음
