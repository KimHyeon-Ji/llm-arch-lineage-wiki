# 09. Numerics — 값 하나에 몇 비트를 쓸 것인가

지금까지의 파일들은 **무엇을 저장하고 무엇을 계산할지**를 다뤘다.
이 파일은 다른 질문을 던진다. **그 값 하나를 몇 비트로 표현할 것인가.**

이 축이 특이한 이유가 있다. metadata와 변환 비용을 제외한 이론적 상한은 다음과 같다.

| 압력 | 효과 |
|---|---|
| 메모리 **용량** | 절반 |
| 메모리 **traffic** | 약 절반 |
| **peak 연산 처리량** | 지원 하드웨어에서 최대 두 배 |
| **통신 byte** | 약 절반 |

> 정밀도는 용량·대역폭·연산·통신에 동시에 영향을 줄 수 있다. 다만 실제 이득은
> 하드웨어와 kernel 지원에 달려 있고, scale metadata·변환 비용·정확도 손실이 따른다.

## 이 장의 발전 계보와 시스템 영향

| 단계 | 대표 형식·기법 | 해결하려는 병목 | 핵심 아이디어 | 시스템 영향 |
|---|---|---|---|---|
| 기준점 | FP32 | 수치 안정성과 범용성 | 모든 값을 32-bit float로 표현 | memory·traffic·compute 비용 최대 |
| 혼합 정밀도 | **FP16 · BF16** | 학습 throughput과 activation memory | 낮은 precision 연산 + 필요한 곳만 FP32 누적 | tensor core 활용, memory 절반, loss scaling·범위 관리 |
| 8-bit | **FP8** | 대규모 학습·서빙 비용 | block/tensor scale로 좁은 표현 범위 보완 | weight·activation traffic 감소, throughput 증가, scale 연산 추가 |
| 4-bit | **MXFP4 · NVFP4** | weight capacity·bandwidth | 작은 block마다 scale을 둬 FP4 오차 제한 | weight memory 대폭 감소, 전용 hardware·kernel 의존 |
| KV 정밀도 | **KV8 · KV4** | context와 함께 증가하는 cache | weight가 아니라 KV를 별도 양자화 | 긴 context의 serving capacity 증가, attention accuracy trade-off |
| outlier 대응 | SmoothQuant · rotation · gated attention | 낮은 bit에서 극단값이 범위를 지배 | scale 이동·회전 또는 구조적으로 outlier 억제 | quantization overhead와 품질 사이 균형, architecture와 numerics 결합 |

> **이 장의 병목 이동:** bit를 줄이면 용량·대역폭·통신·compute가 함께 좋아지지만,
> 병목은 **수치 범위·scale metadata·전용 kernel 지원**으로 이동한다. 특히 긴 context에서는
> 고정 weight보다 계속 자라는 KV의 precision이 serving 수용량을 더 직접적으로 결정한다.

---

## 계보 지도

```
        FP32 (원래)
          │
      FP16 / BF16 (2018~)
      학습·추론 표준
          │
    ┌─────┴──────┐
    │            │
 [학습까지]   [추론만]
    │            │
 FP8 학습     INT8 / INT4 weight-only
 (DeepSeek-V3)    │
    │            │
    └─────┬──────┘
          │
      FP4 시대 (2025~)
    ┌─────┴─────┐
  MXFP4       NVFP4
  32개 블록    16개 블록
  power-of-2   FP8 스케일
    │            │
    └─────┬──────┘
          │
    [KV cache로 확장]
      FP8 KV → FP4 KV
          │
    [attention 내부까지]
      attention quantization (2026)
```
> **그림 9.0** — 축9의 계보

**목차**

| | 절 | 한 줄 |
|---|---|---|
| [9.1](#91-무엇을-양자화할-것인가) | **무엇을 양자화하나** | W / A / KV 구분 |
| [9.2](#92-fp8--학습까지-내려가다) | **FP8** | 학습까지 내려가다 |
| [9.3](#93-fp4--mxfp4와-nvfp4) | **MXFP4 · NVFP4** | 4비트, 그리고 스케일의 차이 |
| [9.4](#94-kv-cache-양자화) | **KV 양자화** | 컨텍스트에 비례하는 것부터 |
| [9.5](#95-outlier--왜-어려운가) | **outlier** | 왜 어려운가 ★ |

---

## 9.1 무엇을 양자화할 것인가

양자화 이야기를 할 때 먼저 구분해야 할 것이 셋 있다.

| 대상 | 표기 | 크기의 성질 | 줄이면 |
|---|---|---|---|
| **가중치** (Weight) | `W` | 모델 크기에 비례, **고정** | 용량 ↓, decode 대역폭 ↓ |
| **활성값** (Activation) | `A` | 배치 × 토큰에 비례 | 연산 처리량 ↑ |
| **KV cache** | `KV` | **배치 × 컨텍스트 × 층에 비례** | 용량 ↓, 대역폭 ↓ |

그래서 설정을 `W4A16`, `W4A4KV4` 같은 표기로 쓴다.
"가중치 4비트, 활성 16비트" 또는 "셋 다 4비트"라는 뜻이다.

### 어느 것부터 줄여야 하나

상황에 따라 다르다.

| 상황 | 지배적인 것 | 먼저 줄일 것 |
|---|---|---|
| 짧은 컨텍스트, 작은 배치 | 가중치 | **W** |
| 긴 컨텍스트, 큰 배치 | **KV cache** | **KV** |
| 대규모 배치 처리량 | 연산 | **A** (연산 처리량이 오른다) |

`00-foundations` `0.5`에서 계산한 대로 배치 32·컨텍스트 32K면 KV가 가중치의 두 배를
넘는다. **긴 컨텍스트 서빙에서는 `W`보다 `KV`를 줄이는 게 효과가 크다.**

> 💡 **weight-only 양자화(`W4A16`)가 가장 먼저 대중화된 이유**는 안전해서다.
> 가중치는 학습이 끝나면 고정된 값이라 분포를 미리 다 알 수 있다.
> 활성값과 KV는 입력에 따라 달라져서 예측이 어렵다.

---

## 9.2 FP8 — 학습까지 내려가다

*8-bit Floating Point*

### 배경

추론에서 정밀도를 낮추는 건 오래된 기법이다. 학습은 달랐다.
gradient는 값의 범위가 넓고 작은 값이 중요해서, 정밀도를 낮추면 학습이 무너지기 쉽다.

BF16이 오래 표준이었던 이유가 이것이다. FP16보다 가수(mantissa)는 짧지만
**지수 범위가 FP32와 같아서** gradient의 넓은 범위를 감당한다.

### FP8 학습

DeepSeek-V3가 **대규모 모델을 FP8로 학습**한 첫 사례로 널리 인용된다.

핵심은 **어디를 FP8로 하고 어디를 남길지**를 세밀하게 나눈 것이다.

| 부분 | 정밀도 |
|---|---|
| 주요 GEMM (forward, backward) | **FP8** |
| 민감한 연산 (정규화, softmax, 최적화기 상태 등) | 높은 정밀도 유지 |
| 누적(accumulation) | 높은 정밀도 |

그리고 **블록 단위 스케일링**을 쓴다. 텐서 전체에 하나의 스케일을 쓰면
극단값 하나가 나머지를 다 망치는데, 작은 블록마다 스케일을 따로 두면 그 영향이 국소화된다.

이 "블록마다 스케일"이 `9.3`의 FP4 포맷들로 이어지는 아이디어다.

### 정리

| | |
|---|---|
| **핵심 아이디어** | 주요 GEMM을 FP8로 수행하되, 민감한 연산과 누적은 높은 정밀도로 남긴다. 블록 단위 스케일링으로 극단값 영향을 국소화 |
| **장점** | · **학습 비용을 크게 줄인다** — 대규모 모델 학습의 문턱을 낮춤<br>· 가중치가 FP8로 저장되어 추론 용량·대역폭도 절반<br>· 최신 GPU의 FP8 tensor core를 활용 |
| **한계** | · **어디를 남길지 세밀하게 조율해야 한다** — 레시피가 까다롭다<br>· 하드웨어 지원이 필요 (Hopper 이후)<br>· 학습 안정성 검증이 대규모에서만 가능 |
| **대표 모델** | **DeepSeek-V3** 및 이후 계열. 이후 다수 모델이 뒤따름 |
| **다음으로** | 4비트까지 내려가면 스케일을 어떻게 붙이느냐가 갈린다 → **MXFP4 · NVFP4** |

---

## 9.3 FP4 — MXFP4와 NVFP4

*4-bit Floating Point · Microscaling FP4 · NVIDIA FP4*

### 4비트로 무엇을 표현하나

둘 다 **E2M1** 형식이다. 4비트를 이렇게 나눈다.

```
 [부호 1비트][지수 2비트][가수 1비트]
```

표현할 수 있는 값이 16개뿐이다. 이것만으로는 아무것도 못 한다.
**그래서 스케일이 결정적이다.**

### 차이는 스케일에 있다

```
 MXFP4                          NVFP4
 ┌──────32개 값──────┐          ┌──16개 값──┐
 [값][값][값]...[값]            [값]...[값]
        ↑                            ↑
   공유 스케일 1개                공유 스케일 1개
   8비트 (2의 거듭제곱)           FP8 (E4M3)
```
> **그림 9.3** — 같은 4비트 값이지만 스케일의 크기와 형식이 다르다.

| | **MXFP4** | **NVFP4** |
|---|---|---|
| 값 형식 | E2M1 | E2M1 |
| 블록 크기 | **32개** | **16개** |
| 스케일 형식 | 8비트 지수 (2의 거듭제곱) | **FP8 (E4M3)** |
| 표준화 | OCP 개방 표준 | NVIDIA |
| 정밀도 | 낮음 | **높음** |
| 메모리 오버헤드 | 낮음 | 조금 높음 |

NVFP4가 더 정확한 이유는 둘 다다. **블록이 작아서** 극단값의 영향 범위가 좁고,
**스케일이 2의 거듭제곱이 아닌 실수**라 더 잘 맞출 수 있다.

MXFP4는 오버헤드가 낮고 개방 표준이라는 장점이 있다.

### 실제 배포 설정

| 설정 | 의미 | 성격 |
|---|---|---|
| `W4A16` | 가중치만 4비트 | 가장 안전. 널리 쓰임 |
| `W4A4` | 가중치·활성 모두 4비트 | 연산 처리량까지 확보 |
| `W4A4KV4` | KV까지 4비트 | 가장 공격적 |

⚠️ 포맷을 지원한다는 것과 대상 GPU에서 최적 kernel을 제공한다는 것은 다르다.
배포 시점의 공식 compatibility matrix를 확인해야 한다.

### 정리

| | |
|---|---|
| **핵심 아이디어** | 4비트 E2M1 값에 작은 블록 단위 공유 스케일을 붙여 표현 범위를 확보한다 |
| **장점** | · **용량·대역폭·연산·통신 전부에 동시 작용**<br>· 지원 GPU와 kernel에서는 FP4 tensor core 활용 가능 |
| **한계** | · **정확도 손실이 실재한다** — 특히 추론(reasoning) 작업에 민감<br>· MXFP4/NVFP4 중 무엇을 쓰느냐로 결과가 달라짐<br>· 하드웨어·프레임워크 지원이 아직 정리 중<br>· 양자화 인지 학습(QAT) 없이는 손실이 커질 수 있음 |
| **대표 모델** | **Kimi K3**(MXFP4-aware post-training) · **DeepSeek-V4**(부분별 FP4/FP8 배포) |

K3의 `config.json`이 MXFP4 스펙과 정확히 맞는다.

| 필드 | 값 | 위 표와 대조 |
|---|---|---|
| `format` | `mxfp4-pack-quantized` | |
| `num_bits` | 4 | E2M1 |
| **`group_size`** | **32** | ✅ MXFP4의 블록 크기 32 |

📌 K3 공식 자료는 조합을 이렇게 밝힌다 — **가중치는 MXFP4, 활성값은 MXFP8.**
`9.1`의 표기로는 `W4A8`이다. 하드웨어 호환성을 위한 선택이라고 설명한다.

| 모델 | 가중치 | 활성값 | KV |
|---|---|---|---|
| **Kimi K3** | **MXFP4** | **MXFP8** | — |
| **DeepSeek-V4** | MoE만 **FP4**, 나머지 FP8 | — | **FP8** |

**두 모델이 서로 다른 축으로 나눴다는 게 흥미롭다.**
K3는 **가중치 vs 활성값**으로, V4는 **모델 부분(MoE vs attention)** 으로 갈랐다.

📌 **DeepSeek-V4는 부분별로 나눴다** (vLLM 배포 문서).
**MoE expert 가중치는 FP4, attention·norm·router는 FP8.**
양이 압도적인 expert만 4비트로 내리고 민감한 부분은 남긴 것이다.
`9.1`의 "무엇을 양자화할 것인가"가 W/A/KV뿐 아니라 **모델 부분별로도** 갈린다는 실례다.
| **다음으로** | 긴 컨텍스트에서 가장 큰 덩어리는 가중치가 아니다 → **KV 양자화** |

---

## 9.4 KV cache 양자화

### 왜 여기가 중요한가

가중치는 **크기가 고정**이다. 70B 모델은 BF16으로 140 GB이고, 그게 전부다.

KV cache는 다르다. `B × S × L`에 비례한다. **배치와 컨텍스트를 키우면 끝없이 늘어난다.**

```
 가중치 140 GB   ← 고정
 KV     320 GB   ← 배치 32, 컨텍스트 32K 기준. 더 키우면 계속 증가
```

그래서 **긴 컨텍스트 서빙에서는 KV를 줄이는 게 가중치를 줄이는 것보다 효과가 크다.**

### 적용 원칙

| 정밀도 | 일반적인 판단 |
|---|---|
| **FP8 KV** | 메모리 절감과 품질 사이의 보수적인 시작점 |
| **FP4 KV** | 절감 폭은 크지만 calibration·scale 설계와 작업별 평가가 더 중요 |

어떤 포맷도 모든 작업에서 자동으로 안전하지는 않다. attention score와 value 누적에
오차가 들어가므로, 목표 컨텍스트 길이와 reasoning·retrieval workload에서 정확도와
지연을 함께 측정해야 한다.

### 축1과의 관계

`01-attention`의 기법들과 **곱해진다는 점**이 중요하다.

```
 GQA (헤드 1/8)  ×  FP8 KV (비트 1/2)  =  1/16
 MLA             ×  FP8 KV             =  더 작게
```

`01-attention` `1.11`의 CLA/YOCO(레이어 방향)와도 곱해진다.
**서로 직교하는 절감 수단이 셋 있는 셈이다** — 가로(헤드·차원), 세로(레이어), 정밀도(비트).

### attention quantization

2026년 들어 attention 연산 자체를 양자화하는 시도가 나오고 있다.
KV를 저정밀로 저장하는 것을 넘어, **attention 내부 계산까지** 저정밀로 수행하는 것이다.

⚠️ 아직 초기 단계이고 지원 범위가 제한적이다.

### 정리

| | |
|---|---|
| **핵심 아이디어** | KV cache를 저정밀로 저장한다. 컨텍스트에 비례해 커지는 덩어리라 효과가 크다 |
| **장점** | · **긴 컨텍스트에서 가중치보다 효과가 크다**<br>· `01-attention`의 기법들과 **곱해진다** (직교)<br>· FP8은 품질 손실이 거의 없이 절반 절감<br>· 모델 재학습 없이 적용 가능 |
| **한계** | · **FP4 KV는 작업별 검증이 필요** — 추론 작업에서 특히 위험<br>· 양자화·역양자화 오버헤드가 attention 커널에 들어감<br>· 페이지 관리(PagedAttention)와 얽혀 구현이 복잡 |
| **대표 구현** | vLLM / SGLang / TensorRT-LLM의 KV quantization 옵션 |
| **다음으로** | 그런데 왜 어떤 값은 양자화가 잘 되고 어떤 값은 안 되나 → **outlier** |

---

## 9.5 outlier — 왜 어려운가

### 문제

양자화가 어려운 근본 원인은 **극단값(outlier)** 이다.

블록 안의 값 대부분이 -1과 1 사이인데 하나가 100이라면, 스케일을 100에 맞춰야 한다.
그러면 나머지 값들은 4비트의 표현 범위 중 아주 좁은 구간에 몰려 **거의 다 0이 된다.**

값 하나가 나머지 전부를 망친다.

### 어디서 극단값이 생기나

여기서 `01-attention` `1.5`와 연결된다.

> **attention sink가 극단값의 주요 원인 중 하나다.**
>
> softmax는 확률 합이 1이라 어딘가에 반드시 배분해야 한다.
> 볼 것이 없을 때 모델은 초기 토큰에 몰아넣었고, 그 위치의 활성값이
> 비정상적으로 커졌다(massive activation).
>
> 이 값이 정확히 양자화를 망치는 극단값이다.

### 그래서 축들이 연결된다

```
 Gated Attention (01-attention 1.5)
        │
        │  softmax의 강제 배분을 게이트로 흡수
        ▼
   attention sink 제거
        │
        │  극단값이 사라진다
        ▼
   양자화가 쉬워진다 (09-numerics)
```

**품질 문제를 풀려고 만든 기법이 양자화 친화성을 부수 효과로 얻은 것**이다.
축을 나눠 정리했지만 실제로는 이렇게 얽혀 있다.

### 다른 대응 방법

| 방법 | 무엇 |
|---|---|
| **블록 스케일링** | 블록을 작게 해서 극단값의 영향 범위를 좁힌다 (`9.3`의 NVFP4) |
| **혼합 정밀도** | 극단값이 많은 층·채널만 높은 정밀도로 남긴다 |
| **회전 변환** | 값들을 회전시켜 극단값을 여러 채널에 분산시킨다 |
| **QAT** | 학습 때부터 양자화를 반영해 모델이 적응하게 한다 |
| **구조적 해결** | 극단값이 안 생기게 아키텍처를 바꾼다 (Gated Attention) |

마지막이 가장 근본적이고, 그래서 최근 아키텍처들이 그 방향으로 가고 있다.

---

## 이 파일의 정리

| 축 | 무엇을 줄이나 | 대표 설정 | 검증 초점 |
|---|---|---|---|
| **가중치** | 고정 크기 | `W8`, `W4` | layer별 outlier와 품질 |
| **활성값** | 배치 × 토큰 | `A8`, `A4` | 동적 범위와 accumulation |
| **KV cache** | **배치 × 컨텍스트 × 층** | `KV8`, `KV4` | 길이별 retrieval·reasoning 품질 |

세 가지를 기억하면 된다.

1. **정밀도는 여러 비용을 함께 낮출 수 있지만, 지원 kernel이 있어야 실효 이득이 난다.**
2. **긴 컨텍스트에서는 KV 정밀도도 중요한 설계 축이다.** 가중치는 고정이지만 KV는 계속 커진다.
3. **`01`의 가로 절감, `1.11`의 세로 절감, 여기의 비트 절감은 서로 곱해진다.**
   조합 가능성은 `99-landscape`에서 정리한다.

그리고 `9.5`가 보여주듯, 양자화의 난이도는 **아키텍처가 정한다.**
극단값이 안 생기는 구조면 저정밀이 쉽고, 그렇지 않으면 어렵다.
정밀도를 독립적인 선택지로 다룰 수 없다는 뜻이다.

---

## 이 다음

`10-serving.md`에서 지금까지의 선택을 실제 런타임에 올린다.
KV 구조·MoE·형상·decoding·정밀도가 batching, paging, parallelism과 만나면
어느 자원이 먼저 병목이 되는지 종합한다.

---

## Sources

**T1 — 논문·표준**
- Micikevicius et al. (2017), *Mixed Precision Training* — FP16 학습
- **OCP Microscaling Formats (MX) Specification** — MXFP4 정의
- NVIDIA, NVFP4 관련 문서 — 블록 16, FP8 스케일
- DeepSeek-AI (2024), *DeepSeek-V3*, arXiv:2412.19437 — FP8 학습 레시피, 블록 스케일링
- Dettmers et al. (2022), *LLM.int8()* — 극단값 문제의 초기 분석
- Xiao et al. (2023), *SmoothQuant* — 활성값 극단값 완화
- Qiu et al. (2025), *Gated Attention*, NeurIPS 2025 — attention sink 제거와 활성값 안정화

**T2 — 구현**
- SGLang *Quantized KV Cache* 문서 — FP8/FP4 KV의 실무 지침
- TensorRT-LLM Quantization 문서
- **LLM Compressor** 릴리스 노트 — attention quantization, MXFP4 실험 지원 상태
- vLLM 양자화 지원 현황

**범위와 주의**
- 프레임워크의 지원 포맷과 커널은 빠르게 바뀐다. 배포 시점의 공식 compatibility
  matrix와 대상 GPU를 확인해야 한다.
- 같은 "4비트"라도 숫자 포맷, block 크기, scale 정밀도와 accumulation 방식이 달라
  품질과 속도를 직접 비교할 수 없다.
