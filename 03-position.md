# 03. Position — 순서를 어떻게 알려줄까

attention에는 이상한 성질이 하나 있다. **순서를 모른다.**

`softmax(q Kᵀ) V`를 보면 K와 V의 순서를 뒤섞어도 결과가 그대로다. 각 토큰과의 내적을
계산해 가중합할 뿐, 그게 몇 번째인지는 식 어디에도 없다. 그대로 두면 모델은
"나는 사과를 먹었다"와 "사과는 나를 먹었다"를 구분하지 못한다.

그래서 위치 정보를 따로 주입해야 한다. 이 파일은 그 방법의 계보다.

`00-foundations` `0.4`의 분류로 보면 여기는 **비용이 아니라 품질** 쪽 압력이다.
빨라지려고 하는 게 아니라 **제대로 동작하게 하려고**, 그리고 **학습 때보다 긴 입력을
다루려고** 바뀌어 왔다.

다만 축1과 완전히 독립적이지는 않다. `3.4`에서 보겠지만 **위치 인코딩 방식이
attention 구조를 제약**하는 지점이 있다.

---

## 계보 지도

```
        Sinusoidal (2017)          Learned (2018~)
        절대 위치를 더한다          위치마다 학습된 벡터
              └───────────┬───────────┘
                          │
                    [상대 위치로]
                          │
              ┌───────────┴───────────┐
              │                       │
          ALiBi (2021)            RoPE (2021)
        점수에 거리 페널티        회전으로 상대 위치
                                      │
                          ┌───────────┼───────────┐
                          │           │           │
                  [길이를 늘린다] [일부만] [아예 뺀다]
                          │           │           │
                    PI / NTK      partial RoPE   NoPE
                     YaRN                         │
                       │                          │
                       └──────────┬───────────────┘
                                  │
                              HoPE (2024)
                        고주파는 RoPE, 저주파는 위치 무관


        [축1과의 충돌]
        MLA + RoPE → decoupled RoPE (2024)
```
> **그림 3.0** — 축3의 계보

**목차**

| | 절 | 한 줄 |
|---|---|---|
| [3.1](#31-절대-위치의-시대) | **Sinusoidal · Learned** | 위치 벡터를 더한다 |
| [3.2](#32-rope--더하지-말고-회전시키자) | **RoPE** | 회전으로 상대 위치를 만든다 |
| [3.3](#33-컨텍스트-확장--pi-ntk-yarn) | **PI · NTK · YaRN** | 학습보다 긴 입력을 다루기 |
| [3.4](#34-decoupled-rope--mla가-부딪힌-벽) | **decoupled RoPE** | 축1×축3 교차점 |
| [3.5](#35-nope--아예-빼면) | **NoPE** | 빼도 된다? |
| [3.6](#36-hope--장기-감쇠를-버리다) | **HoPE** | 저주파를 포기한다 |

---

## 3.1 절대 위치의 시대

### 왜 나왔나

attention이 순서를 모르니 알려줘야 한다. 가장 단순한 방법은 **위치마다 다른 벡터를
만들어 토큰 임베딩에 더하는 것**이다.

### 두 가지 방식

**Sinusoidal** (Transformer 원 논문)은 여러 주파수의 사인·코사인 값을 위치 벡터로 쓴다.
학습이 필요 없고, 논문은 학습보다 긴 길이에도 외삽이 가능할 것이라 기대했다.
실제로는 잘 되지 않았다.

**Learned** (GPT-2, GPT-3, BERT)는 위치마다 벡터를 하나씩 두고 학습한다.
단순하고 성능이 좋지만 명백한 한계가 있다. **학습 길이를 넘어가면 벡터 자체가 없다.**
1024까지 학습했으면 1025번째 토큰에 줄 것이 없다.

### 공통된 한계

둘 다 **절대 위치**를 넣는다. "너는 5번째다"라고 알려주는 것이다.

그런데 언어에서 중요한 건 대개 **상대적 거리**다. "이 형용사가 바로 뒤 명사를 꾸민다"는
관계는 그게 문서의 5번째든 5천 번째든 똑같다. 절대 위치를 더하면 모델이 그 불변성을
스스로 학습해야 하고, 학습 범위를 벗어나면 무너진다.

### 정리

| | |
|---|---|
| **핵심 아이디어** | 위치마다 다른 벡터를 만들어 토큰 임베딩에 더한다 |
| **장점** | · 구현이 단순하다 — 입력단에서 한 번 더하면 끝<br>· attention 구조를 전혀 건드리지 않는다 |
| **한계** | · **절대 위치**라 상대 관계의 불변성을 모델이 따로 배워야 함<br>· Learned는 **학습 길이를 넘으면 아예 값이 없다**<br>· Sinusoidal의 외삽도 실제로는 잘 동작하지 않았다 |
| **대표 모델** | GPT-2, GPT-3, BERT, 초기 Transformer 전반 |
| **다음으로** | 절대 위치를 넣되 **결과가 상대 위치에만 의존**하게 만들 수 있다면 → **RoPE** |

---

## 3.2 RoPE — 더하지 말고 회전시키자

### 왜 나왔나

상대 위치를 직접 다루고 싶은데, 모든 토큰 쌍의 거리를 따로 계산하면 비용이 크다.
**절대 위치를 넣었는데 상대 위치가 튀어나오는** 방법이 있으면 좋겠다.

### 아이디어와 구조

RoPE의 답은 회전이다. `q`와 `k`를 2차원씩 짝지어, **위치에 비례하는 각도로 회전**시킨다.

```
        위치 m의 쿼리              위치 n의 키
           q                          k
           │                          │
        R_m 만큼 회전              R_n 만큼 회전
           ↓                          ↓
         R_m q                      R_n k
           └──────── 내적 ────────────┘
                     ↓
            ⟨R_m q, R_n k⟩ = ⟨q, R_{n−m} k⟩
                                   └─ 위치 차이만 남는다
```
> **그림 3.2** — 회전 행렬의 성질 때문에 두 회전의 내적이 **각도 차이**에만 의존한다.

이것이 RoPE의 전부다. 각각에는 절대 위치(`m`, `n`)를 넣었는데, 내적을 하면
**차이(`n−m`)만 남는다.** 상대 위치를 별도로 계산하지 않고 얻는다.

### 주파수 — 차원마다 회전 속도가 다르다

모든 차원 쌍이 같은 속도로 돌면 정보가 부족하다. RoPE는 차원마다 다른 각속도를 준다.

```
 θ_i = base^(−2i/d)        base는 보통 10000
```

| 차원 | 회전 속도 | 무엇을 구분하나 |
|---|---|---|
| 낮은 차원 | **빠름** (고주파) | 가까운 거리. 한두 칸 차이를 예민하게 |
| 높은 차원 | **느림** (저주파) | 먼 거리. 수천 칸 단위의 대략적 위치 |

이 주파수 구조가 `3.3`과 `3.6` 전체의 배경이 된다.

### 장기 감쇠 — 나중에 문제가 되는 특성

RoPE에는 부수 효과가 있다. 여러 주파수의 회전이 겹치면서, **멀리 있는 토큰과의 내적이
평균적으로 작아진다.** 원 논문은 이를 장점으로 소개했다 — 가까운 것에 더 주목하는 게
언어의 성질에 맞으니까.

긴 컨텍스트 시대에 이 판단이 뒤집힌다. `3.6`에서 다룬다.

### partial RoPE

전 차원이 아니라 **일부 차원에만** RoPE를 적용하고 나머지는 위치 정보 없이 두는 변형이다.
Qwen3-Next 등이 쓴다. 저주파 성분이 일으키는 문제(`3.6`)를 피하면서,
위치에 얽매이지 않는 채널을 남겨 전역 정보 전달에 쓰려는 의도다.

`3.5`의 NoPE와 `3.6`의 HoPE는 이 발상을 각각 극단으로 밀어붙인 것이라고 볼 수 있다.

### 코드와 텐서

| # | 연산 | 식 | shape 변화 | 종류 |
|---|---|---|---|---|
| 1 | 각도 테이블 | `cos(mθ)`, `sin(mθ)` | `(S, d_h/2)` | 미리 계산해 캐시 |
| 2 | **회전 적용** | 2차원씩 짝지어 회전 | `(B,1,n_h,d_h)` **유지** | **elementwise** |

`q`와 `k`에 각각 적용하고, `k`는 회전된 상태로 캐시에 저장한다.

> 💡 **RoPE가 널리 쓰인 실무적 이유가 여기 있다.**
> shape가 안 바뀌고, 연산이 elementwise 하나뿐이고, **회전한 `k`를 그대로 캐시에
> 넣으면 끝난다.** 나중에 꺼내 쓸 때 위치를 다시 고려할 필요가 없다.
> KV cache와 궁합이 좋다는 것이 채택을 갈랐다.

✅ 검증: `⟨R_m q, R_n k⟩`가 `m − n`에만 의존하는지 확인한다.
`snippets/test_equivalence.py::test_rope_relative`

### 정리

| | |
|---|---|
| **핵심 아이디어** | `q`, `k`를 위치에 비례한 각도로 회전시키면 내적이 위치 차이에만 의존한다 |
| **장점** | · **절대 위치를 넣어 상대 위치를 얻는다** — 추가 파라미터가 없다<br>· 회전된 `k`를 그대로 캐시하면 되어 **KV cache와 궁합이 좋다**<br>· 연산이 elementwise 하나뿐<br>· 주파수 대역이 나뉘어 있어 확장 기법을 붙이기 쉽다 |
| **한계** | · **학습 길이를 벗어나면 무너진다** — 못 본 각도 범위로 감<br>· **장기 감쇠** — 먼 거리 정보를 억누른다<br>· 저주파 성분은 학습 범위 안에서 한 바퀴도 못 돌아 정보로서 불완전<br>· **위치별 회전이라 행렬을 미리 곱해둘 수 없다** → MLA와 충돌 (`3.4`) |
| **대표 모델** | Llama 전 라인 · Qwen · Mistral · Gemma · DeepSeek · **2021년 이후 사실상 표준** |
| **다음으로** | 학습보다 긴 입력을 넣으려면 각도를 손봐야 한다 → **PI · NTK · YaRN** |

---

## 3.3 컨텍스트 확장 — PI, NTK, YaRN

### 문제

4K로 학습한 모델에 32K를 넣으면 무슨 일이 벌어지나. RoPE 각도가 학습 때 한 번도 본 적
없는 범위로 들어간다. 모델은 그 영역에서 어떻게 행동해야 할지 모르고, 성능이 급격히
무너진다.

다시 학습시키는 건 비싸다. **각도를 손보는 것만으로** 늘릴 수 없을까.

### 세 가지 접근

**Position Interpolation (PI)** — 위치를 눌러 넣는다.
32K를 쓰고 싶으면 위치 `m`을 `m/8`로 바꿔서 4K 범위 안에 들어오게 한다.
학습된 각도 범위를 벗어나지 않으니 안전하다. 대신 가까운 토큰들이 촘촘히 붙어
**지역 해상도가 떨어진다.**

**NTK-aware** — 고주파는 그대로, 저주파만 늘린다.
가까운 거리 구분은 고주파가 담당하니 건드리지 않고, 먼 거리를 담당하는 저주파만
늘린다. `base`를 키우는 방식으로 구현된다. PI의 해상도 손실을 완화한다.

**YaRN** — 주파수 대역별로 다르게 처리한다.

| 대역 | 학습 범위 안에서 | 처리 |
|---|---|---|
| **고주파** | 여러 바퀴 회전 | **그대로 둔다** — 상대 위치 정보가 이미 충분 |
| **저주파** | 한 바퀴도 못 돎 | **보간한다** — 외삽하면 미지의 영역 |
| 중간 | 애매 | 부드럽게 섞는다 |

여기에 attention 온도 보정을 더한다. 컨텍스트가 길어지면 softmax 분포가 퍼지는데,
그걸 스케일로 되돌린다.

### 여기서 얻은 통찰

> 💡 **주파수마다 성질이 다르므로 다르게 다뤄야 한다.**
> 고주파는 학습 범위 안에서 충분히 회전했으니 믿을 수 있고,
> 저주파는 반 바퀴도 못 돌았으니 그 값이 진짜 "위치 정보"인지 의심스럽다.
>
> 이 관찰을 끝까지 밀면 **"저주파는 아예 안 쓰는 게 낫지 않나"** 가 되는데,
> 그게 `3.6`의 HoPE다.

### 추론에서 달라진 것

| | 무엇이 바뀌나 |
|---|---|
| 모델 가중치 | **안 바뀐다** (재학습 없이 적용 가능) |
| 연산 | 각도 테이블만 다시 만들면 끝 |
| KV cache | 변화 없음 |
| 비용 | **사실상 0** |

**공짜에 가깝다는 것이 이 계열의 핵심 매력이다.** 다만 소량의 추가 학습(fine-tuning)을
곁들이면 품질이 확실히 나아져서, 실제로는 그렇게 쓰는 경우가 많다.

### 정리

| | |
|---|---|
| **핵심 아이디어** | RoPE의 각도 계산을 조정해 학습 길이보다 긴 컨텍스트를 다룬다 |
| **장점** | · **재학습 없이 적용 가능** — 각도 테이블만 교체<br>· 추론 비용이 늘지 않는다<br>· YaRN은 주파수 대역별 처리로 손실을 최소화 |
| **한계** | · 근본 해결이 아니라 **늘려 쓰는 것** — 배율이 커지면 품질이 떨어짐<br>· PI는 지역 해상도를 희생<br>· 소량 fine-tuning을 곁들이지 않으면 성능이 온전치 않음 |
| **대표 모델** | Qwen3 (YaRN으로 131K 지원) · Llama 계열의 긴 컨텍스트 변형 · Code Llama(NTK) 등 광범위 |
| **다음으로** | 여기서 잠깐 옆길로 샌다. RoPE에는 확장 말고 **다른 종류의 문제**가 하나 더 있었다 → **decoupled RoPE** |

---

## 3.4 decoupled RoPE — MLA가 부딪힌 벽

이 절은 축1과 축3의 교차점이다. `01-attention`의 `1.4`를 읽었다면 이어진다.

### 문제

MLA의 핵심은 **흡수**였다. `K = c · W^UK`이므로

```
 q · Kᵀ = q · (c W^UK)ᵀ = (q W^UKᵀ) · cᵀ
```

`W^UK`를 쿼리 쪽으로 옮겨 미리 곱해두면, latent `c`를 K로 복원하지 않고 바로 계산할 수 있다.

그런데 RoPE가 끼면 이 식이 깨진다.

```
 (R_m q) · (R_n k)ᵀ = R_m q · (R_n c W^UK)ᵀ
                              └─ R_n이 위치마다 다르다
```

`R_n`은 **토큰 위치 `n`마다 값이 달라진다.** 학습된 고정 가중치가 아니므로
`W^UK`와 미리 곱해둘 수 없다. 흡수가 불가능해지고, MLA는 이점을 잃는다.

> 💡 **위치 인코딩 방식이 attention 구조를 제약한 사례다.**
> 축들이 독립적이지 않다는 걸 보여주는 가장 선명한 지점이다.

### 해법 — 차원을 쪼갠다

DeepSeek의 답은 **역할을 분리하는 것**이었다.

```
              ┌─ nope 부분 (d_c = 512) ─ 위치 정보 없음 ─ 흡수 경로 ─┐
   헤드 차원 ─┤                                                      ├─ 점수 합산
              └─ rope 부분 (d_r = 64) ── RoPE 적용 ── 그냥 캐시 ─────┘
                                          모든 헤드가 공유
```
> **그림 3.4** — decoupled RoPE. 흡수가 가능한 차원과 위치 정보를 담는 차원을 분리한다.

- **nope 부분**은 위치 정보 없이 흡수 경로를 탄다. MLA의 이점이 그대로 살아난다.
- **rope 부분**은 작게(64차원) 떼어내 RoPE를 적용하고 캐시에 그냥 저장한다.
  **모든 헤드가 공유**하므로 MQA처럼 하나만 저장하면 된다.

최종 attention 점수는 두 부분을 각각 계산해 더한다.

### 대가

| | |
|---|---|
| 캐시가 늘어난다 | `d_r` = 64가 추가로 저장된다 (512 → 576) |
| 위치 정보가 제한된다 | 전체 차원이 아니라 64차원만 위치를 담는다 |
| 점수 계산이 둘로 쪼개진다 | `01`의 `1.4` 연산 표에서 5번과 6번 |

64차원만으로 충분한가는 흥미로운 질문이다. 실제로 잘 동작하는데,
`3.2`의 partial RoPE가 별도로 잘 동작한다는 사실과 통한다.
**위치 정보에 모든 차원이 필요하지는 않다.**

### 정리

| | |
|---|---|
| **핵심 아이디어** | 흡수 가능한 차원(위치 없음)과 RoPE를 적용할 차원(헤드 공유)을 분리한다 |
| **장점** | · MLA의 흡수 이점을 **유지하면서** 위치 정보를 넣을 수 있다<br>· rope 부분이 헤드 공유라 추가 캐시가 작다 (64차원)<br>· partial RoPE와 같은 방향 — 일부 차원만으로 충분하다는 관찰 |
| **한계** | · 캐시가 조금 늘어난다 (512 → 576)<br>· 점수 계산이 두 갈래로 나뉘어 커널이 복잡해짐<br>· MLA 전용 해법이라 범용성이 없다 |
| **대표 모델** | DeepSeek-V2 / V3 / R1 · MLA를 쓰는 모든 모델 |
| **다음으로** | 위치 정보를 줄여도 된다면, **아예 빼면** 어떻게 될까 → **NoPE** |

---

## 3.5 NoPE — 아예 빼면?

### 왜 나왔나

`3.4`에서 64차원만으로 충분했고, partial RoPE도 잘 동작한다. 그러면 자연스러운 질문이 나온다.
**위치 인코딩이 정말 필요한가?**

### 아이디어

decoder-only 모델에는 이미 위치 정보가 하나 숨어 있다. **causal mask**다.

```
 t=0 이 볼 수 있는 것: 1개
 t=1 이 볼 수 있는 것: 2개
 t=2 이 볼 수 있는 것: 3개
   ...
```

각 위치마다 **볼 수 있는 토큰의 개수가 다르다.** 모델은 이 차이로 자기 위치를
추론할 수 있다. 별도 인코딩 없이도 순서를 아는 것이다.

실제로 동작한다. 그리고 **외삽이 오히려 더 잘 되는 경향**이 관찰됐다.
학습 때 못 본 각도 같은 게 애초에 없으니 당연하다.

### 실전에서는 일부 층에만

전 층을 NoPE로 하지는 않는다. 보통 **하이브리드 구성에서 특정 층에만** 쓴다.

| 모델 | 어디에 NoPE를 |
|---|---|
| SmolLM3 | 일부 층 |
| **Kimi Linear** | **full attention 층** (KDA 층은 다른 방식) |
| Arcee Trinity Large | global attention 층 |
| LongCat-Flash-Lite | 일부 층 |

패턴이 보인다. **값싼 지역 층(SWA, linear)이 가까운 위치를 처리하고,
비싼 전역 층은 위치에 얽매이지 않고 검색만 한다.**

> 💡 `02-linear-attention` `2.5`의 하이브리드와 정확히 맞물린다.
> KDA 층이 순차 처리라 위치 정보를 자연히 담으니, full 층은 RoPE의 장기 감쇠에
> 방해받지 않고 먼 곳을 볼 수 있다. **층마다 다른 위치 전략**이라는 설계다.

### 정리

| | |
|---|---|
| **핵심 아이디어** | 위치 인코딩을 넣지 않고 causal mask가 만드는 비대칭에 맡긴다 |
| **장점** | · **외삽이 잘 된다** — 학습 범위 밖 각도라는 개념이 없음<br>· 연산과 파라미터가 아예 없다<br>· 하이브리드에서 전역 층에 쓰면 장기 감쇠 문제를 피할 수 있음 |
| **한계** | · **모든 층에 쓰면 성능이 떨어진다** — 위치 정보가 간접적이라 정밀도가 낮음<br>· 어느 층에 적용할지가 설계 변수로 추가됨<br>· 짧은 컨텍스트에서는 RoPE 대비 이점이 없다 |
| **대표 모델** | SmolLM3 · **Kimi Linear**(full 층) · Arcee Trinity Large(global 층) · Sarvam 105B · LongCat-Flash-Lite |
| **다음으로** | RoPE와 NoPE 사이 어딘가 — 주파수별로 나눠 쓰면 → **HoPE** |

---

## 3.6 HoPE — 장기 감쇠를 버리다

> ⚠️ **HoPE라는 이름의 논문이 최소 세 개다.** 서로 다른 연구이므로 혼동하면 안 된다.
>
> | arXiv | 제목 | 분야 |
> |---|---|---|
> | **2410.21216** | *HoPE: A Novel Positional Encoding Without Long-Term Decay…* | **← 이 절에서 다루는 것** |
> | 2505.20444 | *HoPE: Hybrid of Position Embedding for Long Context VLM* | 비전-언어 모델 |
> | 2509.05218 | *HoPE: Hyperbolic Rotary Positional Encoding* | 쌍곡 공간 |
>
> → `CONTESTED.md` C2

### 왜 나왔나

`3.2`에서 RoPE의 장기 감쇠를 "논문이 장점으로 소개했다"고 했다. 가까운 것에 더 주목하는
것이 언어의 성질에 맞다는 논리였다.

긴 컨텍스트 시대에 이 판단을 다시 볼 필요가 생겼다. 10만 토큰 문서에서 답이 앞쪽에 있다면,
**멀다는 이유로 억눌리는 것은 손해다.**

`3.3`의 YaRN에서 나온 관찰도 있다. **저주파 성분은 학습 범위 안에서 한 바퀴도 회전하지
못한다.** 그렇다면 그 값이 정말 "상대 위치 정보"로 기능하고 있는지 의심스럽다.
회전이 반 바퀴도 안 돌았다면 위치에 따라 값이 단조롭게 변할 뿐이고,
그건 상대 위치라기보다 **절대 위치 신호에 가깝다.**

### 아이디어

HoPE의 답은 단순하다. **저주파 성분을 위치 무관 성분으로 갈아끼운다.**

```
 RoPE:   [고주파 회전][중주파 회전][저주파 회전]
                                    ↓
 HoPE:   [고주파 회전][중주파 회전][위치 무관 (고정)]
                                    └─ NoPE와 같은 취급
```
> **그림 3.6** — 고주파는 RoPE, 저주파 자리는 위치 정보를 빼고 고정 성분으로

효과는 두 가지다.

- **장기 감쇠가 사라진다.** 감쇠를 만들던 저주파 성분이 빠졌으므로.
- **외삽이 안정된다.** 학습 범위를 벗어나면 위험했던 부분이 애초에 없다.

### 계보상 위치

```
 RoPE  ──┐
         ├──►  HoPE   =  고주파는 RoPE, 저주파 자리는 NoPE
 NoPE  ──┘
```

`3.2`의 partial RoPE, `3.4`의 decoupled RoPE와 같은 계열이다.
**모두 "전 차원에 위치를 넣지 않는다"는 아이디어를 공유한다.**
다른 점은 나누는 기준이다.

| 방법 | 나누는 기준 |
|---|---|
| partial RoPE | 차원 개수 (앞쪽 몇 개) |
| decoupled RoPE | 역할 (흡수 가능 / 위치 담당) |
| **HoPE** | **주파수** (회전이 충분한지) |

📌 [T1] 3B 규모 Llama 기반 실험에서 여러 벤치마크 평균이 RoPE 대비 개선되었고,
컨텍스트 길이 변화에 대한 적응도 나았다고 보고한다.

### 정리

| | |
|---|---|
| **핵심 아이디어** | RoPE의 저주파 성분을 위치 무관 성분으로 교체하고 고주파만 남겨 장기 감쇠를 없앤다 |
| **장점** | · **장기 감쇠 제거** — 먼 곳의 정보를 억누르지 않음<br>· 외삽이 안정적 (학습 범위 밖 각도가 없음)<br>· `3.3`에서 관찰된 "저주파는 신뢰하기 어렵다"를 구조로 반영 |
| **한계** | · **동명이인이 셋** — 자료 찾을 때 혼동 주의 (C2)<br>· 대규모 프론티어 모델의 채택 사례가 아직 제한적<br>· 저주파를 완전히 버리는 것이 항상 옳은지는 검증 진행 중 |
| **대표 모델** | 연구 단계. 대규모 채택 사례는 확인되지 않음 ⚠️ |
| **다음으로** | 여기까지가 attention 안쪽이다. 다음은 블록의 나머지 절반 → **04-moe** |

---

## 전체 비교

| 방법 | 위치를 넣는 곳 | 상대/절대 | 학습 밖 외삽 | 대표 모델 |
|---|---|---|---|---|
| **Sinusoidal** | 입력 임베딩에 덧셈 | 절대 | 이론상 가능, 실제 약함 | 원조 Transformer |
| **Learned** | 입력 임베딩에 덧셈 | 절대 | **불가** | GPT-2/3, BERT |
| **ALiBi** | attention 점수에 페널티 | 상대 | 비교적 양호 | BLOOM 등 |
| **RoPE** | q·k 회전 | **상대** | 확장 기법 필요 | **사실상 표준** |
| **partial RoPE** | 일부 차원만 회전 | 상대 | 개선 | Qwen3-Next |
| **PI/NTK/YaRN** | RoPE 각도 조정 | 상대 | **가능** (재학습 없이) | Qwen3, Llama 확장판 |
| **decoupled RoPE** | 전용 차원만 회전 | 상대 | RoPE와 동일 | DeepSeek 전 라인 |
| **NoPE** | 안 넣음 | — | **양호** | Kimi Linear(full 층), SmolLM3 |
| **HoPE** | 고주파만 회전 | 상대 | **양호** | 연구 단계 |

흐름을 정리하면 이렇다.

1. **절대에서 상대로.** 언어의 관계는 대개 거리에 의존하지 위치에 의존하지 않는다.
2. **RoPE가 그 답을 우아하게 냈다.** 절대를 넣어 상대를 얻고, KV cache와도 잘 맞았다.
3. **컨텍스트가 길어지면서 두 문제가 드러났다** — 학습 범위 밖 외삽과 장기 감쇠.
4. **해법은 모두 "전 차원에 위치를 넣지 말자"로 수렴한다.**
   partial RoPE, decoupled RoPE, NoPE, HoPE가 나누는 기준만 다를 뿐 같은 방향이다.

그리고 `3.4`가 보여주듯, **위치 인코딩 선택은 attention 구조와 얽혀 있다.**
축을 나눠 정리했지만 독립적이지 않다는 점을 기억해두면 좋다.

---

## Sources

**T1 — 논문**
- Vaswani et al. (2017), *Attention Is All You Need*, arXiv:1706.03762 — Sinusoidal
- Su et al. (2021), *RoFormer: Enhanced Transformer with Rotary Position Embedding*, arXiv:2104.09864 — RoPE
- Press et al. (2021), *Train Short, Test Long: ALiBi*, arXiv:2108.12409
- Chen et al. (2023), *Extending Context Window via Position Interpolation*, arXiv:2306.15595
- Peng et al. (2023), *YaRN: Efficient Context Window Extension*, arXiv:2309.00071
- DeepSeek-AI (2024), *DeepSeek-V2*, arXiv:2405.04434 — decoupled RoPE (`d_r`=64)
- Chi et al. (2024), *HoPE: A Novel Positional Encoding Without Long-Term Decay*, arXiv:2410.21216
- Kazemnejad et al. (2023), *The Impact of Positional Encoding on Length Generalization* — NoPE의 근거
- Moonshot AI (2025), *Kimi Linear*, arXiv:2510.26692 — full 층 NoPE

**T2 — 구현**
- HuggingFace `transformers` RoPE 구현 및 scaling 옵션 (`rope_scaling`)
- 각 모델 `config.json`의 `rope_theta`, `rope_scaling` 필드

**T3 — 참고**
- Sebastian Raschka, *LLM Architecture Gallery* — NoPE·partial RoPE 채택 현황

**미검증 항목**
- HoPE의 대규모 모델 채택 사례 — 확인되지 않음
- HoPE 동명이인 3종의 각 원문 대조 (C2)
