# 00. Foundations — 기준점 세우기

앞으로 나올 모든 모듈은 "무엇을 개선했다"는 이야기다. 그러려면 **무엇으로부터** 개선했는지가
먼저 있어야 한다. 이 파일에서 기준점이 될 평범한 decoder-only Transformer를 하나 세우고,
토큰 하나가 그 안을 어떻게 지나가는지, 그리고 **무엇이 이 구조를 계속 바꾸게 만드는지**를 정리한다.

여기서 정한 기호와 직관은 나머지 파일 전체에서 그대로 쓴다. 각 모듈 문서에서 다시 유도하지 않는다.

**목차**

- [0.1 모델의 뼈대](#01-모델의-뼈대)
- [0.2 토큰 하나의 여정](#02-토큰-하나의-여정)
- [0.3 무엇이 비싼가 — 세 가지 자원](#03-무엇이-비싼가--세-가지-자원)
- [0.4 아키텍처를 움직이는 압력들](#04-아키텍처를-움직이는-압력들)
- [0.5 KV cache](#05-kv-cache)
- [0.6 왜 decode는 memory-bound인가](#06-왜-decode는-memory-bound인가)
- [0.7 prefill과 decode는 다른 작업이다](#07-prefill과-decode는-다른-작업이다)
- [0.8 하드웨어 참고표](#08-하드웨어-참고표)
- [0.9 전체 지도](#09-전체-지도)

---

## 0.1 모델의 뼈대

2019년 GPT-2부터 2026년의 최신 모델까지, 큰 그림은 놀랄 만큼 그대로다.
토큰을 벡터로 바꾸고, 같은 모양의 레이어를 L번 통과시키고, 마지막에 다음 토큰 확률을 뽑는다.

```
    토큰 ID
      │
      ▼
 ┌─────────────┐
 │  Embedding  │   V × d 테이블에서 한 줄 꺼내기
 └─────────────┘
      │
      ▼
 ┌───────────────────────────────┐
 │  Layer  (× L번 반복)          │
 │                               │
 │    x ──┬─ Norm ─ Attention ─┐ │
 │        └────────────────(+)─┤ │   residual
 │        ┌────────────────────┘ │
 │        ├─ Norm ─ FFN ───────┐ │
 │        └────────────────(+)─┤ │   residual
 │                             │ │
 └───────────────────────────────┘
      │
      ▼
 ┌─────────────┐
 │ Final Norm  │
 │  LM Head    │   d × V
 └─────────────┘
      │
      ▼
  다음 토큰 확률
```
> **그림 0.1** — decoder-only Transformer의 뼈대. 이 위키에서 다루는 모든 변형은
> 이 그림의 어느 한 부분을 바꾼 것이다. `01`·`02`는 Attention 블록, `04`는 FFN 블록,
> `05`는 Norm과 (+) 화살표, `03`은 Attention 안의 위치 정보, `06`은 이 그림의
> 세로 길이와 가로 폭 자체를 건드린다.

### 기호

앞으로 계속 쓸 기호다. 예시 값은 Llama 3 70B 기준이다.

| 기호 | 의미 | 예시값 |
|---|---|---|
| `B` | 배치 크기 (동시에 처리하는 시퀀스 수) | 1 ~ 512 |
| `S` | 컨텍스트 길이 = 지금까지 쌓인 토큰 수 | 4K ~ 1M |
| `L` | 레이어 수 | 80 |
| `d` | 모델 폭 (hidden size) | 8192 |
| `n_h` | 쿼리 헤드 수 | 64 |
| `n_kv` | KV 헤드 수 | 8 |
| `g` | 그룹 크기 `= n_h / n_kv` | 8 |
| `d_h` | 헤드 하나의 차원 | 128 |
| `d_ff` | FFN 중간 차원 | 28672 |
| `V` | vocabulary 크기 | 128256 |
| `b` | 값 하나의 바이트 수 | BF16 → 2 |
| `P` | 파라미터 수 (MoE는 `P_tot` / `P_act` 구분) | 70B |

MoE와 MLA 전용 기호(`E`, `k`, `d_c`, `d_r` 등)는 해당 파일에서 그때 소개한다.

---

## 0.2 토큰 하나의 여정

### 먼저, 두 단계를 구분해야 한다

LLM이 답을 만드는 과정은 성격이 아주 다른 두 단계로 나뉜다.

| | **prefill** | **decode** |
|---|---|---|
| 하는 일 | 프롬프트 전체를 한 번에 읽음 | 토큰을 하나씩 생성 |
| 처리하는 토큰 | `S`개 동시 | 1개 |
| 반복 횟수 | 1번 | 출력 길이만큼 |
| 사용자가 체감하는 것 | 첫 글자가 뜨기까지의 시간 | 글자가 흘러나오는 속도 |

이 구분이 중요한 이유는 **같은 모델, 같은 가중치인데 두 단계의 병목이 정반대**이기 때문이다.
`0.7`에서 자세히 본다. 아래에서는 decode 한 스텝을 따라간다.

### decode 한 스텝에서 벌어지는 일

| # | 어디서 | 하는 일 | 성격 |
|---|---|---|---|
| 1 | 입구 | 토큰 ID로 임베딩 테이블에서 한 줄 꺼내기 | 테이블 조회 |
| 2 | **레이어 1~L 반복** | ↓ 3~9번을 `L`번 | |
| 3 | | 입력 정규화 | 가벼움 |
| 4 | | `q`, `k`, `v` 만들기 (`x`에 `W`들을 곱함) | 가중치 읽기 |
| 5 | | `q`, `k`에 위치 정보 입히기 (RoPE) | 가벼움 |
| 6 | | 방금 만든 `k`, `v`를 **캐시 뒤에 붙이기** | 메모리 쓰기 |
| 7 | | `q`와 **캐시 안의 K, V 전부**로 attention | **캐시 전체 읽기** |
| 8 | | 결과를 `Wo`로 되돌리고 residual에 더하기 | 가중치 읽기 |
| 9 | | 정규화 → FFN → residual | 가중치 읽기 (**가장 큼**) |
| 10 | 출구 | 마지막 정규화 → LM Head로 `V`개 점수 | 가중치 읽기 |

여기서 짚어둘 것이 두 가지 있다.

**첫째, 짧은 컨텍스트에서 가장 비싼 건 9번(FFN)이다.** 파라미터의 대부분이 FFN에 있고,
토큰 하나를 만들 때마다 그걸 전부 읽어야 한다.

**둘째, 컨텍스트 길이 `S`에 비례해서 커지는 건 7번뿐이다.** 4, 8, 9번은 컨텍스트가
1K든 1M이든 하는 일이 똑같다. 그래서 컨텍스트가 길어질수록 7번의 비중이 계속 커진다.

이 둘이 나뉘어 있다는 점이 중요하다. `04-moe`는 9번을 공격하고, `01`·`02`는 7번을
공격한다. **서로 다른 문제를 푸는 것이고, 둘 다 필요하다.**

---

## 0.3 무엇이 비싼가 — 세 가지 자원

성능 이야기를 할 때 섞이기 쉬운 세 가지를 먼저 분리해두자.

| 자원 | 무엇 | 부족하면 |
|---|---|---|
| **연산량** (FLOPs) | GPU가 해야 할 곱셈·덧셈의 수 | 느려진다 (compute-bound) |
| **메모리 이동량** (bytes/s) | HBM에서 읽어와야 할 데이터의 양 | 느려진다 (memory-bound) |
| **메모리 용량** (bytes) | 담아둬야 하는 데이터의 양 | **아예 못 돌린다** |

세 번째가 성격이 다르다. 용량 부족은 "조금 느려지는" 문제가 아니라 "이 설정으로는 서빙이
불가능하다"는 문제다. 그래서 아키텍처 설계에서 가장 먼저 걸리는 제약인 경우가 많다.

여기에 하나를 더 얹어야 한다.

| 자원 | 무엇 | 부족하면 |
|---|---|---|
| **통신** (GPU 간 대역폭) | 여러 GPU에 흩어진 모델이 주고받는 양 | 느려진다 (communication-bound) |

큰 모델은 한 장에 안 들어간다. 그러면 GPU끼리 중간 결과를 주고받아야 하고,
그 대역폭은 HBM보다 훨씬 좁다. MoE처럼 통신을 많이 쓰는 구조에서는
**이쪽이 진짜 병목**이 되기도 한다.

### 연산의 종류

각 모듈 문서의 "코드와 텐서" 절에서는 어떤 연산이 일어나는지를 아래 용어로 표시한다.
종류마다 비용의 성격이 다르기 때문에 구분해두면 병목을 짚기 쉽다.

| 종류 | 무엇 | 비용의 성격 |
|---|---|---|
| **GEMM** | 행렬 × 행렬 | 연산량이 크다. **compute-bound**가 되기 쉽다 |
| **GEMV** | 행렬 × 벡터 | 가중치를 읽는 만큼만 계산한다. **memory-bound** |
| **batched GEMV** | 배치·헤드마다 GEMV 반복 | attention 점수 계산이 여기 해당 |
| **elementwise** | 원소별 연산 (곱, 활성함수, RoPE 회전) | 연산은 거의 없고 **읽고 쓰는 게 전부** |
| **reduction** | 축을 따라 합·최대 (softmax, norm) | 동기화가 필요해 병렬화가 덜 됨 |
| **memory write / read** | 캐시 append, 가중치 로드 | 순수 메모리 트래픽 |
| **gather** | 흩어진 위치에서 모아 읽기 | 연속 읽기보다 **실효 대역폭이 낮다** |

핵심은 **GEMM과 GEMV의 차이**다. 같은 가중치 행렬을 쓰는 같은 연산인데,
곱하는 대상이 벡터 하나면 GEMV(memory-bound), 여러 개를 묶은 행렬이면 GEMM(compute-bound)이
된다. prefill과 decode의 성격이 갈리는 지점이 정확히 여기다 (`0.7`).

그리고 **gather**는 `01-attention`의 희소 계열에서 새로 등장한다.
읽는 양은 줄었는데 흩어져 있어서 기대만큼 빨라지지 않는 현상의 원인이다.

---

## 0.4 아키텍처를 움직이는 압력들

뼈대는 7년째 그대로인데 세부는 계속 바뀌어 왔다. 무엇이 바꾸게 만드는가?
크게 여섯 가지 압력으로 정리된다. **이 위키의 각 파일은 대체로 하나의 압력에 대응한다.**

| 압력 | 무엇이 문제인가 | 대표적인 대응 | 파일 |
|---|---|---|---|
| **메모리** | 컨텍스트가 길어지면 KV cache를 감당할 수 없다 | KV 압축·희소화, 고정 state, 양자화 | 01, 02, 08 |
| **연산량** | 모델을 키우면 토큰당 연산이 그대로 늘어난다 | MoE — 파라미터는 늘리되 활성은 고정 | 04 |
| **직렬 지연** | 레이어는 순서대로 통과할 수밖에 없다 | 깊이↓ 너비↑, parallel block, 한 스텝에 여러 토큰 | 06, 07 |
| **통신** | 모델이 여러 GPU에 흩어져 있다 | 병렬화 전략, EP의 all-to-all | 04, 09 |
| **안정성** | 깊고 큰 모델은 학습이 잘 터진다 | 정규화 위치, QK-Norm, residual 구조 | 05 |
| **길이 일반화** | 학습 때보다 긴 입력을 어떻게 다루나 | 위치 인코딩과 확장 기법 | 03 |

압력마다 성격이 다르다는 점을 봐두면 좋다. 메모리·연산량·통신·지연은 **비용** 문제이고,
안정성과 길이 일반화는 **품질** 문제다. RMSNorm이나 RoPE가 나온 이유는 빨라지려는 게
아니었다. 이 위키는 비용 쪽에 무게를 두지만, 품질 쪽 압력도 같이 다뤄야 왜 그런 선택을
했는지가 설명된다.

그리고 이 위키가 매번 묻게 될 질문이 하나 있다.

> **한 압력을 풀면 대개 다른 압력으로 옮겨간다.**
> MoE는 연산량을 풀지만 통신 부담을 만든다. 희소 attention은 읽는 양을 줄이지만
> 메모리 접근을 흩어놓는다. 레이어를 줄이면 지연은 나아지지만 폭이 넓어져 통신이 는다.
>
> **공짜인 구조는 없다. 무엇을 무엇과 바꿨는가 — 이게 각 모듈에서 확인할 것이다.**

최근 몇 년 동안 가장 크게 자란 압력은 메모리 쪽이다. 컨텍스트 길이가 4K에서 1M으로
늘어나는 동안 KV cache가 그에 비례해 커졌기 때문이다. 그래서 다음 절에서 이걸 먼저 본다.
다만 **이게 전부는 아니다.** MoE가 없으면 요즘 모델 크기가 성립하지 않고,
정규화 없이는 애초에 학습이 안 된다.

---

## 0.5 KV cache

### 왜 필요한가

decode에서 새 토큰의 `q`는 **이전 모든 토큰의 `k`, `v`와** 상호작용해야 한다.
그런데 이전 토큰들의 `k`, `v`는 이미 계산했던 값이고 바뀌지 않는다.
매번 다시 계산하면 토큰 하나당 `O(S)`의 낭비가 생긴다.

그래서 저장해둔다. 이게 KV cache다. **연산량을 메모리와 맞바꾼 것**이고,
`01-attention`의 계보는 그 거래의 대가를 치르는 과정이다.

### 얼마나 큰가

토큰 하나가 레이어 하나에서 남기는 양은 이렇다.

```
2 (K와 V) × n_kv × d_h × b  bytes
```

전체 KV cache는 여기에 `B × S × L`을 곱한 값이다.

Llama 3 70B로 감을 잡아보자. BF16(`b`=2), `n_kv`=8, `d_h`=128, `L`=80이다.

| 단위 | 계산 | 크기 |
|---|---|---|
| 토큰 1개 · 레이어 1개 | `2 × 8 × 128 × 2` | 4 KB |
| 토큰 1개 (전 레이어) | `× 80` | **320 KB** |
| 시퀀스 1개 @ 32K | `× 32,768` | **10 GB** |
| 배치 32 @ 32K | `× 32` | **320 GB** |

> 💡 모델 가중치가 BF16으로 약 140 GB다. **배치 32에 컨텍스트 32K면 KV가 가중치의
> 2배를 넘는다.** 그리고 이건 이미 GQA로 8배 줄인 뒤의 숫자다.
> MHA였다면 (`n_kv`=64) 여기에 8을 더 곱해야 한다 — 2.5 TB.

📌 위 숫자는 감을 잡기 위한 개략 계산이다. 실제 서빙에서는 페이지 단위 할당에 따른
단편화, 프레임워크 오버헤드 등이 더 붙는다.

### 두 가지 성질

KV cache에는 앞으로 계속 따라다닐 성질이 둘 있다.

1. **컨텍스트 길이에 비례해서 커진다.** 1M 컨텍스트는 32K의 32배를 먹는다.
2. **사용자마다 다르다.** 배치를 키우면 그만큼 그대로 늘어난다.

두 번째가 특히 고약하다. 왜 그런지는 다음 절에서 본다.

---

## 0.6 왜 decode는 memory-bound인가

### 직관

decode에서 토큰 하나를 만들려면 **모델 가중치를 전부 한 번씩 읽어야 한다.**
70B 모델을 BF16으로 돌리면 토큰 하나당 140 GB를 HBM에서 읽어오는 것이다.

그렇게 읽어온 가중치로 하는 계산은? 값 하나당 곱셈 한 번, 덧셈 한 번.
**2바이트를 읽어서 2 FLOPs를 한다.** 바이트당 1 FLOP이다.

H100의 균형점은 대략 150 FLOP/byte다 (`0.8` 참고). 이 GPU는 1바이트를 읽어오는
동안 150번 계산할 수 있는데, 우리는 1번밖에 안 시킨다.

> **GPU가 계산하는 시간보다 데이터를 기다리는 시간이 100배 이상 길다.**
> decode 속도는 사실상 HBM 대역폭이 정한다.

### 배치를 키우면 되지 않나 — 절반만 맞다

배치를 `B`로 키우면 가중치는 한 번만 읽어서 `B`개 토큰에 다 쓸 수 있다.
읽은 바이트당 계산량이 `B`배가 되니, 배치를 충분히 키우면 FFN은 compute-bound로 넘어간다.
실제로 서빙에서 배치를 키우는 이유가 이것이다.

그런데 KV cache는 그게 안 된다. **사용자마다 대화 내용이 다르니 KV도 다르다.**
배치가 32면 32명분의 KV를 전부 읽어야 한다.

| | 읽는 양 (배치 1) | 배치 `B`일 때 | 배치로 나눠지나 |
|---|---|---|---|
| 모델 가중치 | `P × b` | `P × b` — **그대로** | ✅ `B`배 이득 |
| KV cache | 토큰당 KV `× S × L` | **`× B`** — 그대로 늘어남 | ❌ 이득 없음 |

> **가중치 읽기는 배치로 amortize되지만, KV 읽기는 되지 않는다.**
> 그래서 배치를 아무리 키워도 attention은 memory-bound에서 빠져나오지 못한다.

이 비대칭이 `01-attention`의 KV 압축 계보와 `02-linear-attention`의 고정 state 계보를
동시에 밀어올린 힘이다. 반대로 `04-moe`는 이 비대칭과는 무관한, 연산량 쪽 이야기다.

---

## 0.7 prefill과 decode는 다른 작업이다

같은 가중치, 같은 코드인데 두 단계의 성격은 정반대다.

| | **prefill** | **decode** |
|---|---|---|
| 한 번에 처리 | `S`개 토큰 | 1개 토큰 |
| 연산 형태 | GEMM (행렬 × 행렬) | GEMV (행렬 × 벡터) |
| 가중치 재사용 | `S`개 토큰이 공유 → 높음 | 1개 토큰만 → 낮음 |
| **병목** | **연산 성능** | **메모리 대역폭** |
| attention 비용 | `S²`에 비례 | `S`에 비례 |
| GPU 활용률 | 높음 | 낮음 |
| 지표 | TTFT (첫 토큰까지) | TPOT (토큰 간 간격) |

같은 GPU에서 둘을 섞어 돌리면 서로를 방해한다. 긴 prefill이 들어오면 진행 중이던
decode가 밀려서 출력이 뚝뚝 끊긴다. 이 문제와 해법(chunked prefill, PD 분리)은
`09-serving`에서 다룬다.

지금 기억할 건 하나다. **어떤 모듈의 비용을 이야기할 때는 prefill인지 decode인지 반드시
함께 말해야 한다.** 이 위키의 모듈 문서들은 별도 언급이 없으면 decode 기준이다.

---

## 0.8 하드웨어 참고표

모듈이 시스템에 무엇을 요구하는지 이야기하려면 기준이 될 하드웨어 숫자가 있어야 한다.

| GPU | 메모리 | 대역폭 | BF16 (dense) | 균형점 |
|---|---|---|---|---|
| A100 80GB SXM | 80 GB HBM2e | 2.0 TB/s | ~312 TFLOPS | ~156 |
| H100 SXM | 80 GB HBM3 | 3.35 TB/s | ~495 TFLOPS | ~148 |
| H200 SXM | 141 GB HBM3e | 4.8 TB/s | ~495 TFLOPS | ~103 |
| **B200** | **192 GB** HBM3e | **8.0 TB/s** | **2.25 PFLOPS** | **~281** |
| **R100 (Rubin)** | **288 GB** HBM4 (8 스택) | **22 TB/s** | 미공개 | — |

📌 B200의 다른 정밀도: FP8 4.5 PFLOPS, **FP4 9 PFLOPS** (전부 dense 기준.
sparsity 포함 표기는 이 값의 2배다).

📌 R100은 **NVFP4 추론 50 PFLOPS**, NVFP4 학습 35 PFLOPS로 발표되었다.
BF16 dense 수치는 확인하지 못했다.

> 📎 B200·R100 값은 여러 자료가 일치하지만 **공식 데이터시트 PDF를 직접 대조한 것은 아니다.**
> 다만 R100의 22 TB/s는 **8개 HBM4 스택 합산**이라 스택당 ~2.75 TB/s로,
> 처음에 의심했던 것과 달리 무리한 수치가 아니다 (`CONTESTED.md` C4).

**균형점(machine balance)** 은 `연산 성능 ÷ 메모리 대역폭`이다.
"1바이트를 읽어오는 동안 몇 번 계산할 수 있는가"를 뜻한다.
어떤 연산이 바이트당 하는 계산이 이 값보다 적으면 memory-bound다.
`0.6`에서 본 decode는 바이트당 1~2 FLOP이었으니 한참 아래다.

> ⚠️ 벤더 자료의 TFLOPS는 **sparsity 2배를 포함한 값**인 경우가 많다.
> 위 표는 dense 기준으로 맞춘 것이다. 다른 자료와 비교할 때 주의.

세대별 흐름에서 읽을 것은 두 가지다.

- **메모리 용량이 빠르게 늘고 있다** (80 → 141 → 192 → 288 GB).
  메모리 압력에 대한 하드웨어 쪽 대답이다.
- **연산 성능이 대역폭보다 빠르게 늘어난다.** H100의 균형점이 ~148인데 B200은 ~281이다.
  **두 세대 만에 두 배**가 됐다. memory-bound인 decode에는 불리한 방향이고,
  세대가 지날수록 **읽는 양을 줄이는 일의 가치가 커진다.**

R100에서 FP4로 계산해보면 이 경향이 더 뚜렷하다.
50 PFLOPS ÷ 22 TB/s ≈ **2,270 FLOP/byte**다. `0.6`에서 본 decode의 바이트당 1~2 FLOP과
비교하면 **천 배 이상 벌어져 있다.**

> 💡 정밀도가 다르므로 BF16 균형점과 직접 비교할 수는 없다.
> 하지만 **FP4로 서빙한다면 실제로 마주할 숫자**가 이것이고,
> 방향은 분명하다 — 연산은 넘쳐나고 데이터를 나르는 게 문제다.

GPU 간 연결(NVLink 등)은 통신 압력과 직결되므로 `04-moe`와 `09-serving`에서 따로 다룬다.

---

## 0.9 전체 지도

`0.4`의 압력별로, 각 파일이 어디를 건드리는지 펼치면 이렇게 된다.

```
【메모리 압력】 KV cache가 B × S에 비례해 커지고, 배치로 amortize되지 않는다
   ├─ 저장량을 줄이자 ──────────────────────► 01-attention
   │     헤드 공유(MQA·GQA) · 저차원 압축(MLA)
   │     일부만 읽기(SWA·NSA·MoBA·DSA·CSA) · 레이어 공유(CLA·YOCO)
   ├─ 아예 고정 크기로 ─────────────────────► 02-linear-attention
   │     Linear Attention · Mamba · DeltaNet · KDA
   ├─ 한 값당 비트를 줄이자 ────────────────► 08-numerics
   └─ 재사용하고 알뜰하게 관리하자 ─────────► 09-serving
         prefix caching · PagedAttention

【연산량 압력】 모델을 키우면 토큰당 연산이 그대로 늘어난다
   └─ 파라미터는 늘리되 활성은 고정 ────────► 04-moe
         MoE · fine-grained expert · shared expert

【통신 압력】 모델이 여러 GPU에 흩어져 있다
   ├─ expert를 어떻게 배치할까 ─────────────► 04-moe
   └─ 무엇을 쪼개고 무엇을 주고받을까 ──────► 09-serving
         TP · EP · PP · CP

【직렬 지연 압력】 레이어는 순서대로 통과해야 한다
   ├─ 깊이와 너비의 균형 ───────────────────► 06-shape
   └─ 한 스텝에 토큰 여러 개 ───────────────► 07-decoding
         MTP · speculative decoding

【안정성 압력】 깊고 큰 모델은 학습이 잘 터진다
   └─ 정규화와 residual 구조 ───────────────► 05-norm-residual

【길이 일반화 압력】 학습보다 긴 입력을 다뤄야 한다
   └─ 위치 인코딩과 확장 ───────────────────► 03-position

【종합】 누가 무엇을 골랐고, 그 조합이 시스템에 무엇을 요구하나
   └───────────────────────────────────────► 99-landscape
```

---

## 이 다음

`01-attention.md`로 넘어가면 메모리 압력의 첫 번째 갈래를 따라간다.
MHA에서 시작해 DeepSeek-V4의 CSA/HCA까지, "KV를 어떻게 줄일 것인가"라는 질문에 대한
열 가지 대답을 계보 순서로 본다.

---

## Sources

**T1 — 논문·공식 자료**
- Vaswani et al., *Attention Is All You Need* (arXiv:1706.03762) — Transformer 원형
- Llama 3 / DeepSeek-V3 등 각 모델의 HuggingFace `config.json` — 표의 예시값 출처

**T2 — 구현·하드웨어**
- NVIDIA 데이터시트 (A100 / H100 / H200 / B200) — `0.8` 스펙표
  ⚠️ B200·R100 항목은 미확인. `CONTESTED.md` C4 참조
- vLLM, *Efficient Memory Management for LLM Serving with PagedAttention* (arXiv:2309.06180)
  — KV cache 단편화와 실제 메모리 사용량

**T3 — 참고**
- Sebastian Raschka, *The Big LLM Architecture Comparison* — 뼈대가 그대로라는 관점

**미검증 항목**
- `0.8` B200·Rubin 스펙 (C4)
